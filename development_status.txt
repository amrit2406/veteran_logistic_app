# Veteran Logistics - Product & Implementation Summary

## 1. Overview

This document records the implementation work completed across the repository and maps it to the current folder structure. It captures architecture decisions, the main files added or updated, DI registrations, configuration changes, and guidance for future extension.

## 2. Development Phases

### Phase 0.1: Repository Initialization
* **Goal**: Create the repository and basic project scaffolding.
* **Details**: Established the initial solution layout, repository metadata, and top-level WPF project structure.

### Phase 0.2: Core Packages & Build
* **Goal**: Add and validate essential NuGet packages and build settings.
* **Details**: Added the core Microsoft, Entity Framework Core, CommunityToolkit.Mvvm, and Serilog packages required for the desktop application.

### Phase 0.3: Solution & Project Structure
* **Goal**: Define the solution and project layout.
* **Details**: Organized the code into the current folder structure:
  * `Configuration`
  * `DependencyInjection`
  * `Desktop`
  * `MVVM`
  * `Options`
  * `Resources`
  * `Styles`
  * `Themes`
  * `VeteranLogistics.Data`
  * `VeteranLogistics.Shared`

### Phase 0.4: Shell & Window Scaffold
* **Goal**: Implement the main application window and shell skeleton.
* **Details**: Implemented `MainWindow`, `ShellView`, and `ShellViewModel` to host the application shell through `DataTemplate` mapping.

### Phase 0.5: Dependency Injection Scaffolding
* **Goal**: Establish the core dependency injection infrastructure.
* **Details**: Created the application composition root and supporting DI registration helpers under `DependencyInjection`.

### Phase 0.6: Configuration & Options
* **Goal**: Introduce strongly typed configuration.
* **Details**: Added options models under `Options` for application, database, feature, logging, and reporting settings, with host configuration bound from `appsettings.json`.

### Phase 0.7: Navigation
* **Goal**: Prepare the navigation infrastructure.
* **Details**: Added navigation abstractions and implementations under `Desktop\Navigation`, including `INavigationService`, `IViewModelFactory`, `NavigationService`, and `ViewModelFactory`.

### Phase 0.8: Dialog & Notification Infrastructure
* **Goal**: Provide a reusable UI feedback abstraction to decouple ViewModels from direct UI calls.
* **Key Components**:
  * `IDialogService` and `DialogService`
  * `INotificationService` and `NotificationService`
  * Supporting models and enums such as `DialogOptions`, `DialogResult`, `DialogType`, `NotificationMessage`, and `NotificationType`
* **DI Registration**: Registered as singletons through the application composition root.

### Phase 0.9: Logging & Global Exception Handling
* **Goal**: Implement enterprise-grade logging and global exception handling.
* **Logging (Serilog)**:
  * **Configuration**: Managed through `appsettings.json` and `Configuration/HostConfiguration.cs`.
  * **Sinks**: Configured for Console, Debug, and a daily rolling file under `Logs`.
  * **Performance**: Uses `Serilog.Sinks.Async` to avoid blocking UI threads.
  * **Enrichment**: Adds application, version, environment, machine, process, and thread metadata to each log entry.
* **Global Exception Handling**:
  * **Hooks**: `DispatcherUnhandledException`, `AppDomain.CurrentDomain.UnhandledException`, and `TaskScheduler.UnobservedTaskException` are handled in `App.xaml.cs`.
  * **Behavior**: Exceptions are logged and a user-friendly error dialog is shown through `IDialogService`.

### Phase 0.10: Theme Infrastructure
* **Goal**: Establish a centralized UI design system for the WPF shell.
* **Details**: Added foundational color, brush, typography, and spacing resources plus reusable control styles under `Styles`.
* **Theme Layer**: Added `Themes/LightTheme.xaml` and `Themes/ThemeManager.cs` to support future theme switching without changing views.
* **Shell Update**: Updated the shell and data templates to consume the shared resource dictionaries instead of hardcoded colors.

### Package Security Maintenance
* **Goal**: Remediate known vulnerabilities in NuGet dependencies.
* **Updates**:
  * `Microsoft.Data.SqlClient` -> `5.1.3`
  * `Azure.Identity` -> `1.11.4`
  * `System.IdentityModel.Tokens.Jwt` -> `6.34.0`
* **Validation**: Packages were restored and the build was validated after the updates.

### Phase 1.1: Authentication Infrastructure
* **Goal**: Add the authentication infrastructure scaffolding only, without implementing login, authorization, or persistence behavior.
* **Included**:
  * Authentication contracts for service, session, and application context abstractions
  * Simple runtime models for authenticated user, authentication state, and user session
  * Runtime-only session and application context holders
  * Centralized DI registration for the authentication infrastructure
* **Explicitly Excluded**:
  * Repository, EF queries, database initializer, password handling, validators, audit logging, login UI, ViewModels, authorization, permissions, remember me, security workflow, and login implementation
* **Validation Status**: Build verification was attempted, but the environment could not restore NuGet packages because network access to `nuget.org` is blocked in this workspace.

### Phase 1.2: Authentication Persistence & Security
* **Goal**: Add authentication persistence, password hashing, password policy, and validation infrastructure.
* **Included**:
  * Authentication contracts for repository, password hasher, password policy, and audit service
  * Models for password hash result and authentication audit entry
  * PBKDF2 password hashing implementation with HMAC-SHA256 (128-bit salt, 100,000 iterations, 256-bit hash)
  * Password policy validation requiring 8+ characters with uppercase, lowercase, digit, and special character
  * EF Core repository for user data access with async methods
  * User entity in data layer with username, password hash, salt, display name, email, role, and active status
  * Authentication seed for default administrator account reading from configuration
  * AuthenticationOptions for default admin credentials configuration
  * DI registration for Phase 1.2 services (repository as scoped, hasher/policy as singleton)
  * Updated ApplicationDbContext with Users DbSet
  * Added Microsoft.AspNetCore.Cryptography.KeyDerivation package for PBKDF2 support
  * EF Core migrations for database schema creation (InitialCreate migration)
  * DesignTimeDbContextFactory for design-time DbContext creation (migrations)
  * Database seeding in App.xaml.cs startup to ensure default admin user exists
  * Connection string configuration in appsettings.json under ConnectionStrings section
  * Data layer DI registration in main ServiceCollectionExtensions
* **Explicitly Excluded**:
  * Login Window, Login ViewModel, authentication workflow, session creation, Remember Me, Financial Year selection, authorization, permissions, logout, navigation, business workflow, and UI components
  * Login request models and validators (moved to Phase 1.4)
* **Validation Status**: Build verified successfully with no errors. Database migrations created and applied. Default admin user seeded on startup.

### Phase 1.3: Authentication UI
* **Goal**: Implement the Login screen UI with ViewModel and View.
* **Included**:
  * LoginViewModel with username, password, error message, and password visibility properties
  * LoginView XAML with branded login card, username/password inputs, password toggle, error display, loading indicator, and exit button
  * DataTemplate mapping for LoginViewModel to LoginView in Resources/DataTemplates.xaml
  * DI registration for LoginViewModel as transient
  * PasswordBox code-behind for secure password binding
* **Explicitly Excluded**:
  * Authentication workflow, business logic, repository calls, session creation, navigation to Shell, Remember Me, Financial Year selection, authorization, permissions, logout, and security workflow
* **Validation Status**: Build verified successfully with no errors.

### Phase 1.4: User Authentication Workflow
* **Goal**: Implement the complete authentication workflow by connecting the Login UI with the authentication infrastructure.
* **Included**:
  * LoginRequest model  (already existed from Phase 1.2)
  * LoginRequestValidator for validating username and password presence
  * AuthenticationResult model for operation results with Success/Failure factory methods
  * AuthenticationAuditService implementation for audit logging
  * AuthenticationService implementation with full workflow:
    - Validates LoginRequest using LoginRequestValidator
    - Retrieves user using IAuthenticationRepository
    - Verifies password using IPasswordHasher
    - Records authentication attempt using IAuthenticationAuditService
    - On success: Creates UserSession, stores in ISessionManager, updates IApplicationContext, sets AuthenticationState, navigates to Shell
    - On failure: Returns error message, keeps Login screen visible
  * Updated IAuthenticationService contract with AuthenticateAsync method
  * Updated LoginViewModel to use AuthenticationService and INavigationService
  * AuthenticationWorkflowServiceCollectionExtensions for DI registration
  * Value converters: NullToVisibilityConverter and BooleanToVisibilityConverter
  * Updated MainWindow to use LoginViewModel instead of ShellViewModel (Login-first startup)
  * Updated ShellViewModel registration from Singleton to Transient for proper navigation lifecycle
  * Updated LoginView to bind ErrorMessage and IsBusy with visibility converters
  * Updated App.xaml to register converters
* **Explicitly Excluded**:
  * Remember Me, Financial Year Selection, Role-Based Authorization, Permission-Based Authorization, Logout, Password Reset, Multi-Factor Authentication, Account Lockout, Session Persistence, and License Checking
* **Validation Status**: Build verified successfully with no errors.

### Phase 1.4A: Post-Implementation Cleanup & Shell Navigation Fix
* **Goal**: Resolve Phase 1.4 follow-up issues without changing authentication workflow behavior or refactoring unrelated code.
* **Included**:
  * **DI cleanup** (`AuthenticationWorkflowServiceCollectionExtensions`):
    - Removed duplicate registrations already owned by earlier phases
    - Extension now registers only Phase 1.4 services: `IAuthenticationService` and `IAuthenticationAuditService`
    - Left `AddAuthenticationPersistence` (Phase 1.2) and `AddAuthenticationUI` (Phase 1.3) unchanged for repository, password hasher, password policy, and `LoginViewModel`
  * **Code cleanup** (`LoginViewModel`):
    - Removed temporary debug comments
    - No functional changes to login validation or navigation
  * **Shell navigation fix** (`ShellViewModel`):
    - Fixed infinite Shell nesting after successful login
    - Root cause: after login, `NavigationService` current VM became `ShellViewModel`, and `ShellViewModel` mirrored that into its inner `CurrentViewModel`; the `ShellViewModel` DataTemplate then rendered `ShellView` inside itself repeatedly
    - Added `ResolveShellContent()` so the shell content area ignores top-level navigation targets (`ShellViewModel`, `LoginViewModel`) and shows `PlaceholderViewModel` instead
    - Main window still navigates to the full Shell chrome (header, menu, status bar) after login; only the inner content area uses the placeholder until a module ViewModel is navigated in a later phase
* **Explicitly Unchanged**:
  * Authentication workflow, login UI, startup flow, session creation, validation logic, authentication service, repository, and password hasher
* **Validation Status**: Build verified successfully with no errors. Login-to-Shell navigation confirmed stable with no recursive rendering.

### Phase 1.5: Remember Me
* **Goal**: Implement a secure Remember Me feature that remembers the users login preference and username without bypassing authentication.
* **Included**:
  * `IRememberMeService` contract for loading, saving, and clearing Remember Me settings
  * `RememberMeSettings` model containing only `Username` and `RememberMe` flag
  * `RememberMeService` implementation persisting settings to a local JSON file at `%LocalAppData%/{ApplicationName}/Settings/remember-me.json`
  * `AuthenticationRememberMeServiceCollectionExtensions` for DI registration of `IRememberMeService` as singleton
  * Updated `LoginViewModel`:
    - Added `RememberMe` property
    - Loads saved settings on startup and restores username and checkbox state when Remember Me was previously enabled
    - After successful authentication: saves username and flag when checked, clears saved settings when unchecked
    - Does not restore password, auto-authenticate, auto-navigate, or create a session on startup
  * Updated `LoginView.xaml` with Remember Me checkbox bound to `RememberMe`
  * Updated `ServiceCollectionExtensions` to call `AddAuthenticationRememberMe()`
  * Graceful handling of missing or corrupted settings files via logging and default values
* **Storage Rules**:
  * Persisted: Username, RememberMe flag only
  * Never stored: Password, PasswordHash, Salt, Token, Session, UserId, Role, AuthenticationState
* **Explicitly Excluded**:
  * Automatic login, session persistence, Financial Year Selection, authorization, permissions, logout, password reset, MFA, Windows Credential Manager, and cloud synchronization
* **Validation Status**: Build verified successfully with no errors. Application still requires password entry and manual login even when Remember Me is enabled.

### Phase 1.5A: Post-Implementation Architectural Improvements
* **Goal**: Harden the Phase 1.5 Remember Me implementation without changing authentication workflow behavior or refactoring unrelated code.
* **Included**:
  * **ViewModel lifecycle improvement** (`LoginViewModel`):
    - Removed synchronous file I/O from the constructor (`RestoreRememberMeSettings()` with blocking `.GetAwaiter().GetResult()`)
    - Constructor now only assigns dependencies and remains lightweight
    - Overrode `InitializeAsync()` from `ViewModelBase` to load Remember Me settings asynchronously
    - `InitializeAsync()` restores username and Remember Me checkbox state when previously enabled, then marks the ViewModel initialized
    - Renamed restore helper to `RestoreRememberMeSettingsAsync()` for proper async flow
  * **Login screen startup** (`MainWindow`):
    - Invokes `loginViewModel.InitializeAsync()` on `Loaded` so Remember Me restoration runs at Login screen startup without changing navigation or authentication workflow
  * **Remember Me service robustness** (`RememberMeService`):
    - Added `EnsureSettingsDirectoryExists()` to create the Settings directory automatically when missing
    - Called from constructor and `SaveAsync()` so storage is ready before read/write operations
    - Missing JSON file continues to return default `RememberMeSettings`
    - Corrupted or unreadable JSON is handled with a broad exception catch, warning log via `ILogger`, and default settings fallback
    - Directory creation failures are logged and do not throw; application startup cannot fail because of Remember Me storage
  * **Code cleanup**:
    - Removed unused empty `TextBox_TextChanged` handler from `LoginView.xaml.cs`
    - No functional changes to login validation, persistence rules, or navigation
* **Explicitly Unchanged**:
  * Authentication workflow, password verification, session creation, login UI layout, navigation, `AuthenticationService`, repository, password hasher, and `ApplicationContext`
  * No automatic login, password storage, session persistence, Windows Credential Manager, Financial Year Selection, authorization, permissions, or logout
* **Validation Status**: Build verified successfully with no errors. Application still starts on the Login screen, restores only username and Remember Me state, requires password entry, and navigates to Shell only after successful authentication.

### Phase 1.6: Financial Year Selection
* **Goal**: Require users to select a financial year after successful authentication and before accessing the main application shell.
* **Included**:
  * Financial Year domain models and selection result (`FinancialYear`, `FinancialYearSelectionResult`).
  * Contracts for data retrieval and logic (`IFinancialYearRepository`, `IFinancialYearService`).
  * Session state representation (`IFinancialYearContext`) added to `IApplicationContext`.
  * `FinancialYearSelectionViewModel` to load options, manage selection, and perform navigation.
  * `FinancialYearSelectionView` containing the UI (combobox, buttons, error messages).
  * Navigation updated in `LoginViewModel` to navigate to the Financial Year selection screen upon successful login instead of the Shell.

### Phase 1.6A: Financial Year Architectural Improvements
* **Goal**: Clean up the Financial Year implementation by properly decoupling it from the Authentication module and correcting view rendering.
* **Included**:
  * Extracted the entire Financial Year feature out of the `Authentication` folder into a dedicated top-level `FinancialYear` module.
  * Refactored namespaces, references, and Dependency Injection to point to the new `FinancialYear` domain.
  * Corrected missing `DataTemplate` mapping for `FinancialYearSelectionViewModel` in `DataTemplates.xaml` to ensure the correct View renders.
* **Validation Status**: Build verified successfully with no errors. Application navigates cleanly: Login -> Financial Year Selection -> Shell.

### Phase 1.7: Session Management
* **Goal**: Strengthen the runtime session infrastructure with metadata, validation, lifecycle management, and events.
* **Included**:
  * **Created** `Authentication/Models/SessionInfo.cs`: Read-only model providing session metadata for external consumers (SessionId, LoginTime, LastActivity, AuthenticatedUser, FinancialYear).
  * **Created** `Authentication/Events/SessionChangedEventArgs.cs`: Standard .NET event args for SessionChanged event with SessionInfo property.
  * **Modified** `Authentication/Contracts/ISessionManager.cs`: Added properties (HasActiveSession, SessionId, LoginTime, LastActivity), event (SessionChanged), and methods (IsSessionValid, UpdateActivity, ClearSession).
  * **Modified** `Authentication/Models/UserSession.cs`: Extended with new properties (LoginTime, LastActivity, AuthenticatedUser, FinancialYear) while preserving existing properties.
  * **Modified** `Authentication/Session/SessionManager.cs`: Implemented all new contract members. CurrentSession setter now raises SessionChanged event. IsSessionValid checks active session state. UpdateActivity updates LastActivity timestamp and raises event. ClearSession nullifies CurrentSession. Private helper CreateSessionInfo builds read-only SessionInfo.
  * **Modified** `Authentication/Session/ApplicationContext.cs`: Added ISessionManager dependency injection and exposed SessionManager property for runtime access without duplicating state.
* **Explicitly Excluded**:
  * Logout, Remember Me changes, Authorization, Permissions, JWT, Refresh Tokens, Idle timeout logout, Session persistence, Automatic login, Multi-device sessions, Business modules
* **Validation Status**: Build verified successfully with no errors. SessionManager is now the single runtime authority for session state.

### Phase 1.7A: UserSession Improvements
* **Goal**: Harden UserSession model by making identity properties immutable and removing duplicate user state.
* **Included**:
  * **Modified** `Authentication/Models/UserSession.cs`:
    * Made identity properties immutable (init-only): SessionId, LoginTime, AuthenticatedUser, FinancialYear, StartedAt, IsActive
    * Removed duplicate user properties: UserId, Username (now available through AuthenticatedUser)
    * LastActivity and LastActivityAt now use backing fields with init-only public setters and private setters for internal updates
    * Added UpdateLastActivity() method for controlled access to update activity timestamps
    * AuthenticatedUser is now the single source of truth for user identity
  * **Modified** `Authentication/Services/AuthenticationService.cs`:
    * Reordered session creation: AuthenticatedUser created first, then passed to UserSession
    * UserSession now receives AuthenticatedUser instead of duplicating UserId and Username
    * Removed duplicate user data assignment
  * **Modified** `Authentication/Session/SessionManager.cs`:
    * Updated UpdateActivity() to call UserSession.UpdateLastActivity() instead of direct property access
* **Explicitly Unchanged**:
  * Authentication workflow, login UI, startup flow, session creation, validation logic, authentication service, repository, password hasher, ApplicationContext, and FinancialYearContext
* **Validation Status**: Build verified successfully with no errors. Runtime behavior unchanged. UserSession is now the canonical runtime session model without duplicated user state.

### Phase 1.8: Role-Based Authorization
* **Goal**: Introduce runtime role-based authorization based on the authenticated user's role.
* **Included**:
  * **Created** `Authorization/Models/ApplicationRole.cs`: Strongly typed enum defining application roles (None, Administrator, Manager, User, Viewer).
  * **Created** `Authorization/Contracts/IRoleAuthorizationService.cs`: Contract defining role-based authorization methods (HasRole, HasAnyRole, HasAllRoles, IsAdministrator).
  * **Created** `Authorization/Services/RoleAuthorizationService.cs`: Implementation that reads the current authenticated user's role from IApplicationContext (runtime session only). Parses role string to ApplicationRole enum with case-insensitive comparison. Logs warnings for unknown roles and defaults to None. Never accesses database, never injects repositories, never modifies session state. Registered as Singleton.
  * **Created** `Authorization/DependencyInjection/AuthorizationServiceCollectionExtensions.cs`: DI registration extension for authorization services with AddAuthorization() method.
  * **Modified** `DependencyInjection/ServiceCollectionExtensions.cs`: Added using statement for Authorization.DependencyInjection and added services.AddAuthorization() call to RegisterApplication method.
* **Explicitly Excluded**:
  * Permission-based authorization, Permission models, Permission repositories, Claims, Policies, Authorization attributes, UI hiding, Menu filtering, Database role management, Role CRUD, Logout, Business modules
* **Validation Status**: Build verified successfully with no errors. Role authorization relies solely on the existing runtime session without any database access.

### Phase 1.8A: Strongly Typed Runtime Roles
* **Goal**: Eliminate runtime role parsing and use strongly typed ApplicationRole values throughout the runtime session.
* **Included**:
  * **Modified** `Authentication/Models/AuthenticatedUser.cs`: Changed Role property from `string?` to `ApplicationRole` enum. Role is now strongly typed at runtime.
  * **Modified** `Authentication/Services/AuthenticationService.cs`: Added MapToApplicationRole private static method to map database role string to ApplicationRole enum during authentication. Role mapping happens once during authentication, not during authorization checks. Updated AuthenticatedUser creation to use the mapped ApplicationRole value.
  * **Modified** `Authorization/Services/RoleAuthorizationService.cs`: Removed TryParseRole method entirely. Simplified GetCurrentUserRole to directly return currentUser.Role (ApplicationRole enum). Removed warning logs for unknown role parsing. Now performs direct enum comparisons only.
* **Explicitly Unchanged**:
  * Authentication workflow, SessionManager, FinancialYear, Navigation, Permissions, Permission checks, Logout, Repositories, Database schema (database continues storing role names as strings)
* **Validation Status**: Build verified successfully with no errors. Runtime behavior unchanged. Runtime role parsing eliminated. Authorization now uses strongly typed ApplicationRole values only.

### Phase 1.9: Permission-Based Authorization
* **Goal**: Introduce centralized runtime permission-based authorization evaluated entirely in memory using the authenticated user's role from the existing runtime session.
* **Included**:
  * **Created** `Authorization/Models/ApplicationPermission.cs`: Flags enum defining application permissions for Driver, Vehicle, Trip, and Dispatch modules (View, Create, Edit, Delete for each).
  * **Created** `Authorization/Contracts/IPermissionProvider.cs`: Contract defining permission mapping from ApplicationRole to permissions.
  * **Created** `Authorization/Providers/DefaultPermissionProvider.cs`: Hard-coded implementation mapping roles to permissions (Administrator: all permissions, Manager: view/create/edit, User: view/create, Viewer: view only). No database access, no configuration files.
  * **Created** `Authorization/Contracts/IPermissionAuthorizationService.cs`: Contract defining permission-based authorization methods (HasPermission, HasAnyPermission, HasAllPermissions).
  * **Created** `Authorization/Services/PermissionAuthorizationService.cs`: Implementation that reads authenticated user's role from IApplicationContext, obtains permissions through IPermissionProvider, and performs permission evaluation only. Never accesses database, never injects repositories, never modifies session state. Registered as Singleton.
  * **Modified** `Authorization/DependencyInjection/AuthorizationServiceCollectionExtensions.cs`: Added using statement for Authorization.Providers and registered IPermissionProvider and IPermissionAuthorizationService as Singletons.
* **Explicitly Excluded**:
  * Permission CRUD, Permission editor, Database permissions, Claims, Policies, Authorization attributes, UI hiding, Menu filtering, Dynamic permissions, Logout, Business modules
* **Validation Status**: Build verified successfully with no errors. Permission evaluation relies solely on the runtime session and the in-memory permission provider without any database access.

### Phase 1.9A: Scalable Permission Representation
* **Goal**: Replace flags-based permission representation with a scalable strongly typed record struct suitable for a large enterprise ERP.
* **Included**:
  * **Modified** `Authorization/Models/ApplicationPermission.cs`: Changed from [Flags] enum to sealed record struct. Each permission is now a static readonly instance with a unique string ID (e.g., "drivers.view", "vehicles.create"). Private constructor ensures only predefined permission instances can be created. Preserves strongly typed permission names and compile-time safety while providing unlimited scalability for permission growth.
  * **Modified** `Authorization/Providers/DefaultPermissionProvider.cs`: Updated GetAdministratorPermissions() to use explicit array of permission instances instead of Enum.GetValues<ApplicationPermission>(). Continues to return the same permission sets for each role.
* **Explicitly Unchanged**:
  * Authentication, SessionManager, FinancialYear, Role authorization, Repositories, Database, Navigation, UI, Logout, Business modules, PermissionAuthorizationService public behavior, IPermissionProvider public behavior, consumer permission check methods
* **Validation Status**: Build verified successfully with no errors. Runtime behavior unchanged. Permission evaluation remains entirely in memory using the existing runtime session.

### Phase 1.10: Logout
* **Goal**: Implement a centralized logout workflow that clears the runtime session and returns the user to the Login screen while keeping the application running.
* **Included**:
  * **Created** `Authentication/Contracts/ILogoutService.cs`: Contract defining the logout operation with single method `Task LogoutAsync(CancellationToken cancellationToken = default)`.
  * **Created** `Authentication/Services/LogoutService.cs`: Implementation that injects ISessionManager, IApplicationContext, IFinancialYearContext, INavigationService, and ILogger. Performs 6-step logout process: (1) Clears SessionManager, (2) Resets AuthenticationState (IsAuthenticated = false, AuthenticatedAt = null), (3) Clears CurrentUser, (4) Clears FinancialYearContext, (5) Resets runtime application context (placeholder for future), (6) Navigates to LoginViewModel.
  * **Created** `Authentication/DependencyInjection/AuthenticationLogoutServiceCollectionExtensions.cs`: DI extension method `AddAuthenticationLogout()` that registers ILogoutService as singleton.
  * **Modified** `DependencyInjection/ServiceCollectionExtensions.cs`: Added `services.AddAuthenticationLogout()` call after `AddAuthenticationRememberMe()` to register logout services in the DI container.
  * **Modified** `Desktop/Shell/ShellViewModel.cs`: Added ILogoutService injection to constructor, added `LogoutCommand` property (IAsyncRelayCommand), and added `ExecuteLogoutAsync()` method that calls `_logoutService.LogoutAsync()`. Added CommunityToolkit.Mvvm.Input using statement.
* **Remember Me Preservation**: Logout clears only runtime session state and does NOT delete remember-me.json or clear Remember Me settings. The remembered username remains available for future logins.
* **Explicitly Excluded**:
  * Exit Application, Confirmation Dialog, Idle Timeout, Auto Logout, Database Updates, Audit History UI, Permission Cleanup, Role Cleanup, Remember Me Cleanup, Token Revocation, Session Persistence, Business modules
* **Validation Status**: Build verified successfully with no errors. Logout clears runtime session state while preserving Remember Me settings.

### Phase 2.1: Administration Infrastructure (Refactored)
* **Goal**: Establish the technical foundation for the Administration feature without introducing any business functionality, UI, or CRUD operations. Refactored to remove unnecessary abstractions and placeholder logic, aligning with enterprise-grade architecture standards.
* **Included**:
  * **Created** `Administration/DependencyInjection/AdministrationServiceCollectionExtensions.cs`: DI registration extension following project's feature-based architecture pattern with `AddAdministration()` method. Returns IServiceCollection without registering any services, establishing the feature's presence in the DI container for future phases.
  * **Created** Administration domain-oriented folder structure: DependencyInjection, Shared, Users, Roles, Permissions (all empty, prepared for future domain-specific implementations).
  * **Modified** `DependencyInjection/ServiceCollectionExtensions.cs`: Added using statement for Administration.DependencyInjection and added `services.AddAdministration()` call to RegisterApplication method.
* **Removed**:
  * `Administration/Contracts/IAdministrationService.cs`: Unnecessary interface with no real responsibility.
  * `Administration/Services/AdministrationService.cs`: Unnecessary implementation with placeholder logic.
  * `Administration/Contracts/`: Empty folder after interface removal.
  * `Administration/Services/`: Empty folder after service removal.
  * `Administration/Helpers/`: Unnecessary empty folder.
  * `Administration/Models/`: Unnecessary empty folder.
  * `Administration/Navigation/`: Unnecessary empty folder.
  * `Administration/Repositories/`: Unnecessary empty folder.
  * `Administration/Resources/`: Unnecessary empty folder.
  * `Administration/Validators/`: Unnecessary empty folder.
  * `Administration/ViewModels/`: Unnecessary empty folder.
  * `Administration/Views/`: Unnecessary empty folder.
* **Explicitly Excluded**:
  * User List, Add User, Edit User, Delete User, Role Management, Permission Matrix, Financial Year CRUD, database tables, entities, repositories, CRUD logic, ViewModels for business screens, Views, XAML, Validators, Navigation UI, Commands related to business operations, menu items, shell integration, migrations, seed data, authorization rules, business services, Options classes, appsettings.json modifications, DbContext changes, Fluent API changes, placeholder methods, fake implementations, TODO business logic
* **Validation Status**: Build verified successfully with no errors. Administration feature is now a minimal infrastructure foundation with no business functionality, no UI, no CRUD operations, no placeholder logic, and no unnecessary abstractions.

### Phase 2.2: Administration → Users → User Listing
* **Goal**: Implement a read-only User Listing module that allows viewing users without any CRUD operations. This screen serves as the foundation for future phases (Add User, Edit User, User Security).
* **Included**:
  * **Created** `Administration/Users/Contracts/IUserQueryService.cs`: Service contract for querying user data with methods for GetAllUsersAsync and SearchUsersAsync.
  * **Created** `Administration/Users/Models/UserListItem.cs`: Read-only model for displaying users in the grid with properties: Id, Username, DisplayName, RoleName, IsActive, CreatedOn. Excludes password and security data.
  * **Created** `Administration/Users/Services/UserQueryService.cs`: Implementation of IUserQueryService using EF Core with AsNoTracking(), projecting entities into UserListItem, supporting search by username, display name, and role name. Registered as Scoped.
  * **Created** `Administration/Users/ViewModels/UsersViewModel.cs`: ViewModel with ObservableCollection<UserListItem>, SearchText, SelectedUser, IsLoading, RefreshCommand. Loads users on initialization, supports search filtering, no CRUD commands.
  * **Created** `Administration/Users/Views/UsersView.xaml`: Read-only DataGrid view with page title, search TextBox, refresh button. DataGrid displays: Username, Display Name, Role, Active Status, Created Date. Grid is read-only with no action buttons, context menus, or double-click actions.
  * **Created** `Administration/Users/Views/UsersView.xaml.cs`: Code-behind for UsersView with standard UserControl initialization.
  * **Modified** `Administration/DependencyInjection/AdministrationServiceCollectionExtensions.cs`: Added DI registrations for IUserQueryService (Scoped), UsersViewModel (Transient), and UsersView (Transient).
  * **Modified** `Resources/DataTemplates.xaml`: Added DataTemplate mapping for UsersViewModel to UsersView with namespace declarations for Administration.Users.ViewModels and Administration.Users.Views.
* **Explicitly Excluded**:
  * Add User, Edit User, Delete User, Reset Password, Change Password, Lock User, Unlock User, Activate User, Deactivate User, Role Assignment, Permission Assignment, Audit History, Import, Export, Printing, Dialogs, Confirmation Windows, Context Menus, Toolbar Actions, Business Rules, Validation Rules, Authorization Changes, Authentication Changes, Financial Year Changes, Dashboard Changes, Shell Changes, Database Schema Changes, DbContext Changes, Fluent API Changes, Migrations, Seeders, Repository Pattern, CQRS, MediatR
* **Validation Status**: Build verified successfully with no errors. User Listing module is completely read-only with no CRUD functionality, no database schema modifications, and no business rules introduced.

### Phase 2.2 A: User Listing Architectural & Quality Improvements
* **Goal**: Refine the Phase 2.2 implementation by improving architecture, performance, maintainability, and enterprise readiness without altering business logic or application behavior.
* **Included**:
  * **Service Optimization (`UserQueryService.cs`)**: Centralized LINQ projection logic into a single method (`ProjectToListItem()`), removing duplicate `.Select(...)` statements.
  * **ViewModel Enhancements (`UsersViewModel.cs`)**: Extracted the duplicate user collection population logic into a single `UpdateUsers()` method.
  * **UI Refinements (`UsersView.xaml`)**: Removed hardcoded magic numbers (e.g., `Width="250"`, `Width="100"`) from TextBox and DataGrid columns, substituting with fluid alignments or standard dimensions. Removed the hardcoded emoji (`↻`) on the refresh button, replacing it with standard text ("Refresh").
  * **Navigation Integration (`ShellViewModel.cs` & `ShellView.xaml`)**: Added `NavigateToUsersCommand` to the `ShellViewModel` and wired it into the Administration menu in `ShellView.xaml`, ensuring the Users screen can be accessed from the primary shell without redesigning the navigation framework.
* **Explicitly Excluded**:
  * Business rules, structural architectural redesigns, schema updates, backend CRUD operations, new UI functionality, and Phase 2.3 tasks.
* **Validation Status**: UI behavior remains identical. Database query efficiency, codebase readability, and navigation robustness successfully verified.

### Phase 2.3: Administration → Users → Add User
* **Goal**: Implement the complete Add User workflow allowing administrators to create new users. Only user creation is implemented; no editing or user maintenance functionality.
* **Included**:
  * **Created** `Administration/Users/Contracts/IUserCommandService.cs`: Service contract for user command operations with CreateUserAsync method.
  * **Created** `Administration/Users/Models/CreateUserRequest.cs`: Request model for user creation with properties: Username, DisplayName, Password, ConfirmPassword, Role, IsActive.
  * **Created** `Administration/Users/Models/CreateUserResult.cs`: Result model for user creation operations with UserId, IsSuccess, ErrorMessage, and static factory methods for Success/Failure results.
  * **Created** `Administration/Users/Validators/CreateUserValidator.cs`: Static validator for CreateUserRequest validating: Username (required, length 3-50), DisplayName (required), Password (required), ConfirmPassword (required, must match), Role (required). Uses existing ValidationResult infrastructure.
  * **Created** `Administration/Users/Services/UserCommandService.cs`: Implementation of IUserCommandService that validates duplicate username, reuses Phase 1 IPasswordHasher (PBKDF2) for password hashing, creates User entity, persists via DbContext, and logs unexpected failures only (no sensitive data logged). Registered as Scoped.
  * **Created** `Administration/Users/ViewModels/AddUserViewModel.cs`: ViewModel for Add User screen maintaining form state (Username, DisplayName, Password, ConfirmPassword, SelectedRole, IsActive, IsBusy), calls validation before service, executes Save and Cancel commands, navigates back after successful creation using NavigationService, loads roles from existing ApplicationRole enum. No database access, no DbContext, no password hashing, no business rules. Registered as Transient.
  * **Created** `Administration/Users/Views/AddUserView.xaml`: MVVM-only view with no code-behind logic. Fields: Username, Display Name, Password, Confirm Password (PasswordBox with attached property for MVVM binding), Role (ComboBox), Active (CheckBox). Buttons: Save, Cancel. Uses existing Design System resources, no inline styling, no hardcoded colors/fonts.
  * **Created** `Administration/Users/Views/AddUserView.xaml.cs`: Code-behind contains only InitializeComponent(), no business logic.
  * **Created** `Desktop/Converters/StringToVisibilityConverter.cs`: Value converter for string-to-visibility binding (empty/null → Collapsed, non-empty → Visible).
  * **Created** `Desktop/AttachedProperties/PasswordBoxHelper.cs`: Attached property helper for enabling MVVM binding on PasswordBox controls.
  * **Modified** `Administration/DependencyInjection/AdministrationServiceCollectionExtensions.cs`: Added DI registrations for IUserCommandService (Scoped), UserCommandService (Scoped), and AddUserViewModel (Transient).
  * **Modified** `Administration/Users/ViewModels/UsersViewModel.cs`: Added INavigationService dependency and AddUserCommand to navigate to AddUserViewModel.
  * **Modified** `Administration/Users/Views/UsersView.xaml`: Added "Add User" button in header bound to AddUserCommand.
  * **Modified** `Styles/TextBoxStyles.xaml`: Added BaseTextBoxStyle key to enable style inheritance for password fields.
* **Explicitly Excluded**:
  * Edit User, Delete User, Reset Password, Lock User, Unlock User, Activate User, Deactivate User, Permission Assignment, Role Management, Audit History, Import, Export, Printing, Bulk Insert, Bulk Update, Email Notifications, Password Expiry, Two-Factor Authentication, Profile Images, Business Rules outside user creation, Authentication changes, Authorization changes, Financial Year changes, Dashboard changes, Database schema changes, DbContext modifications, Fluent API modifications, Migrations.
* **Validation Status**: Build verified successfully with no errors. User creation is fully implemented with complete workflow from UI to database, validation, duplicate checking, password hashing using Phase 1 infrastructure, and persistence. No CRUD other than Create has been implemented. No database schema changes were made. No authentication or authorization functionality was modified. No Phase 2.4 functionality has been introduced.

### Phase 2.3 A: Add User Architectural & Quality Improvements
* **Goal**: Refine the Phase 2.3 implementation by improving architecture, security, maintainability, and enterprise readiness while preserving 100% of existing behavior.
* **Included**:
  * **Validator DI Conversion (`ICreateUserValidator.cs` & `CreateUserValidator.cs`)**: Converted CreateUserValidator from static class to DI-managed service implementing ICreateUserValidator interface. Enables testability, extensibility for future dependencies (localization, password policy, configuration), and aligns with enterprise DI patterns. Validator remains focused only on validation with no database access or business logic.
  * **Service Layer Validation Integration (`UserCommandService.cs`)**: Updated UserCommandService to inject ICreateUserValidator instead of calling static validator. Implements dependency inversion, separates validation logic from service logic, and improves maintainability. Removed redundant comments and simplified code structure.
  * **ViewModel Simplification (`AddUserViewModel.cs`)**: Removed validation call from AddUserViewModel since validation is now handled in UserCommandService. Removed unused using statements (System.Windows, veteran_logistic.Administration.Users.Validators) and redundant comments. ViewModel now only maintains form state and handles navigation, with no business logic.
  * **UI Hardcoded Value Removal (`AddUserView.xaml`)**: Removed MaxWidth="600" from StackPanel and Width="100" from both Save and Cancel buttons. Enables responsive design, follows existing pattern of avoiding magic numbers, improves maintainability, and provides better adaptation to different screen resolutions.
  * **DI Registration Cleanup (`AdministrationServiceCollectionExtensions.cs`)**: Added ICreateUserValidator registration (Scoped) and removed redundant inline comments. Ensures complete service registration and maintains clean registration style.
* **Explicitly Excluded**:
  * Business rules, structural architectural redesigns, schema updates, backend CRUD operations, new UI functionality, Phase 2.4 tasks, authentication changes, authorization redesign, navigation redesign, shell redesign.
* **Validation Status**: User experience remains identical. Validator is now DI-managed and ready for future enhancements. Code is production-ready with improved testability, maintainability, and enterprise readiness. No database schema changes, no authentication/authorization modifications, no new CRUD functionality.

### Phase 2.3 B: Add User Navigation Integration Fix
* **Goal**: Fix XAML parsing error preventing AddUserView from rendering when navigating from Users screen.
* **Included**:
  * **Modified** `Styles/TextBlockStyles.xaml`: Added missing `BodyTextBlockStyle` definition with normal font size and weight settings. The style was referenced in AddUserView.xaml but not defined in the resource dictionaries, causing a XAML parsing exception when the view was loaded.
* **Root Cause**: The AddUserView.xaml referenced `BodyTextBlockStyle` which did not exist in the application's resource dictionaries. When the NavigationService navigated to AddUserViewModel, WPF attempted to render AddUserView via the DataTemplate mapping, but the missing resource caused a XAML parsing exception.
* **Navigation Verification**: Confirmed that the navigation infrastructure was already correctly configured:
  * DataTemplates.xaml already contained the correct ViewModel-to-View mapping for AddUserViewModel
  * AdministrationServiceCollectionExtensions.cs already registered AddUserViewModel as Transient
  * UsersViewModel.cs already had the correct navigation command calling NavigateAsync<AddUserViewModel>()
* **Explicitly Excluded**:
  * Navigation architecture redesign, Shell redesign, MainWindow redesign, DI redesign, ViewLocator redesign, DataTemplate mechanism changes, Edit User implementation, CRUD functionality, dialogs, windows, Phase 2.4 functionality.
* **Validation Status**: Build verified successfully with no errors. AddUserView now renders correctly when clicking "Add User" instead of displaying the ViewModel type name. No navigation architecture was redesigned. No Phase 2.4 functionality has been introduced.

### Phase 2.3 C: Threading Fix for Navigation DataContext Updates
* **Goal**: Fix InvalidOperationException when navigating from Add User back to Users screen due to cross-thread DataContext assignment.
* **Included**:
  * **Modified** `MainWindow.xaml.cs`: Wrapped DataContext assignment in `OnCurrentViewModelChanged` with `Dispatcher.Invoke()` to ensure UI thread access. The navigation event was being raised on a background thread (due to ConfigureAwait(false) in async operations), causing WPF to throw "The calling thread cannot access this object because a different thread owns it" when updating the DataContext.
* **Root Cause**: When AddUserViewModel.SaveAsync() completes successfully, it calls `_navigationService.GoBackAsync()`. The NavigationService raises the CurrentViewModelChanged event on the background thread where the async operation completed. MainWindow.OnCurrentViewModelChanged() attempts to set DataContext, which is a UI-owned property, causing a cross-thread exception and application crash.
* **Explicitly Excluded**:
  * Navigation architecture redesign, async/await pattern changes, ConfigureAwait usage changes, Shell redesign, Phase 2.4 functionality.
* **Validation Status**: Build verified successfully with no errors. Navigation now completes without crashing, and the Users list correctly displays after adding a user. No navigation architecture was redesigned.

### Phase 2.4: Administration → Users → Edit User
* **Goal**: Implement complete Edit User workflow allowing administrators to edit existing user information (DisplayName, Role, IsActive) without modifying username or password.
* **Included**:
  * **Created** `Administration/Users/Contracts/IUpdateUserValidator.cs`: Interface for validating user update requests using the existing ValidationResult infrastructure from VeteranLogistics.Shared.Validation.
  * **Created** `Administration/Users/Models/UpdateUserRequest.cs`: Request model containing UserId (for identification), DisplayName, Role, and IsActive. Excludes Username (immutable), Password, and ConfirmPassword.
  * **Created** `Administration/Users/Models/UpdateUserResult.cs`: Result model with IsSuccess and ErrorMessage properties, providing static factory methods for Success() and Failure(errorMessage).
  * **Created** `Administration/Users/Validators/UpdateUserValidator.cs`: Validator implementation that validates DisplayName (required) and Role (required). Uses the existing ValidationResult/ValidationError pattern. No database access, no business logic.
  * **Created** `Administration/Users/ViewModels/EditUserViewModel.cs`: ViewModel for Edit User screen with properties for UserId, Username (read-only), DisplayName, SelectedRole, IsActive, ValidationError, IsBusy. Includes LoadUser(UserListItem) method to populate form from selected user, SaveCommand to validate and persist changes, CancelCommand to navigate back. No DbContext access, no password handling, no business logic.
  * **Created** `Administration/Users/Views/EditUserView.xaml`: MVVM view matching AddUserView layout for UI consistency. Contains read-only Username field (with DisabledBrush background), editable DisplayName, Role ComboBox, Active CheckBox, Save/Cancel buttons, validation error display, and loading indicator. Uses existing Design System resources, no inline styling, no hardcoded values.
  * **Created** `Administration/Users/Views/EditUserView.xaml.cs`: Standard UserControl code-behind with only InitializeComponent().
  * **Modified** `Administration/Users/Contracts/IUserCommandService.cs`: Added UpdateUserAsync method signature: Task<UpdateUserResult> UpdateUserAsync(UpdateUserRequest request, CancellationToken cancellationToken = default).
  * **Modified** `Administration/Users/Services/UserCommandService.cs`: Added IUpdateUserValidator injection (renamed existing validator to ICreateUserValidator for clarity). Implemented UpdateUserAsync method that validates request, locates user by UserId, updates only DisplayName, Role, and IsActive, persists changes, and returns appropriate result. Does NOT modify Username, Password, PasswordHash, SecurityStamp, CreatedDate, or CreatedBy.
  * **Modified** `Administration/Users/ViewModels/UsersViewModel.cs`: Added EditUserCommand that navigates to EditUserViewModel and calls LoadUser on the created ViewModel with the selected user data. Uses NavigationService.CurrentViewModel to access the created ViewModel after navigation.
  * **Modified** `Administration/Users/Views/UsersView.xaml`: Added "Edit User" button in the header toolbar next to "Add User" button, bound to EditUserCommand.
  * **Modified** `Administration/DependencyInjection/AdministrationServiceCollectionExtensions.cs`: Registered IUpdateUserValidator as Scoped and EditUserViewModel as Transient.
  * **Modified** `Resources/DataTemplates.xaml`: Added DataTemplate mapping EditUserViewModel → EditUserView using the same pattern as other ViewModels.
* **Explicitly Excluded**:
  * Username modification, password management, password reset, password change, security settings, audit history, bulk operations, import/export, printing, email notifications, two-factor authentication, profile images, authentication changes, authorization redesign, Phase 2.5 functionality, database schema changes, new entities, Fluent API modifications, migrations.
* **Validation Status**: Build verified successfully with no errors. Edit User workflow is fully functional: select user from Users list, click Edit User, modify DisplayName/Role/IsActive, save changes, and return to Users screen. Username remains immutable. Passwords are untouched. Authentication data is unchanged. Only editable profile information is updated. No database schema changes were made. No Phase 2.5 functionality has been introduced.

### Phase 2.4 A: Architecture Review - ViewModel Decoupling and Query Layer Integration
* **Goal**: Improve Edit User implementation to follow enterprise-grade architecture by removing ViewModel coupling, implementing query layer integration, and ensuring proper CancellationToken propagation.
* **Included**:
  * **Created** `Administration/Users/Models/EditUserModel.cs`: Query model for edit user operations containing only UserId, Username, DisplayName, Role, and IsActive. Excludes password fields, security fields, and audit fields.
  * **Modified** `Administration/Users/Contracts/IUserQueryService.cs`: Added GetUserForEditAsync method signature for loading a single user for editing.
  * **Modified** `Administration/Users/Services/UserQueryService.cs`: Implemented GetUserForEditAsync with EF Core projection to EditUserModel using AsNoTracking for optimal performance.
  * **Modified** `Administration/Users/ViewModels/EditUserViewModel.cs`: Removed LoadUser(UserListItem) method, added IUserQueryService injection, implemented InitializeAsync to extract UserId from NavigationParameter and load data via query service, removed public UserId property (now private field), fixed SaveAsync to use private _userId field and pass CancellationToken.None.
  * **Modified** `Administration/Users/ViewModels/UsersViewModel.cs`: Modified EditUserAsync to pass UserId via NavigationParameter instead of accessing NavigationService.CurrentViewModel and calling LoadUser. Eliminated tight coupling between ViewModels.
  * **Modified** `MVVM/ViewModelBase.cs`: Added NavigationParameter property (public) to support parameter passing during navigation. Provides standard mechanism for passing navigation parameters without relying on CurrentViewModel.
  * **Modified** `Desktop/Navigation/NavigationService.cs`: Modified NavigateAsync to set NavigationParameter on ViewModelBase before calling InitializeAsync. Ensures parameters are available during initialization lifecycle.
* **Architecture Improvements**:
  * **ViewModel Decoupling**: EditUserViewModel no longer depends on NavigationService.CurrentViewModel or direct casting. UsersViewModel no longer depends on EditUserViewModel's internal structure (LoadUser method).
  * **Query Layer Integration**: EditUserViewModel now loads fresh data from the database via IUserQueryService instead of receiving potentially stale UserListItem from parent ViewModel.
  * **Data Minimization**: EditUserModel contains only the fields needed for editing (5 columns) instead of full entity or UserListItem with unnecessary fields.
  * **Type Safety**: NavigationParameter provides type-safe parameter passing via TryGetValue<T>.
  * **Lifecycle Consistency**: Parameters are available during InitializeAsync, following the established ViewModel lifecycle pattern.
  * **CancellationToken Propagation**: All async EF Core operations (GetUserForEditAsync, UpdateUserAsync, CreateUserAsync) receive CancellationToken parameters. InitializeAsync receives CancellationToken and passes it to LoadUserAsync.
* **Explicitly Excluded**:
  * Navigation architecture redesign, authentication changes, authorization redesign, Phase 2.5 functionality, database schema changes, new CRUD operations (Delete, Reset Password, etc.).
* **Validation Status**: Build verified successfully with no errors. User experience remains identical: select user, click Edit User, modify data, save, return. EditUserViewModel no longer depends on NavigationService.CurrentViewModel. UserListItem is no longer used as edit data source. Edit screen loads latest data from query layer. DbContext is never accessed from ViewModel. CancellationToken propagates correctly. No database schema changes. Code is production-ready.

### Phase 2.4 B: Navigation Parameter Lifecycle Refinement
* **Goal**: Improve navigation parameter mechanism to pass parameters through the navigation lifecycle instead of storing them as permanent state on ViewModelBase, while maintaining 100% backward compatibility.
* **Included**:
  * **Created** `Desktop/Navigation/INavigationAware.cs`: Interface for ViewModels that need to receive navigation parameters. Provides OnNavigatedTo(NavigationParameter? parameter) method for receiving parameters during navigation lifecycle.
  * **Modified** `Desktop/Navigation/NavigationService.cs`: Updated navigation flow to call INavigationAware.OnNavigatedTo() before InitializeAsync(). Removed unused using statement. Parameters are now passed through the lifecycle instead of being stored on ViewModelBase.
  * **Modified** `MVVM/ViewModelBase.cs`: Removed NavigationParameter property and backing field. Removed using veteran_logistic.Navigation. ViewModelBase is now lighter and doesn't store navigation state.
  * **Modified** `Administration/Users/ViewModels/EditUserViewModel.cs`: Implemented INavigationAware interface. Added OnNavigatedTo method to receive UserId parameter. Moved parameter extraction from InitializeAsync to OnNavigatedTo. InitializeAsync now focuses on loading data using the already-set _userId field.
* **Architecture Improvements**:
  * **Separation of Concerns**: Navigation parameters are now passed through the lifecycle method rather than stored as mutable state. ViewModels that don't need parameters remain clean and unpolluted.
  * **Reduced State Pollution**: Only ViewModels that actually need navigation parameters implement INavigationAware. Eliminates unnecessary state pollution on ViewModels that don't need parameters.
  * **Better Lifecycle Management**: Parameters are received at the point of navigation (OnNavigatedTo). Initialization (InitializeAsync) is separate and focuses on setup logic.
  * **Extensibility**: The INavigationAware pattern can be extended for additional navigation lifecycle events. Flexible without being complex.
  * **Testability**: ViewModels can be tested by directly calling OnNavigatedTo with test parameters. Clear, explicit input point for navigation parameters.
* **Backward Compatibility**:
  * Existing NavigateAsync<T>() still works - calls parameterized overload with null.
  * New overload supports parameter passing - NavigateAsync<TViewModel>(NavigationParameter? parameter) continues to function.
  * Existing ViewModels require no modification - ViewModels that don't implement INavigationAware simply don't receive OnNavigatedTo calls.
  * Login, Financial Year, Shell, Users List, Add User work unchanged - these ViewModels don't implement INavigationAware.
  * EditUserViewModel still receives UserId - now implements INavigationAware and receives parameter through OnNavigatedTo.
  * EditUserViewModel still loads data through IUserQueryService - no changes to data loading logic.
* **Explicitly Excluded**:
  * Navigation architecture redesign, authentication changes, authorization redesign, Phase 2.5 functionality, database schema changes, new CRUD operations, UI changes, Shell redesign.
* **Validation Status**: Build verified successfully with no errors. No UI changes. No breaking changes. Fully backward compatible.

### Phase 2.5: Administration → Users → Activate/Deactivate User
* **Goal**: Implement the ability for an administrator to activate or deactivate an existing user. This is a business operation, not a delete operation. Inactive users remain in the database.
* **Included**:
  * **Created** `Administration/Users/Models/UpdateUserStatusRequest.cs`: Request model for user status updates containing UserId and IsActive properties.
  * **Created** `Administration/Users/Models/UpdateUserStatusResult.cs`: Result model for user status update operations with IsSuccess and ErrorMessage properties, plus static factory methods for Success() and Failure(errorMessage).
  * **Created** `Administration/Users/Contracts/IUpdateUserStatusValidator.cs`: Interface for validating user status update requests. Validates that user exists and requested state differs from current state.
  * **Created** `Administration/Users/Validators/UpdateUserStatusValidator.cs`: Implementation that validates request and current state to prevent redundant operations. Returns appropriate error messages for already-active or already-inactive scenarios.
  * **Modified** `Administration/Users/Contracts/IUserCommandService.cs`: Added UpdateUserStatusAsync method signature taking UpdateUserStatusRequest and CancellationToken, returning UpdateUserStatusResult.
  * **Modified** `Administration/Users/Services/UserCommandService.cs`: Added IUpdateUserStatusValidator dependency injection. Implemented UpdateUserStatusAsync method that validates user existence and state before update. Updates only the IsActive field. No other fields are modified. Logs only unexpected failures.
  * **Modified** `Administration/Users/ViewModels/UsersViewModel.cs`: Added IUserCommandService dependency injection. Added ValidationError property for displaying validation errors. Added ActivateUserCommand and DeactivateUserCommand with CanExecute guards. Added CanExecuteUserCommand method to enable/disable buttons based on selection. Commands reload the grid after success and clear selection. Removed unused using statement.
  * **Modified** `Administration/Users/Views/UsersView.xaml`: Added new Grid row for validation error display. Added Activate and Deactivate buttons in toolbar. Added validation error border using StringToVisibilityConverter. Buttons use existing Design System resources.
  * **Modified** `Administration/DependencyInjection/AdministrationServiceCollectionExtensions.cs`: Added IUpdateUserStatusValidator registration as Scoped.
* **Business Rules**:
  * **User Existence**: User must exist in the database.
  * **State Change**: Requested state must differ from current state (cannot activate an already-active user, cannot deactivate an already-inactive user).
  * **Field Restrictions**: Only IsActive is updated. Username, Password, Role, CreatedDate, CreatedBy, and SecurityStamp never change.
* **Architecture Improvements**:
  * **Separation of Concerns**: Validation logic separated into dedicated validator. Service layer handles business logic and persistence. ViewModel handles UI state and user interaction.
  * **Reusability**: Reuses existing User entity (no schema changes). Reuses existing DbContext. Reuses existing ValidationResult infrastructure. Reuses existing Design System resources.
  * **Consistency**: Follows patterns established in Phase 2.3 (Create User) and Phase 2.4 (Edit User). Same validation approach, result pattern, command pattern, and error display mechanism.
  * **Extensibility**: IUpdateUserStatusValidator can be extended for additional validation rules. Command pattern allows easy addition of bulk operations in future.
* **Performance**:
  * **Database Operations**: Two queries per status update (one for validation with AsNoTracking, one for update). Single SaveChangesAsync call. No N+1 query issues.
  * **UI Performance**: Grid reload only after successful operation. Debounced search unchanged. CanExecute notifications on selection change only.
  * **Threading Fix**: Removed ConfigureAwait(false) from UI async calls; now UI updates resume on Dispatcher thread, preventing NotSupportedException crashes on Activate/Deactivate.
  * **Memory**: No memory leaks (CancellationTokenSource properly disposed). No unnecessary object retention.
* **Explicitly Excluded**:
  * Delete User, Reset Password, Lock User, Unlock User, Password Expiry, Audit History, Soft Delete, Bulk Activate, Bulk Deactivate, Role Management, Permission Management, Authentication changes, Authorization changes, Remember Me changes, database schema changes, migrations.
* **Validation Status**: Build verified successfully with no errors. Only IsActive is modified (confirmed in service implementation). Username, Password, Role, CreatedDate, CreatedBy, and SecurityStamp never change. No authentication logic changed. No authorization logic changed. No database schema changes. No migrations required. Code is production-ready.

### Phase 2.5 A: Threading Fix for Activate/Deactivate Commands
* **Goal**: Fix NotSupportedException when activating or deactivating users due to ConfigureAwait(false) causing UI updates on wrong thread.
* **Included**:
  * **Modified** `Administration/Users/ViewModels/UsersViewModel.cs`: Removed ConfigureAwait(false) from async calls in ActivateUserAsync and DeactivateUserCommand methods. UI updates (LoadUsersAsync, SelectedUser assignment, command notifications) now resume on the Dispatcher thread, preventing "The calling thread cannot access this object because a different thread owns it" exceptions.
* **Root Cause**: ConfigureAwait(false) in async operations caused continuation to run on a thread pool thread instead of the UI thread. When the operation completed and attempted to update UI-bound properties (Users collection, SelectedUser, command CanExecute state), WPF threw NotSupportedException because these properties are owned by the UI thread.
* **Explicitly Excluded**:
  * Navigation architecture redesign, async/await pattern changes, Phase 2.6 functionality, business logic changes, UI layout changes.
* **Validation Status**: Build verified successfully with no errors. Activate and Deactivate commands now complete without crashing. Grid correctly refreshes after status changes. No navigation architecture was redesigned.

### Phase 2.6: Administration → Users → Reset Password
* **Goal**: Implement the ability for an administrator to reset another user's password. This phase allows password reset without requiring the user to know their current password.
* **Included**:
  * **Created** `Administration/Users/Contracts/IResetPasswordValidator.cs`: Interface for validating reset password requests using the existing ValidationResult infrastructure.
  * **Created** `Administration/Users/Models/ResetPasswordRequest.cs`: Request model containing only UserId, NewPassword, and ConfirmPassword. Excludes username, display name, role, and other user properties.
  * **Created** `Administration/Users/Models/ResetPasswordResult.cs`: Result model with IsSuccess and ErrorMessage properties, following the existing Success()/Failure() factory pattern.
  * **Created** `Administration/Users/Validators/ResetPasswordValidator.cs`: Validator implementation that validates: User exists, Password required, Confirm Password required, Passwords match. Does NOT validate duplicate passwords, previous passwords, password history, or complexity rules. Reuses existing ValidationResult infrastructure.
  * **Created** `Administration/Users/ViewModels/ResetPasswordViewModel.cs`: ViewModel for Reset Password screen with properties for NewPassword, ConfirmPassword, ValidationError, and IsBusy. Implements INavigationAware to receive UserId parameter. Has SaveCommand and CancelCommand. No DbContext access, no password hashing, no business logic. Registered as Transient.
  * **Created** `Administration/Users/Views/ResetPasswordView.xaml`: MVVM view with New Password and Confirm Password fields (PasswordBox controls), Save and Cancel buttons, validation error display, and loading indicator. Uses existing Design System resources, no inline styling.
  * **Created** `Administration/Users/Views/ResetPasswordView.xaml.cs`: Code-behind handles PasswordBox.PasswordChanged events to bind to ViewModel properties (WPF PasswordBox does not support data binding natively).
  * **Modified** `Administration/Users/Contracts/IUserCommandService.cs`: Added ResetPasswordAsync method signature: Task<ResetPasswordResult> ResetPasswordAsync(ResetPasswordRequest request, CancellationToken cancellationToken = default).
  * **Modified** `Administration/Users/Services/UserCommandService.cs`: Added IResetPasswordValidator dependency injection. Implemented ResetPasswordAsync method that: (1) Loads user by UserId, (2) Returns failure if user not found, (3) Validates request using validator, (4) Hashes password using existing IPasswordHasher from Phase 1, (5) Replaces PasswordHash and PasswordSalt, (6) SaveChangesAsync. Does NOT modify Username, DisplayName, Role, IsActive, CreatedDate, CreatedBy, or SecurityStamp. Logs only unexpected failures (no sensitive data logged).
  * **Modified** `Administration/Users/ViewModels/UsersViewModel.cs`: Added ResetPasswordAsync command that navigates to ResetPasswordViewModel with UserId parameter. Added ResetPasswordCommand notification when SelectedUser changes.
  * **Modified** `Administration/Users/Views/UsersView.xaml`: Added "Reset Password" button in the header toolbar beside "Edit User" button, bound to ResetPasswordCommand.
  * **Modified** `Administration/DependencyInjection/AdministrationServiceCollectionExtensions.cs`: Added IResetPasswordValidator registration as Scoped and ResetPasswordViewModel registration as Transient.
  * **Modified** `Resources/DataTemplates.xaml`: Added DataTemplate mapping ResetPasswordViewModel → ResetPasswordView.
* **Business Rules**:
  * **User Existence**: User must exist in the database.
  * **Password Requirements**: New password required, Confirm password required, Passwords must match.
  * **Field Restrictions**: Only PasswordHash and PasswordSalt are updated. Username, DisplayName, Role, IsActive, CreatedDate, CreatedBy, and SecurityStamp never change.
  * **Security**: Passwords never displayed, never logged. Hash values never logged. Confirm password never logged. Only unexpected exceptions are logged.
* **Architecture Improvements**:
  * **Reusability**: Reuses existing IPasswordHasher from Phase 1 (PBKDF2). Reuses existing User entity (no schema changes). Reuses existing DbContext. Reuses existing ValidationResult infrastructure. Reuses existing Design System resources.
  * **Consistency**: Follows patterns established in Phase 2.3 (Create User), Phase 2.4 (Edit User), and Phase 2.5 (Activate/Deactivate). Same validation approach, result pattern, command pattern, and navigation pattern.
  * **Security**: PasswordBox controls for secure input. No password storage in ViewModel. No password logging. Service layer handles hashing.
* **Explicitly Excluded**:
  * Password History, Temporary Passwords, Email, SMS, OTP, Password Expiry, Force Password Change, Unlock User, Login Changes, Session Logout, Audit History, Bulk Reset, Role Changes, Permissions, Authentication module changes, Authorization module changes, database schema changes, migrations.
* **Validation Status**: Build verified successfully with no errors. Only PasswordHash and PasswordSalt are updated (confirmed in service implementation). Username, DisplayName, Role, IsActive, CreatedDate, CreatedBy, and SecurityStamp never change. Authentication module is untouched. Authorization module is untouched. Existing IPasswordHasher is reused. No duplicate security logic exists. No database schema changes. No migrations required. Code is production-ready.

### Phase 2.6A: Reset Password - Architectural Review and Refinement
* **Goal**: Refine the Phase 2.6 implementation by fixing UI connectivity, improving PasswordBox binding reusability, and achieving pure MVVM compliance without changing application behavior.
* **Included**:
  * **Modified** `Resources/DataTemplates.xaml`: Added DataTemplate mapping for ResetPasswordViewModel → ResetPasswordView. The mapping was missing, causing the Reset Password screen to be unreachable via navigation.
  * **Modified** `Administration/Users/Views/ResetPasswordView.xaml`: Replaced code-behind PasswordBox binding with reusable PasswordBoxHelper attached property from Phase 2.3. Added namespace declaration for AttachedProperties. Changed PasswordBox controls to use `ap:PasswordBoxHelper.Attach="True"` and `ap:PasswordBoxHelper.Password="{Binding NewPassword, Mode=TwoWay}"` pattern.
  * **Modified** `Administration/Users/Views/ResetPasswordView.xaml.cs`: Removed all code-behind logic including Loaded event handler and PasswordBox.PasswordChanged event subscriptions. Code-behind now contains only InitializeComponent(), achieving pure MVVM compliance.
* **UI Connectivity Fix**:
  * **Issue**: ResetPasswordView was not reachable because the DataTemplate mapping was missing from DataTemplates.xaml. Navigation would fail to render the view.
  * **Fix**: Added the DataTemplate mapping to connect ResetPasswordViewModel to ResetPasswordView, enabling proper navigation and view rendering.
* **PasswordBox Reusability Improvement**:
  * **Issue**: ResetPasswordView used code-behind event handlers to bind PasswordBox controls to ViewModel properties. This duplicated the pattern from AddUserView and was not reusable for future password screens (e.g., Change Password).
  * **Fix**: Replaced code-behind binding with the existing enterprise PasswordBoxHelper attached property from Phase 2.3. This eliminates code-behind logic, reuses the enterprise solution, provides consistency across all password screens (Login, Add User, Reset Password), and is ready for future Change Password screen.
* **Architecture Improvements**:
  * **MVVM Compliance**: ViewModel contains no UI logic, View contains no business logic, code-behind eliminated (pure MVVM).
  * **Reusability**: PasswordBoxHelper is now used consistently across Login, Add User, and Reset Password screens, providing a single reusable solution for PasswordBox binding.
  * **Validator Purity**: Validator validates only request data. Database state (userExists) is passed as parameter from service layer. No database access in validator.
  * **PasswordHasher Reuse**: Existing IPasswordHasher from Phase 1 reused. No duplicate password hashing logic. Single source of truth maintained.
  * **Navigation**: ViewModel-first navigation maintained, INavigationAware pattern used correctly, DataTemplate mapping now complete.
* **Security Review**:
  * **No Password Logging**: Password never logged, confirm password never logged, hash never logged, salt never logged. Only unexpected exceptions logged (without sensitive data).
  * **Secure Input**: PasswordBox controls used, passwords never displayed in UI, no password storage in ViewModel after operation.
  * **User Entity Integrity**: Username, DisplayName, Role, IsActive, CreatedDate, CreatedBy, and SecurityStamp unchanged. Only PasswordHash and PasswordSalt updated.
  * **Module Integrity**: Authentication module untouched, Authorization module untouched, no authentication/authorization logic changed.
* **Performance Review**:
  * **Database Operations**: Single query to load user, single SaveChangesAsync call, no N+1 query issues, CancellationToken properly propagated.
  * **UI Performance**: Busy state during operation, no unnecessary grid refresh, navigation back only after success, no blocking operations.
  * **Memory**: No memory leaks, proper disposal patterns, no unnecessary object retention.
* **Explicitly Excluded**:
  * Password History, Temporary Passwords, Email, SMS, OTP, Password Expiry, Force Password Change, Unlock User, Login Changes, Session Logout, Audit History, Bulk Reset, Role Changes, Permissions, Authentication module changes, Authorization module changes, database schema changes, migrations, Repositories, Mediator, Phase 2.7 functionality.
* **Validation Status**: Build verified successfully with no errors. Application behavior remains unchanged. UI connectivity fixed (screen now renders). PasswordBox binding improved (reusable enterprise solution). Code-behind eliminated (pure MVVM). No Phase 2.7 functionality introduced. Code is production-ready.

### Phase 2.6B: Reset Password - Final Enterprise Review and Refinement
* **Goal**: Perform final enterprise-quality review of Phase 2.6 (Reset Password) and implement navigation lifecycle consistency with automatic Users screen refresh after successful operations.
* **Included**:
  * **Modified** `Administration/Users/ViewModels/UsersViewModel.cs`: Added `OnNavigatedToAsync` override to refresh user list when navigating back to the screen. Added `RefreshUsersAsync` public method for child ViewModels to trigger refresh after successful operations.
  * **Modified** `Administration/Users/ViewModels/ResetPasswordViewModel.cs`: Modified `SaveAsync` to call `RefreshUsersAsync` on the parent UsersViewModel after successful password reset.
* **Navigation Lifecycle Consistency**:
  * **Issue**: When a child ViewModel (AddUser, EditUser, ResetPassword) navigates back to the Users screen, the UsersViewModel needs to refresh its data to reflect changes. The existing `InitializeAsync` only runs once (guarded by `IsInitialized`), so subsequent navigations back to the screen would not trigger a refresh.
  * **Fix**: Added `OnNavigatedToAsync` override to ensure the user list is refreshed every time the screen is navigated to, regardless of whether it's the first visit or a return from a child screen.
* **Users Screen Refresh After Successful Operations**:
  * **Issue**: Child ViewModels need a way to trigger a refresh of the parent UsersViewModel after successful operations without navigating.
  * **Fix**: Added `RefreshUsersAsync` public method to provide a clean, explicit API for child ViewModels to refresh the parent's data. ResetPasswordViewModel now calls this method after successful password reset.
  * **Implementation**: After `GoBackAsync` completes, ResetPasswordViewModel accesses `INavigationService.CurrentViewModel`, casts to UsersViewModel, and calls `RefreshUsersAsync()`.
* **Search/Filter State Preservation**:
  * **Behavior**: Search text and filter state are preserved during refresh. `RefreshUsersAsync()` calls `LoadUsersAsync()` which uses the current value of `SearchText` property. No explicit clearing of search text occurs.
  * **Consistency**: Same pattern used by Activate/Deactivate operations, manual Refresh button, and consistent with existing Add User and Edit User behavior.
* **Architecture Improvements**:
  * **Navigation Lifecycle**: Added OnNavigatedToAsync to UsersViewModel for consistent refresh behavior. Ensures user list refreshes on every navigation to the screen. Aligns with enterprise navigation patterns.
  * **Refresh API**: Added RefreshUsersAsync public method for child ViewModel integration. Provides explicit, type-safe refresh mechanism. Preserves search text and filter state.
  * **Child-Parent Communication**: ResetPasswordViewModel refreshes parent after successful operation. Uses NavigationService.CurrentViewModel to access parent. Consistent with Add User and Edit User patterns.
* **Navigation Review**:
  * **Flow**: Users screen → Click Reset Password button → Navigate to ResetPasswordViewModel → ResetPasswordView renders → Click Save → ResetPasswordAsync executes → GoBackAsync → Users screen refreshes.
  * **Resolution**: ResetPasswordViewModel registered as Transient in DI, NavigationService resolves via IViewModelFactory, DataTemplate mapping exists.
  * **Parameter Passing**: UserId passed via NavigationParameter, INavigationAware.OnNavigatedTo receives parameter, parameter extraction is type-safe.
  * **Lifecycle**: OnNavigatedTo called before InitializeAsync, InitializeAsync guarded by IsInitialized, OnNavigatedToAsync called on every navigation to Users screen, refresh triggered appropriately.
* **UI Connectivity Confirmation**:
  * **All Verified**: Reset Password button exists on Users screen, button is bound to ResetPasswordCommand, command navigates through INavigationService, ResetPasswordViewModel resolves correctly, DataTemplate exists, ResetPasswordView is displayed, PasswordBoxHelper binding works, Save command executes, password updates in database, navigation returns correctly after success.
* **Performance Review**:
  * **Database Operations**: Single query to load user, single SaveChangesAsync call, no N+1 query issues, CancellationToken properly propagated.
  * **Refresh Efficiency**: LoadUsersAsync reuses existing query service, search text preserved (no unnecessary full reload), debounced search unchanged, no redundant queries.
  * **UI Performance**: Busy state during operation, no unnecessary grid refresh during operation, navigation back only after success, refresh happens after navigation completes, no blocking operations.
  * **Memory**: No memory leaks, proper disposal patterns, no unnecessary object retention.
* **Explicitly Excluded**:
  * Password History, Temporary Passwords, Email, SMS, OTP, Password Expiry, Force Password Change, Unlock User, Login Changes, Session Logout, Audit History, Bulk Reset, Role Changes, Permissions, Authentication module changes, Authorization module changes, database schema changes, migrations, Repositories, Mediator, CQRS, Phase 2.7 functionality.
* **Validation Status**: Build verified successfully with no errors. UI connectivity complete (DataTemplate, DI, navigation). PasswordBox binding reusable (PasswordBoxHelper). Code-behind eliminated (pure MVVM). Navigation lifecycle consistent (OnNavigatedToAsync). Users screen refreshes after successful operations. Search/filter state preserved. PasswordHasher reused from Phase 1. Validator purity maintained. Security practices followed (no password logging). Performance optimized (single queries, async throughout). Architecture enterprise-grade (MVVM, DI, separation of concerns). No Phase 2.7 functionality introduced. No database schema changes. No authentication/authorization changes. No completed phases modified except where explicitly required. Phase 2.6 (Reset Password) is COMPLETE and FROZEN.

### Phase 2.6C: Navigation Lifecycle Architecture Refactoring
* **Goal**: Refactor navigation lifecycle to eliminate architectural coupling between child and parent ViewModels. Phase 2.6B introduced a regression where child ViewModels (ResetPasswordViewModel) needed to cast CurrentViewModel and call parent refresh methods (RefreshUsersAsync). This violated loose coupling principles.
* **Included**:
  * **Modified** `Desktop/Navigation/NavigationService.cs`: Updated `GoBackAsync()` to call full navigation lifecycle after restoring previous ViewModel. Now calls `INavigationAware.OnNavigatedTo(null)` and `ViewModelBase.OnNavigatedToAsync()` to match `NavigateAsync()` behavior.
  * **Modified** `MVVM/ViewModelBase.cs`: Added `OnNavigatedToAsync()` virtual method as lifecycle hook for ViewModels to refresh when navigated to. Called by NavigationService after both `NavigateAsync()` and `GoBackAsync()`.
  * **Modified** `Administration/Users/ViewModels/UsersViewModel.cs`: Removed `RefreshUsersAsync()` public method. Refresh logic now entirely contained within `OnNavigatedToAsync()` override. Search/filter state preserved automatically.
  * **Modified** `Administration/Users/ViewModels/ResetPasswordViewModel.cs`: Removed lines that cast `CurrentViewModel` to `UsersViewModel` and called `RefreshUsersAsync()`. After successful password reset, now only calls `await GoBackAsync()`.
* **Architecture Improvements**:
  * **NavigationService Ownership**: NavigationService now owns the complete navigation lifecycle. Both `NavigateAsync()` and `GoBackAsync()` invoke lifecycle methods on ViewModels. No ViewModel needs to manually trigger parent refreshes.
  * **Loose Coupling**: Child ViewModels no longer reference parent ViewModels. No CurrentViewModel casts. No parent method calls. Child ViewModels simply navigate back, and NavigationService handles the lifecycle.
  * **Self-Contained Refresh**: Parent ViewModels refresh themselves through `OnNavigatedToAsync()`. UsersViewModel automatically refreshes when navigated back to, without any child ViewModel involvement.
  * **Single Responsibility**: Each ViewModel owns its refresh logic. NavigationService owns the lifecycle orchestration. Clear separation of concerns.
  * **SOLID Principles**: Single Responsibility (ViewModels only refresh themselves), Open/Closed (ViewModelBase extensible via virtual methods), Dependency Inversion (ViewModels depend on abstractions INavigationAware, ViewModelBase).
* **Navigation Lifecycle Flow**:
  * **NavigateAsync**: Create ViewModel → Push to history → Set Current → Notify → Call INavigationAware.OnNavigatedTo → Call InitializeAsync
  * **GoBackAsync**: Pop from history → Set Current → Notify → Call INavigationAware.OnNavigatedTo(null) → Call OnNavigatedToAsync
  * **Consistency**: Both navigation paths now invoke the same lifecycle methods, ensuring ViewModels can refresh appropriately regardless of how they are navigated to.
* **Verification**:
  * **Add User Flow**: Users → AddUserViewModel → Save → GoBackAsync → UsersViewModel.OnNavigatedToAsync → Users refreshes automatically
  * **Edit User Flow**: Users → EditUserViewModel → Save → GoBackAsync → UsersViewModel.OnNavigatedToAsync → Users refreshes automatically
  * **Reset Password Flow**: Users → ResetPasswordViewModel → Save → GoBackAsync → UsersViewModel.OnNavigatedToAsync → Users refreshes automatically
  * **No Child Knowledge**: Child ViewModels (AddUserViewModel, EditUserViewModel, ResetPasswordViewModel) have no knowledge of UsersViewModel or its refresh methods.
  * **Search/Filter Preservation**: SearchText property preserved during refresh. LoadUsersAsync uses current SearchText value. No explicit clearing.
* **Explicitly Excluded**:
  * Authentication, Authorization, Password hashing, Password reset logic, Validators, UserCommandService, UserQueryService, Database, Entities, EF Core, DI registrations, NavigationParameter implementation, INavigationAware interface, UI, XAML, Commands, Business rules, Phase 2.7 functionality.
* **Validation Status**: Build verified successfully with no errors. Navigation lifecycle consistent between NavigateAsync and GoBackAsync. Child ViewModels decoupled from parent ViewModels. No CurrentViewModel casts remain. RefreshUsersAsync removed. UsersViewModel refreshes through OnNavigatedToAsync. Search/filter state preserved. Application behavior identical. Architecture enterprise-grade (SOLID, loose coupling, single responsibility). No Phase 2.7 functionality introduced. Phase 2.6 (Reset Password) is COMPLETE and FROZEN.

### Phase 2.7: Soft Delete Infrastructure
* **Goal**: Prepare the application for enterprise-grade soft delete by introducing the infrastructure required so that Delete User can be implemented safely in the next phase. This phase does NOT implement Delete User functionality.
* **Included**:
  * **Modified** `VeteranLogistics.Data/Entities/Administration/User.cs`: Added soft-delete fields to User entity: `IsDeleted` (bool, default: false) and `DeletedOn` (DateTime?, default: null). Both fields have sensible defaults ensuring existing records remain unaffected.
  * **Created** `VeteranLogistics.Data/Configurations/UserConfiguration.cs`: Entity configuration implementing `IEntityTypeConfiguration<User>`. Configures global query filter: `builder.HasQueryFilter(user => !user.IsDeleted)`. Automatically applies to all User queries through EF Core.
  * **Created** `Migrations/20260707114333_AddUserSoftDeleteFields.cs`: EF Core migration that adds `DeletedOn` column (datetime2, nullable) and `IsDeleted` column (bit, not null, default: false). No other schema changes. Existing records will have `IsDeleted = false` and `DeletedOn = null`.
  * **Modified** `Desktop/Navigation/NavigationService.cs`: Fixed duplicate variable name error from previous architecture review. Combined InitializeAsync() and OnNavigatedToAsync() calls into single if block to resolve compilation error. This was a bug fix, not a Phase 2.7 change.
* **Global Query Filter Explanation**:
  * The global query filter is configured in UserConfiguration.cs: `builder.HasQueryFilter(user => !user.IsDeleted)`
  * This filter is automatically applied by EF Core to all queries against the Users DbSet
  * Applies to: UserQueryService.GetAllUsersAsync(), UserQueryService.SearchUsersAsync(), UserQueryService.GetUserForEditAsync(), AuthenticationRepository.GetByUsernameAsync()
  * Benefits: No need to manually add `.Where(x => !x.IsDeleted)` in every query, consistent behavior across all services, future-proof (any new queries automatically inherit the filter), can be bypassed if needed using `IgnoreQueryFilters()`
* **Existing Functionality Verification**:
  * **Login**: AuthenticationRepository uses `_dbContext.Set<User>()` which automatically applies the global query filter. Deleted users cannot log in.
  * **User Listing**: UserQueryService uses `_dbContext.Users` which automatically applies the global query filter. Deleted users won't appear.
  * **Search**: UserQueryService uses `_dbContext.Users` which automatically applies the global query filter. Deleted users won't appear in search results.
  * **Edit User**: UserQueryService uses `_dbContext.Users` which automatically applies the global query filter. Deleted users cannot be edited.
  * All other features unchanged and will continue to work identically.
* **Explicitly Excluded**:
  * Delete User service method, Delete User models, Delete User validators, Delete User command, Delete User button, Delete User dialog, Delete User navigation, Restore User, Permanent Delete, Bulk Delete, Recycle Bin, Deleted User Listing, Admin Restore, Any UI, Any ViewModel, Any Command, Any Navigation, Authentication changes, Authorization changes, Password hashing changes, Session Management changes, MVVM changes, Validation changes, Services unrelated to User queries.
* **Validation Status**: Build verified successfully with no errors. Soft-delete infrastructure in place. Database schema ready (migration created). Global query filter configured. All existing queries automatically protected. No conflicts with existing functionality. Clean separation of concerns. Enterprise-grade implementation. No Delete User functionality implemented. No UI added. All previous phases remain unchanged. Application is now ready for Phase 2.8 (Delete User).

### Phase 2.8: Delete User (Soft Delete)
* **Goal**: Implement enterprise-grade Delete User functionality using the Soft Delete infrastructure introduced in Phase 2.7. Deleting a user MUST perform a soft delete. The record must remain in the database. The user must automatically disappear from the application because of the existing EF Core Global Query Filter.
* **Included**:
  * **Created** `Administration/Users/Models/DeleteUserRequest.cs`: Request model with UserId property.
  * **Created** `Administration/Users/Models/DeleteUserResult.cs`: Result model with IsSuccess and ErrorMessage properties. Factory methods: Success() and Failure(string message).
  * **Created** `Administration/Users/Contracts/IDeleteUserValidator.cs`: Validator interface using existing ValidationResult infrastructure.
  * **Created** `Administration/Users/Validators/DeleteUserValidator.cs`: Validates request not null and UserId is positive. No DbContext, no business logic, no logging.
  * **Modified** `Administration/Users/Contracts/IUserCommandService.cs`: Added DeleteUserAsync method signature.
  * **Modified** `Administration/Users/Services/UserCommandService.cs`: Injected IDeleteUserValidator. Implemented DeleteUserAsync method that reuses existing DbContext, logging, and ValidationResult patterns. Performs soft delete by setting user.IsDeleted = true and user.DeletedOn = DateTime.UtcNow. No other user properties modified.
  * **Modified** `Administration/Users/ViewModels/UsersViewModel.cs`: Added System.Windows import for MessageBox. Added DeleteUserCommand.NotifyCanExecuteChanged() to SelectedUser setter. Added DeleteUserAsync command with confirmation dialog. Command requires SelectedUser. Shows confirmation dialog before deleting. Reloads Users list on success. Clears SelectedUser on success. Displays validation errors on failure.
  * **Modified** `Administration/Users/Views/UsersView.xaml`: Added Delete User button beside Deactivate button. Uses existing Design System resources (IconButtonStyle, CaptionTextBlockStyle). No icons, no emojis, no inline styling.
  * **Modified** `Administration/DependencyInjection/AdministrationServiceCollectionExtensions.cs`: Registered IDeleteUserValidator as scoped service.
* **Delete Workflow**:
  1. User selects a user in the Users grid
  2. User clicks Delete User button
  3. MessageBox confirmation dialog appears: "Delete User - Are you sure you want to delete this user? This action hides the user from the application."
  4. If user confirms, DeleteUserCommand executes
  5. DeleteUserRequest is created with UserId
  6. IDeleteUserValidator validates the request
  7. UserCommandService.DeleteUserAsync locates the user
  8. If user not found, returns failure
  9. If user found, performs soft delete: Sets user.IsDeleted = true, Sets user.DeletedOn = DateTime.UtcNow
  10. SaveChangesAsync() commits changes
  11. Users list is reloaded
  12. User disappears from grid automatically due to Global Query Filter
* **Explicitly Excluded**:
  * Permanent Delete, Physical DELETE statement, Restore User, Deleted Users screen, Recycle Bin, Bulk Delete, Audit History, Delete Reason, Delete Notes, Delete Permissions, Authentication changes, Authorization changes, Navigation redesign, Database schema changes, Migration, IgnoreQueryFilters(), Username modification, DisplayName modification, PasswordHash modification, PasswordSalt modification, Role modification, IsActive modification, CreatedOn modification, ModifiedOn modification.
* **Validation Status**: Build verified successfully with no errors. Only IsDeleted and DeletedOn are modified during delete operation. User is NOT physically deleted. User disappears because of existing Global Query Filter. Authentication automatically blocks deleted users. No database schema changes were made. No authentication or authorization functionality was modified. No Restore User or Permanent Delete functionality introduced. Phase 2.8 is production-ready and ready to be frozen.

### Phase 2.9.1: Administration → Role Management Foundation & Role Listing
* **Goal**: Implement the foundation of the Role Management module together with the initial Role Listing screen. Create the complete database and domain foundation before implementing CRUD operations. This phase establishes the infrastructure for future Role Management features.
* **Included**:
  * **Created** `VeteranLogistics.Data/Entities/Administration/Role.cs`: Role entity with properties: Id, Name, Description, IsActive, IsSystemRole, CreatedOn, ModifiedOn, IsDeleted, DeletedOn. Inherits from BaseEntity. Name is required and unique. Description is optional. IsActive defaults to true. IsSystemRole defaults to false. IsDeleted defaults to false.
  * **Created** `VeteranLogistics.Data/Configurations/RoleConfiguration.cs`: Fluent API configuration implementing IEntityTypeConfiguration<Role>. Configures Name as required with max length 100 and unique index. Configures Description as optional with max length 500. Configures global query filter: builder.HasQueryFilter(role => !role.IsDeleted). Automatically applies to all Role queries through EF Core.
  * **Created** `Migrations/20260707XXXXXX_AddRoleTable.cs`: EF Core migration that adds Roles table with all required columns. No other schema changes.
  * **Modified** `VeteranLogistics.Data/Context/VeteranLogisticsDbContext.cs`: Added Roles DbSet property.
  * **Created** `Administration/Roles/Models/RoleListItem.cs`: Read-only model for displaying roles in the grid with properties: Id, Name, Description, IsActive, IsSystemRole, CreatedOn.
  * **Created** `Administration/Roles/Models/RoleSearchModel.cs`: Placeholder model for future search functionality with SearchTerm property.
  * **Created** `Administration/Roles/Contracts/IRoleQueryService.cs`: Service contract for querying role data with GetAllRolesAsync method. API designed so search can be added later without breaking consumers.
  * **Created** `Administration/Roles/Services/RoleQueryService.cs`: Implementation of IRoleQueryService using EF Core with AsNoTracking(), projecting entities into RoleListItem, ordered by Name. Registered as Scoped.
  * **Created** `Administration/Roles/ViewModels/RolesViewModel.cs`: ViewModel with ObservableCollection<RoleListItem>, SelectedRole, RefreshCommand. Loads roles on initialization and navigation. Uses existing navigation lifecycle (InitializeAsync, OnNavigatedToAsync). No Add/Edit/Delete commands.
  * **Created** `Administration/Roles/Views/RolesView.xaml`: Read-only DataGrid view with page title, search textbox (disabled placeholder), Add Role button (disabled placeholder), Refresh button (functional), loading indicator, empty state. DataGrid displays: Name, Description, Active, System Role, Created Date. Grid is read-only with no action buttons. Layout matches Users module exactly.
  * **Created** `Administration/Roles/Views/RolesView.xaml.cs`: Code-behind for RolesView with standard UserControl initialization.
  * **Modified** `Administration/DependencyInjection/AdministrationServiceCollectionExtensions.cs`: Added DI registrations for IRoleQueryService (Scoped) and RolesViewModel (Transient).
  * **Modified** `Resources/DataTemplates.xaml`: Added namespace declarations for Administration.Roles.ViewModels and Administration.Roles.Views. Added DataTemplate mapping for RolesViewModel to RolesView.
  * **Modified** `Desktop/Shell/ShellViewModel.cs`: Added NavigateToRolesCommand property (IAsyncRelayCommand). Command navigates to RolesViewModel using INavigationService.
  * **Modified** `Desktop/Shell/ShellView.xaml`: Added Roles menu item under Administration menu with Command binding to NavigateToRolesCommand.
* **Explicitly Excluded**:
  * Add Role, Edit Role, Delete Role, Activate/Deactivate Role, Permission Assignment, Search functionality, Role CRUD operations, Role Assignment, Permission Matrix, Repository Pattern, MediatR, CQRS, Business logic inside Views, DbContext inside ViewModels.
* **Validation Status**: Build verified successfully with no errors. Roles table exists in SQL Server (migration created). EF Core migration executes successfully. Global Query Filter excludes soft-deleted roles. Roles page opens from Administration menu. Roles load from database. Loading indicator works correctly. Empty state displays when no roles exist. Query/Command separation maintained. No business logic in Views. No DbContext in ViewModels. Existing architecture fully reused. Implementation matches Users module quality and architecture. Phase 2.9.1 is production-ready and ready to be frozen.

### Phase 2.9.2: Administration → Role Management → Search Roles
* **Goal**: Implement Role Search functionality using the exact architecture and behaviour established in the Users module. This phase implements only search functionality without any CRUD operations.
* **Included**:
  * **Modified** `Administration/Roles/Contracts/IRoleQueryService.cs`: Added SearchRolesAsync method with search term and CancellationToken parameters. Returns IReadOnlyList<RoleListItem>.
  * **Modified** `Administration/Roles/Services/RoleQueryService.cs`: Implemented SearchRolesAsync using EF.Functions.Like for case-insensitive search on Name and Description fields. Uses AsNoTracking(), reuses ProjectToListItem() projection, orders by Name, respects Global Query Filter, supports CancellationToken. Empty or whitespace-only search returns all roles.
  * **Modified** `Administration/Roles/ViewModels/RolesViewModel.cs`: Added SearchText property with two-way binding. Added _searchCancellationTokenSource field for debouncing. Added DebouncedSearchAsync() method with 300ms delay and cancellation handling. Added SearchRolesAsync() method that calls query service and updates UI. SearchText property setter triggers debounced search on change. Loading indicator shows during search.
  * **Modified** `Administration/Roles/Views/RolesView.xaml`: Enabled Search TextBox with Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" binding. Removed IsEnabled="False" placeholder. Maintains existing styling, layout, and placeholder structure.
* **Search Behavior**:
  * Debounced with 300ms delay to prevent excessive database queries
  * Asynchronous with CancellationToken support
  * Cancels previous search when user continues typing
  * Case-insensitive search on Name and Description fields
  * Empty or whitespace-only input automatically reloads all roles
  * Respects soft delete via Global Query Filter
  * Loading indicator displays during search operations
* **Explicitly Excluded**:
  * Add Role, Edit Role, Delete Role, Activate/Deactivate Role, Permission Assignment, Role CRUD operations, Role Assignment, Permission Matrix, Repository Pattern, MediatR, CQRS, Business logic inside Views, DbContext inside ViewModels, Unit tests, Integration tests, Configurable debounce delay, Exception logging beyond OperationCanceledException, Database index on Description.
* **Architecture Compliance**:
  * MVVM: ViewModel contains no business logic, View binds to ViewModel properties
  * SOLID: Single Responsibility, Open/Closed, Dependency Inversion principles followed
  * Query Layer: Uses AsNoTracking(), efficient LINQ with projection, ordered by Name, respects Global Query Filter, supports CancellationToken
  * Performance: Debounced search (300ms), CancellationToken cancels previous searches, single query execution per search, ConfigureAwait(false) in query service
  * Navigation: No navigation changes, reuses existing infrastructure
  * Consistency: Identical debounce pattern to UsersViewModel, same CancellationToken handling, same loading indicator pattern, same case-insensitive search using EF.Functions.Like
* **Known Limitations**:
  * Database index exists on Name but not on Description (may impact performance on large datasets when searching by description)
  * No unit tests or integration tests for search behavior
  * Debounce delay is hard-coded to 300ms (not configurable)
  * Exception handling only catches OperationCanceledException (other exceptions propagate uncaught and unlogged)
* **Validation Status**: Build verified successfully with no errors. Search works identically to Users module. Search is debounced. Search is asynchronous. Search supports Name and Description. Search is case insensitive. Empty search reloads all roles. Search respects soft delete. Loading indicator behaves correctly. No duplicate queries occur. CancellationToken is correctly handled. Query/Command separation is maintained. No DbContext exists in the ViewModel. Existing architecture is fully reused. No regression introduced into previous phases. Phase 2.9.2 is complete and ready for Phase 2.9.2A.

### Phase 2.9.2A: Role Search Architecture Review
* **Goal**: Perform a comprehensive enterprise architecture review of the Role Search implementation to ensure it matches the architecture, quality, and behaviour of the Users module. Refine code where necessary without introducing new features or redesigning existing architecture.
* **Included**:
  * **Modified** `Administration/Roles/ViewModels/RolesViewModel.cs`: Removed ILogger<RolesViewModel> dependency to match UsersViewModel constructor pattern. Changed debounce delay from ApplicationConstants.SearchDebounceDelayMilliseconds to hardcoded 300ms to match UsersViewModel exactly. Removed extra exception handling with logging from DebouncedSearchAsync and SearchRolesAsync to match UsersViewModel's minimal error handling pattern.
  * **Modified** `Administration/DependencyInjection/AdministrationServiceCollectionExtensions.cs`: Changed RolesViewModel registration from factory delegate with manual service resolution to simple AddTransient<RolesViewModel>() to match UsersViewModel registration pattern.
  * **Deleted** `Administration/Roles/Models/RoleSearchModel.cs`: Removed unused dead code placeholder file.
* **Architecture Review Results**:
  * **MVVM Compliance**: PASS - ViewModel properly inherits from ViewModelBase, View contains no business logic, clean separation of concerns
  * **SOLID Compliance**: PASS - Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion all followed
  * **Search Architecture**: PASS - Debounce implementation now matches UsersViewModel exactly (300ms hardcoded), CancellationToken usage correct, previous search cancellation working, empty search behaviour correct
  * **Query Layer**: PASS - Interface design matches IUserQueryService, async implementation correct, AsNoTracking used, LINQ efficient with projection, case-insensitive search, soft delete filtering applied
  * **ViewModel**: PASS - SearchText implementation correct, debounce logic matches UsersViewModel, CancellationToken usage correct, loading indicator working, no duplicate logic, no business logic, no DbContext usage
  * **MVVM**: PASS - View bindings correct, commands follow conventions, observable properties implemented correctly, no code-behind logic
  * **Navigation**: PASS - Navigation behaviour unchanged, existing lifecycle preserved, no regressions
  * **Dependency Injection**: PASS - Service registrations correct (IRoleQueryService as Scoped, RolesViewModel as Transient), constructor injection only, no manual object creation, no service locator
  * **Performance**: PASS - Debounce delay appropriate (300ms), no duplicate queries, previous searches cancelled correctly, efficient LINQ, AsNoTracking used, CancellationToken propagated correctly, no unnecessary allocations, single query execution per search
  * **UI**: PASS - Search TextBox reused, existing styles reused, existing spacing reused, Design System reused, loading indicator behaviour correct, no inline styling, no hardcoded resources
  * **Code Quality**: PASS - SOLID principles followed, DRY principle followed, naming consistent, namespace consistent, XML documentation present, dead code removed, no duplicate code, no long methods, no magic strings, no magic numbers
  * **Security**: PASS - Search respects Global Query Filter, no sensitive logging, no authentication changes, no authorization changes, no security regressions
* **Justification for Changes**:
  * **ILogger Removal**: UsersViewModel does not have ILogger dependency. Removing it eliminates architectural inconsistency and reduces unnecessary dependencies.
  * **Hardcoded Debounce Delay**: UsersViewModel uses hardcoded 300ms delay. Changing from constant to hardcoded value ensures both modules use identical debounce implementation.
  * **Removed Extra Exception Handling**: UsersViewModel has minimal exception handling (only OperationCanceledException). Removing extra logging aligns error handling pattern with reference architecture.
  * **Simplified DI Registration**: UsersViewModel uses simple AddTransient registration. Factory delegate was unnecessary since constructor can be resolved automatically by DI container.
  * **Deleted RoleSearchModel.cs**: File was unused dead code described as "placeholder for future search functionality" but search is already implemented without it.
* **Explicitly Unchanged**:
  * Search architecture, query layer implementation, ViewModel search logic, UI layout, navigation, authentication, authorization, database schema, Global Query Filter, Phase 0-2.8, Phase 2.9.1, existing functionality
* **Validation Status**: Build verified successfully with no errors. Role Search implementation now matches Users module reference architecture exactly. All architectural inconsistencies resolved. Implementation is enterprise-grade and ready to be frozen.

### Phase 2.9.3: Administration → Role Management → Add Role
* **Goal**: Implement complete Add Role functionality following the exact architecture, patterns, and quality established by the Users module.
* **Included**:
  * **Created** `Administration/Roles/Models/CreateRoleRequest.cs`: Request model for role creation containing Name, Description, and IsActive properties.
  * **Created** `Administration/Roles/Models/CreateRoleResult.cs`: Result model with RoleId, IsSuccess, ErrorMessage properties and Success/Failure factory methods.
  * **Created** `Administration/Roles/Contracts/ICreateRoleValidator.cs`: Interface for validating create role requests using existing ValidationResult infrastructure.
  * **Created** `Administration/Roles/Validators/CreateRoleValidator.cs`: Implementation that validates Name (required, max 100 chars, trimmed), Description (optional, max 500 chars), and provides friendly error messages.
  * **Created** `Administration/Roles/Contracts/IRoleCommandService.cs`: Service contract for role command operations with CreateRoleAsync method.
  * **Created** `Administration/Roles/Services/RoleCommandService.cs`: Implementation that validates request, checks duplicate role names (case-insensitive, trimmed), creates new role with default values, persists changes, and returns appropriate result.
  * **Created** `Administration/Roles/ViewModels/AddRoleViewModel.cs`: ViewModel for Add Role screen with Name, Description, IsActive properties, SaveCommand, CancelCommand, ValidationError, and loading indicator.
  * **Created** `Administration/Roles/Views/AddRoleView.xaml`: XAML for Add Role form with Role Name, Description, Active checkbox, Save/Cancel buttons, validation error display, and loading indicator.
  * **Created** `Administration/Roles/Views/AddRoleView.xaml.cs`: Code-behind for AddRoleView.
  * **Modified** `Administration/Roles/Views/RolesView.xaml`: Enabled Add Role button with Command binding to AddRoleCommand.
  * **Modified** `Administration/Roles/ViewModels/RolesViewModel.cs`: Added INavigationService dependency injection and AddRoleCommand that navigates to AddRoleViewModel.
  * **Modified** `Administration/DependencyInjection/AdministrationServiceCollectionExtensions.cs`: Added IRoleCommandService (Scoped), ICreateRoleValidator (Scoped), and AddRoleViewModel (Transient) registrations.
  * **Modified** `Resources/DataTemplates.xaml`: Added DataTemplate mapping AddRoleViewModel → AddRoleView.
* **Architecture Review Results**:
  * **MVVM Compliance**: PASS - AddRoleViewModel properly inherits from ViewModelBase, AddRoleView contains no business logic, clean separation of concerns, proper data binding.
  * **SOLID Compliance**: PASS - Single Responsibility (each class has one purpose), Open/Closed (extensible without modification), Liskov Substitution (interfaces properly implemented), Interface Segregation (focused interfaces), Dependency Inversion (depends on abstractions).
  * **Validation Architecture**: PASS - Validation separated into dedicated validator (ICreateRoleValidator), uses existing ValidationResult infrastructure, no validation in ViewModel, friendly error messages.
  * **Command Layer**: PASS - IRoleCommandService follows existing pattern, RoleCommandService handles business logic and persistence, duplicate check with AsNoTracking, async throughout, ConfigureAwait(false) in service layer, proper error handling and logging.
  * **Query/Command Separation**: PASS - IRoleCommandService is separate from IRoleQueryService, no DbContext in ViewModels, clear separation of concerns.
  * **Navigation/Dialog Integration**: PASS - Uses existing INavigationService, NavigateAsync to AddRoleViewModel, GoBackAsync after save/cancel, matches Users module pattern.
  * **Dependency Injection**: PASS - All services registered correctly (IRoleCommandService as Scoped, ICreateRoleValidator as Scoped, AddRoleViewModel as Transient), constructor injection only, no manual object creation.
  * **Performance**: PASS - Single database write, duplicate check uses efficient query with AsNoTracking, async throughout, CancellationToken support, ConfigureAwait(false) in service layer, no unnecessary refreshes.
  * **UI**: PASS - Reuses existing TextBox styles, Button styles, Validation styles, Dialog layout, spacing, typography, Design System resources, no inline styling, no hardcoded resources.
  * **Code Quality**: PASS - SOLID principles followed, DRY principle followed, naming consistent with Users module, namespace consistent, XML documentation present, no duplicate code, no long methods, no magic strings, no magic numbers.
  * **Security**: PASS - Input trimmed before validation and database, duplicate validation prevents conflicting data, soft delete respected in duplicate check, no sensitive logging, no authentication/authorization changes.
* **Duplicate Handling**: PASS - Case-insensitive comparison using trimmed name, checks existing roles with AsNoTracking, returns friendly error message if duplicate exists.
* **Explicitly Unchanged**:
  * Edit Role, Activate/Deactivate Role, Delete Role, Permission Assignment, database schema, Global Query Filter, Phase 0-2.8, Phase 2.9.1, Phase 2.9.2, existing functionality
* **Validation Status**: Build verified successfully with no errors. Add Role dialog opens from Roles screen. User can enter Role Name, Description and Active status. Validation works correctly. Duplicate role names are prevented. Role is successfully saved. Dialog closes after successful save. Roles list refreshes automatically via OnNavigatedToAsync. Query/Command separation is maintained. No DbContext exists in ViewModels. Existing architecture is fully reused. No regressions are introduced. Implementation is enterprise-grade and ready for Phase 2.9.3A.

### Phase 2.9.3A: Role Add Architecture Review
* **Goal**: Perform a comprehensive enterprise architecture review of the Add Role implementation to ensure it matches the architecture, quality, and behaviour of the Users module. Refine code where necessary without introducing new features or redesigning existing architecture.
* **Included**:
  * **No code modifications required** - The Add Role implementation already matches the Users module architecture exactly.
* **Architecture Review Results**:
  * **MVVM Compliance**: PASS - AddRoleViewModel properly inherits from ViewModelBase, AddRoleView contains no business logic, clean separation of concerns, proper data binding with UpdateSourceTrigger=PropertyChanged.
  * **SOLID Compliance**: PASS - Single Responsibility (each class has one purpose), Open/Closed (extensible without modification), Liskov Substitution (interfaces properly implemented), Interface Segregation (focused interfaces), Dependency Inversion (depends on abstractions).
  * **Model Review**: PASS - CreateRoleRequest naming matches CreateUserRequest pattern, properties have appropriate nullability and default values (string.Empty, true for IsActive), XML documentation present, namespace consistent with Users module.
  * **Validation Review**: PASS - CreateRoleValidator follows CreateUserValidator pattern exactly, uses existing ValidationResult infrastructure, validates required fields and maximum lengths, provides friendly error messages, no validation in ViewModel, input trimming in service layer.
  * **Command Layer Review**: PASS - IRoleCommandService interface design matches IUserCommandService, CreateRoleAsync method naming consistent, return type matches CreateUserResult pattern, RoleCommandService validates request, checks duplicates with AsNoTracking, uses ConfigureAwait(false), proper exception handling with logging, single SaveChangesAsync call.
  * **Query/Command Separation**: PASS - IRoleCommandService is separate from IRoleQueryService, no DbContext in ViewModels, clear separation of concerns, command service handles business logic only.
  * **ViewModel Review**: PASS - AddRoleViewModel constructor dependencies match AddUserViewModel (command service + navigation service), observable properties use SetProperty, SaveCommand and CancelCommand implemented with [RelayCommand], validation error handling matches pattern, busy state managed correctly, navigation uses GoBackAsync, no business logic, no DbContext.
  * **View Review**: PASS - AddRoleView reuses existing Design System resources (BackgroundBrush, SurfaceBrush, BorderBrush, StandardBorderThickness, StandardCornerRadius, Medium/Large/Small/ExtraSmall spacing), TextBox styles reused, Button styles reused, Validation styles reused, Dialog layout matches AddUserView, no inline styling, no hardcoded colours or fonts.
  * **Navigation/Dialog Review**: PASS - Add Role button in RolesView bound to AddRoleCommand, navigation uses NavigateAsync<AddRoleViewModel>(), Save closes dialog with GoBackAsync(), Cancel closes dialog with GoBackAsync(), Roles list refreshes via OnNavigatedToAsync in RolesViewModel, existing search/filter state preserved, navigation lifecycle unchanged.
  * **Dependency Injection Review**: PASS - IRoleCommandService registered as Scoped (matches IUserCommandService), ICreateRoleValidator registered as Scoped (matches ICreateUserValidator), AddRoleViewModel registered as Transient (matches AddUserViewModel), constructor injection only, no manual object creation, no service locator pattern.
  * **Performance Review**: PASS - Single database write with SaveChangesAsync, duplicate check uses efficient query with AsNoTracking, async throughout with ConfigureAwait(false) in service layer, CancellationToken support in method signature, no duplicate queries, no unnecessary UI refreshes.
  * **Code Quality Review**: PASS - SOLID principles followed, DRY principle followed (no duplicate code), naming consistent with Users module (CreateRoleRequest, CreateRoleResult, ICreateRoleValidator, IRoleCommandService), folder structure matches Users module, namespace consistent, XML documentation present on all public members, no dead code, no long methods, no magic strings (constants for max lengths), no magic numbers.
  * **Security Review**: PASS - Duplicate check respects Global Query Filter (soft-deleted roles excluded), input trimmed in service layer before database operations, no sensitive logging (only role name logged), no authentication changes, no authorization changes, no security regressions.
* **Explicitly Unchanged**:
  * All Phase 2.9.3 implementation files, database schema, Global Query Filter, Phase 0-2.8, Phase 2.9.1, Phase 2.9.2, existing functionality
* **Files Modified**: None - No code changes required during architecture review.
* **Justification for No Changes**:
  * **Model Consistency**: CreateRoleRequest and CreateRoleResult naming and structure already match CreateUserRequest and CreateUserResult exactly.
  * **Validation Architecture**: CreateRoleValidator already follows the same pattern as CreateUserValidator with identical ValidationResult usage and error handling.
  * **Command Layer**: RoleCommandService already implements the same async pattern, duplicate checking logic, ConfigureAwait(false) usage, and exception handling as UserCommandService.
  * **ViewModel Pattern**: AddRoleViewModel already matches AddUserViewModel's constructor pattern, command implementation, and navigation flow.
  * **View Consistency**: AddRoleView already reuses the same Design System resources, styles, and layout as AddUserView.
  * **DI Registrations**: Service lifetimes already match the Users module (Scoped for services, Transient for ViewModels).
  * **Performance**: Async/await pattern, AsNoTracking usage, and ConfigureAwait(false) already implemented correctly.
  * **Code Quality**: Naming, documentation, and structure already consistent with the Users module.
* **Validation Status**: Build verified successfully with no errors. Add Role architecture matches Add User exactly. Model naming is consistent with the Users module. Validation architecture is identical. Command layer follows the same patterns. Navigation/Dialog behaviour matches the Users module. Query/Command separation is maintained. No DbContext exists in ViewModels. Dependency Injection follows existing conventions. Performance is optimal. No duplicate queries. No duplicate code. No dead code. Existing phases remain unchanged. No regressions are introduced. The implementation is enterprise-grade and ready to be frozen.

### Phase 2.9.4: Administration → Role Management → Edit Role
* **Goal**: Implement complete Edit Role functionality following the exact architecture, patterns, behaviour, and quality established by the Users module.
* **Included**:
  * **Created** `Administration/Roles/Models/UpdateRoleRequest.cs`: Request model for role update containing Id, Name, Description, and IsActive properties.
  * **Created** `Administration/Roles/Models/UpdateRoleResult.cs`: Result model with IsSuccess, ErrorMessage properties and Success/Failure factory methods.
  * **Created** `Administration/Roles/Contracts/IUpdateRoleValidator.cs`: Interface for validating update role requests using existing ValidationResult infrastructure.
  * **Created** `Administration/Roles/Validators/UpdateRoleValidator.cs`: Implementation that validates Name (required, max 100 chars, trimmed), Description (optional, max 500 chars), and provides friendly error messages.
  * **Created** `Administration/Roles/Models/EditRoleModel.cs`: Model representing role data for editing with Id, Name, Description, and IsActive properties.
  * **Extended** `Administration/Roles/Contracts/IRoleCommandService.cs`: Added UpdateRoleAsync method for role update operations.
  * **Extended** `Administration/Roles/Services/RoleCommandService.cs`: Added UpdateRoleAsync implementation that validates request, verifies role exists, checks duplicate role names (excluding current role), updates role properties, persists changes, and returns appropriate result.
  * **Extended** `Administration/Roles/Contracts/IRoleQueryService.cs`: Added GetRoleForEditAsync method for loading a single role for editing.
  * **Extended** `Administration/Roles/Services/RoleQueryService.cs`: Added GetRoleForEditAsync implementation that loads role data using AsNoTracking, respects Global Query Filter, and returns EditRoleModel.
  * **Created** `Administration/Roles/ViewModels/EditRoleViewModel.cs`: ViewModel for Edit Role screen with Name, Description, IsActive properties, SaveCommand, CancelCommand, ValidationError, loading indicator, and INavigationAware implementation for receiving RoleId parameter.
  * **Created** `Administration/Roles/Views/EditRoleView.xaml`: XAML for Edit Role form with Role Name, Description, Active checkbox, Save/Cancel buttons, validation error display, and loading indicator.
  * **Created** `Administration/Roles/Views/EditRoleView.xaml.cs`: Code-behind for EditRoleView.
  * **Modified** `Administration/Roles/Views/RolesView.xaml`: Added Edit Role button with Command binding to EditRoleCommand.
  * **Modified** `Administration/Roles/ViewModels/RolesViewModel.cs`: Added EditRoleCommand that navigates to EditRoleViewModel with RoleId parameter when a role is selected.
  * **Modified** `Administration/DependencyInjection/AdministrationServiceCollectionExtensions.cs`: Added IUpdateRoleValidator (Scoped) and EditRoleViewModel (Transient) registrations.
  * **Modified** `Resources/DataTemplates.xaml`: Added DataTemplate mapping EditRoleViewModel → EditRoleView.
* **Architecture Review Results**:
  * **MVVM Compliance**: PASS - EditRoleViewModel properly inherits from ViewModelBase and INavigationAware, EditRoleView contains no business logic, clean separation of concerns, proper data binding with UpdateSourceTrigger=PropertyChanged.
  * **SOLID Compliance**: PASS - Single Responsibility (each class has one purpose), Open/Closed (extensible without modification), Liskov Substitution (interfaces properly implemented), Interface Segregation (focused interfaces), Dependency Inversion (depends on abstractions).
  * **Model Review**: PASS - UpdateRoleRequest naming matches UpdateUserRequest pattern, EditRoleModel matches EditUserModel pattern, properties have appropriate nullability and default values, XML documentation present, namespace consistent with Users module.
  * **Validation Review**: PASS - UpdateRoleValidator follows UpdateUserValidator pattern exactly, uses existing ValidationResult infrastructure, validates required fields and maximum lengths, provides friendly error messages, no validation in ViewModel, input trimming in service layer.
  * **Command Layer Review**: PASS - IRoleCommandService UpdateRoleAsync matches IUserCommandService UpdateUserAsync pattern, RoleCommandService validates request, verifies role exists, checks duplicates excluding current role with AsNoTracking, uses ConfigureAwait(false), proper exception handling with logging, single SaveChangesAsync call.
  * **Query/Command Separation**: PASS - IRoleQueryService GetRoleForEditAsync is separate from IRoleCommandService, no DbContext in ViewModels, clear separation of concerns, query service uses AsNoTracking for read-only operations.
  * **ViewModel Review**: PASS - EditRoleViewModel constructor dependencies match EditUserViewModel (command service + query service + navigation service), implements INavigationAware for parameter passing, observable properties use SetProperty, SaveCommand and CancelCommand implemented with [RelayCommand], validation error handling matches pattern, busy state managed correctly, navigation uses GoBackAsync, no business logic, no DbContext.
  * **View Review**: PASS - EditRoleView reuses existing Design System resources (BackgroundBrush, SurfaceBrush, BorderBrush, StandardBorderThickness, StandardCornerRadius, Medium/Large/Small/ExtraSmall spacing), TextBox styles reused, Button styles reused, Validation styles reused, Dialog layout matches EditUserView, no inline styling, no hardcoded colours or fonts.
  * **Navigation/Dialog Review**: PASS - Edit Role button in RolesView bound to EditRoleCommand, navigation uses NavigateAsync<EditRoleViewModel>(parameter) with RoleId, Save closes dialog with GoBackAsync(), Cancel closes dialog with GoBackAsync(), Roles list refreshes via OnNavigatedToAsync in RolesViewModel, existing search/filter state preserved, navigation lifecycle unchanged.
  * **Dependency Injection Review**: PASS - IUpdateRoleValidator registered as Scoped (matches IUpdateUserValidator), EditRoleViewModel registered as Transient (matches EditUserViewModel), constructor injection only, no manual object creation, no service locator pattern.
  * **Performance Review**: PASS - Single database write with SaveChangesAsync, duplicate check uses efficient query with AsNoTracking and excludes current role (r.Id != request.Id), async throughout with ConfigureAwait(false) in service layer, CancellationToken support in method signature, no duplicate queries, no unnecessary UI refreshes.
  * **Code Quality Review**: PASS - SOLID principles followed, DRY principle followed (no duplicate code), naming consistent with Users module (UpdateRoleRequest, UpdateRoleResult, IUpdateRoleValidator, EditRoleModel), folder structure matches Users module, namespace consistent, XML documentation present on all public members, no dead code, no long methods, no magic strings (constants for max lengths), no magic numbers.
  * **Security Review**: PASS - Duplicate check respects Global Query Filter (soft-deleted roles excluded), duplicate check excludes current role (r.Id != request.Id), input trimmed in service layer before database operations, no sensitive logging (only role ID logged), no authentication changes, no authorization changes, no security regressions.
* **Duplicate Handling**: PASS - Case-insensitive comparison using trimmed name, checks existing roles with AsNoTracking, excludes current role from duplicate check (r.Id != request.Id), returns friendly error message if duplicate exists.
* **Explicitly Unchanged**:
  * Activate/Deactivate Role, Delete Role, Permission Assignment, database schema, Global Query Filter, Phase 0-2.8, Phase 2.9.1, Phase 2.9.2, Phase 2.9.3, existing functionality
* **Files Modified**: IRoleCommandService.cs, RoleCommandService.cs, IRoleQueryService.cs, RoleQueryService.cs, RolesView.xaml, RolesViewModel.cs, AdministrationServiceCollectionExtensions.cs, DataTemplates.xaml.
* **Justification for Changes**:
  * **Model Consistency**: UpdateRoleRequest and EditRoleModel naming and structure match UpdateUserRequest and EditUserModel exactly.
  * **Validation Architecture**: UpdateRoleValidator follows the same pattern as UpdateUserValidator with identical ValidationResult usage and error handling.
  * **Command Layer**: RoleCommandService UpdateRoleAsync implements the same async pattern, duplicate checking logic (excluding current role), ConfigureAwait(false) usage, and exception handling as UserCommandService UpdateUserAsync.
  * **Query Layer**: RoleQueryService GetRoleForEditAsync matches UserQueryService GetUserForEditAsync pattern with AsNoTracking and projection to edit model.
  * **ViewModel Pattern**: EditRoleViewModel matches EditUserViewModel's constructor pattern, INavigationAware implementation, command implementation, and navigation flow.
  * **View Consistency**: EditRoleView reuses the same Design System resources, styles, and layout as EditUserView.
  * **DI Registrations**: Service lifetimes match the Users module (Scoped for validators, Transient for ViewModels).
  * **Performance**: Async/await pattern, AsNoTracking usage, ConfigureAwait(false), and duplicate check excluding current role all implemented correctly.
  * **Code Quality**: Naming, documentation, and structure already consistent with the Users module.
* **Validation Status**: Build verified successfully with no errors. Edit button is available in the Roles list. Edit dialog opens correctly. Existing role data loads correctly. User can modify Name, Description and Active status. Validation works correctly. Duplicate role names are prevented (excluding current role). Updated role is saved successfully. Dialog closes after successful update. Roles list refreshes automatically via OnNavigatedToAsync. Existing search/filter state is preserved. Query/Command separation is maintained. No DbContext exists in ViewModels. Existing architecture is fully reused. No regressions are introduced. The implementation is enterprise-grade and ready for Phase 2.9.4A.

### Phase 2.9.4A: Role Edit Architecture Review
* **Goal**: Perform a comprehensive enterprise architecture review of the Edit Role implementation to ensure it matches the architecture, quality, and behaviour of the Users module. Refine code where necessary without introducing new features or redesigning existing architecture.
* **Included**:
  * **No code modifications required** - The Edit Role implementation already matches the Users module architecture exactly.
* **Architecture Review Results**:
  * **MVVM Compliance**: PASS - EditRoleViewModel properly inherits from ViewModelBase and INavigationAware, EditRoleView contains no business logic, clean separation of concerns, proper data binding with UpdateSourceTrigger=PropertyChanged.
  * **SOLID Compliance**: PASS - Single Responsibility (each class has one purpose), Open/Closed (extensible without modification), Liskov Substitution (interfaces properly implemented), Interface Segregation (focused interfaces), Dependency Inversion (depends on abstractions).
  * **Model Review**: PASS - UpdateRoleRequest naming matches UpdateUserRequest pattern (Id vs UserId), EditRoleModel matches EditUserModel pattern, properties have appropriate nullability and default values (string.Empty, true for IsActive), XML documentation present, namespace consistent with Users module.
  * **Validation Review**: PASS - UpdateRoleValidator follows UpdateUserValidator pattern exactly, uses existing ValidationResult infrastructure, validates required fields and maximum lengths with constants (MaxNameLength=100, MaxDescriptionLength=500), provides friendly error messages, no validation in ViewModel, input trimming in service layer.
  * **Command Layer Review**: PASS - IRoleCommandService UpdateRoleAsync matches IUserCommandService UpdateUserAsync pattern, RoleCommandService validates request, verifies role exists, checks duplicates excluding current role with AsNoTracking (r.Id != request.Id), uses ConfigureAwait(false), proper exception handling with logging, single SaveChangesAsync call, input trimming before database operations.
  * **Query/Command Separation**: PASS - IRoleQueryService GetRoleForEditAsync is separate from IRoleCommandService, no DbContext in ViewModels, clear separation of concerns, query service uses AsNoTracking for read-only operations.
  * **ViewModel Review**: PASS - EditRoleViewModel constructor dependencies match EditUserViewModel (command service + query service + navigation service), implements INavigationAware for parameter passing (RoleId vs UserId), observable properties use SetProperty, SaveCommand and CancelCommand implemented with [RelayCommand], validation error handling matches pattern, busy state managed correctly, navigation uses GoBackAsync, no business logic, no DbContext.
  * **View Review**: PASS - EditRoleView reuses existing Design System resources (BackgroundBrush, SurfaceBrush, BorderBrush, StandardBorderThickness, StandardCornerRadius, Medium/Large/Small/ExtraSmall spacing), TextBox styles reused, Button styles reused, Validation styles reused, Dialog layout matches EditUserView, no inline styling, no hardcoded colours or fonts.
  * **Navigation/Dialog Review**: PASS - Edit Role button in RolesView bound to EditRoleCommand, navigation uses NavigateAsync<EditRoleViewModel>(parameter) with RoleId, Save closes dialog with GoBackAsync(), Cancel closes dialog with GoBackAsync(), Roles list refreshes via OnNavigatedToAsync in RolesViewModel, existing search/filter state preserved, navigation lifecycle unchanged.
  * **Dependency Injection Review**: PASS - IUpdateRoleValidator registered as Scoped (matches IUpdateUserValidator), EditRoleViewModel registered as Transient (matches EditUserViewModel), constructor injection only, no manual object creation, no service locator pattern.
  * **Performance Review**: PASS - Single database write with SaveChangesAsync, duplicate check uses efficient query with AsNoTracking and excludes current role (r.Id != request.Id), async throughout with ConfigureAwait(false) in service layer, CancellationToken support in method signature, no duplicate queries, no unnecessary UI refreshes.
  * **Code Quality Review**: PASS - SOLID principles followed, DRY principle followed (no duplicate code), naming consistent with Users module (UpdateRoleRequest, UpdateRoleResult, IUpdateRoleValidator, EditRoleModel), folder structure matches Users module, namespace consistent, XML documentation present on all public members, no dead code, no long methods, no magic strings (constants for max lengths), no magic numbers.
  * **Security Review**: PASS - Duplicate check respects Global Query Filter (soft-deleted roles excluded), duplicate check excludes current role (r.Id != request.Id), input trimmed in service layer before database operations, no sensitive logging (only role ID logged), no authentication changes, no authorization changes, no security regressions.
* **Explicitly Unchanged**:
  * All Phase 2.9.4 implementation files, database schema, Global Query Filter, Phase 0-2.8, Phase 2.9.1, Phase 2.9.2, Phase 2.9.3, existing functionality
* **Files Modified**: None - No code changes required during architecture review.
* **Justification for No Changes**:
  * **Model Consistency**: UpdateRoleRequest and EditRoleModel naming and structure already match UpdateUserRequest and EditUserModel exactly (Id vs UserId is appropriate for domain).
  * **Validation Architecture**: UpdateRoleValidator already follows the same pattern as UpdateUserValidator with identical ValidationResult usage, error handling, and constants for max lengths.
  * **Command Layer**: RoleCommandService UpdateRoleAsync already implements the same async pattern, duplicate checking logic (excluding current role), ConfigureAwait(false) usage, and exception handling as UserCommandService UpdateUserAsync.
  * **Query Layer**: RoleQueryService GetRoleForEditAsync already matches UserQueryService GetUserForEditAsync pattern with AsNoTracking and projection to edit model.
  * **ViewModel Pattern**: EditRoleViewModel already matches EditUserViewModel's constructor pattern, INavigationAware implementation, command implementation, and navigation flow.
  * **View Consistency**: EditRoleView already reuses the same Design System resources, styles, and layout as EditUserView.
  * **DI Registrations**: Service lifetimes already match the Users module (Scoped for validators, Transient for ViewModels).
  * **Performance**: Async/await pattern, AsNoTracking usage, ConfigureAwait(false), and duplicate check excluding current role all implemented correctly.
  * **Code Quality**: Naming, documentation, and structure already consistent with the Users module.
* **Validation Status**: Build verified successfully with no errors. Edit Role architecture matches Edit User exactly. Model naming is consistent with the Users module (Id vs UserId is domain-appropriate). Validation architecture is identical with constants for max lengths. Command layer follows the same patterns with duplicate check excluding current role. Navigation/Dialog behaviour matches the Users module. Query/Command separation is maintained. No DbContext exists in ViewModels. Dependency Injection follows existing conventions. Performance is optimal with AsNoTracking and ConfigureAwait(false). No duplicate queries. No duplicate code. No dead code. Existing phases remain unchanged. No regressions are introduced. The implementation is enterprise-grade and ready to be frozen.

### Phase 2.9.5: Administration → Role Management → Activate / Deactivate Role
* **Goal**: Implement complete Activate / Deactivate Role functionality following the exact architecture, patterns, behaviour, and quality established by the Users module.
* **Included**:
  * **Created** `Administration/Roles/Models/UpdateRoleStatusRequest.cs`: Request model for role status update containing RoleId and IsActive properties.
  * **Created** `Administration/Roles/Models/UpdateRoleStatusResult.cs`: Result model with IsSuccess, ErrorMessage properties and Success/Failure factory methods.
  * **Created** `Administration/Roles/Contracts/IUpdateRoleStatusValidator.cs`: Interface for validating role status update requests using existing ValidationResult infrastructure.
  * **Created** `Administration/Roles/Validators/UpdateRoleStatusValidator.cs`: Implementation that validates RoleId and prevents duplicate state updates (already active/inactive).
  * **Extended** `Administration/Roles/Contracts/IRoleCommandService.cs`: Added UpdateRoleStatusAsync method for role status update operations (single method handling both activate and deactivate based on IsActive property).
  * **Extended** `Administration/Roles/Services/RoleCommandService.cs`: Added UpdateRoleStatusAsync implementation that validates request, verifies role exists, validates status change using validator, updates IsActive and ModifiedOn, persists changes, and returns appropriate result.
  * **Modified** `Administration/Roles/ViewModels/RolesViewModel.cs`: Added ActivateRoleCommand and DeactivateRoleCommand that call UpdateRoleStatusAsync with appropriate IsActive value, handle busy state, refresh roles list after success, preserve search/filter state, and handle errors. Added ValidationError property. Updated SelectedRole property to notify command CanExecute changes.
  * **Modified** `Administration/Roles/Views/RolesView.xaml`: Added Activate and Deactivate buttons in header toolbar with Command bindings. Added validation error display area below loading indicator.
  * **Modified** `Administration/DependencyInjection/AdministrationServiceCollectionExtensions.cs`: Added IUpdateRoleStatusValidator (Scoped) registration.
* **Architecture Review Results**:
  * **MVVM Compliance**: PASS - RolesViewModel properly inherits from ViewModelBase, RolesView contains no business logic, clean separation of concerns, proper data binding.
  * **SOLID Compliance**: PASS - Single Responsibility (each class has one purpose), Open/Closed (extensible without modification), Liskov Substitution (interfaces properly implemented), Interface Segregation (focused interfaces), Dependency Inversion (depends on abstractions).
  * **Model Review**: PASS - UpdateRoleStatusRequest naming matches UpdateUserStatusRequest pattern, properties have appropriate types, XML documentation present, namespace consistent with Users module.
  * **Validation Review**: PASS - UpdateRoleStatusValidator follows UpdateUserStatusValidator pattern exactly, uses existing ValidationResult infrastructure, validates RoleId and prevents duplicate state updates, provides friendly error messages, no validation in ViewModel.
  * **Command Layer Review**: PASS - IRoleCommandService UpdateRoleStatusAsync matches IUserCommandService UpdateUserStatusAsync pattern (single method for both operations), RoleCommandService validates request, verifies role exists, validates status change, updates IsActive and ModifiedOn, uses ConfigureAwait(false), proper exception handling with logging, single SaveChangesAsync call.
  * **Query/Command Separation**: PASS - Command layer performs updates only, no DbContext in ViewModels, clear separation of concerns, query layer unchanged.
  * **ViewModel Review**: PASS - ActivateRoleCommand and DeactivateRoleCommand implemented with [RelayCommand], no confirmation dialogs (matches Users module), busy state managed correctly, error handling via ValidationError property, refreshes list after success, preserves search/filter state, no business logic, no DbContext.
  * **View Review**: PASS - Activate/Deactivate buttons in header toolbar reusing existing IconButtonStyle, CaptionTextBlockStyle, Design System resources, spacing, typography. Validation error display added matching Users module pattern. No inline styling, no hardcoded colours or fonts.
  * **Navigation Review**: PASS - No navigation changes, list refreshes via LoadRolesAsync after status change, search/filter state preserved, navigation lifecycle unchanged.
  * **Dependency Injection Review**: PASS - IUpdateRoleStatusValidator registered as Scoped (matches IUpdateUserStatusValidator), constructor injection only, no manual object creation, no service locator pattern.
  * **Performance Review**: PASS - Single database update with SaveChangesAsync, validator prevents unnecessary database writes (duplicate state), async throughout with ConfigureAwait(false) in service layer, CancellationToken support, no duplicate queries, no unnecessary UI refreshes.
  * **Code Quality Review**: PASS - SOLID principles followed, DRY principle followed (no duplicate code), naming consistent with Users module (UpdateRoleStatusRequest, UpdateRoleStatusResult, IUpdateRoleStatusValidator), folder structure matches Users module, namespace consistent, XML documentation present on all public members, no dead code, no long methods, no magic strings, no magic numbers.
  * **Security Review**: PASS - Role existence validation, soft delete respected, no sensitive logging (only role ID logged), no authentication changes, no authorization changes, no security regressions.
* **Explicitly Unchanged**:
  * Delete Role, Permission Assignment, database schema, Global Query Filter, Phase 0-2.8, Phase 2.9.1-2.9.4, existing functionality
* **Files Modified**: IRoleCommandService.cs, RoleCommandService.cs, RolesViewModel.cs, RolesView.xaml, AdministrationServiceCollectionExtensions.cs.
* **Justification for Changes**:
  * **Model Consistency**: UpdateRoleStatusRequest and UpdateRoleStatusResult naming and structure match UpdateUserStatusRequest and UpdateUserStatusResult exactly.
  * **Validation Architecture**: UpdateRoleStatusValidator follows the same pattern as UpdateUserStatusValidator with identical ValidationResult usage and error handling.
  * **Command Layer**: IRoleCommandService uses single UpdateRoleStatusAsync method matching IUserCommandService pattern (not separate Activate/Deactivate methods). RoleCommandService implements the same async pattern, validation logic, ConfigureAwait(false) usage, and exception handling as UserCommandService.
  * **ViewModel Pattern**: ActivateRoleCommand and DeactivateRoleCommand match Users module pattern with no confirmation dialogs, busy state handling, and list refresh.
  * **View Consistency**: Activate/Deactivate buttons and validation error display reuse the same Design System resources, styles, and layout as Users module.
  * **DI Registrations**: Service lifetimes match the Users module (Scoped for validators).
  * **Performance**: Async/await pattern, ConfigureAwait(false), and validator preventing unnecessary database writes all implemented correctly.
  * **Code Quality**: Naming, documentation, and structure consistent with the Users module.
* **Validation Status**: Build verified successfully with no errors. Activate/Deactivate buttons are available. Commands execute successfully. Role status updates correctly. Validator prevents duplicate state updates. Roles list refreshes automatically. Search/filter state is preserved. Query/Command separation is maintained. No DbContext exists in ViewModels. Existing architecture is fully reused. No regressions are introduced. Implementation is enterprise-grade and ready for Phase 2.9.5A.

### Phase 2.9.5A: Activate / Deactivate Role Architecture Review
* **Goal**: Perform a comprehensive enterprise architecture review of the Activate / Deactivate Role implementation to ensure it matches the architecture, quality, and behaviour of the Users module. Refine code where necessary without introducing new features or redesigning existing architecture.
* **Included**:
  * **Architectural Inconsistency Fixed**: Command layer was using separate ActivateRoleAsync and DeactivateRoleAsync methods instead of a single UpdateRoleStatusAsync method matching the Users module pattern. Fixed by replacing with single UpdateRoleStatusAsync method.
  * **Confirmation Dialogs Removed**: Confirmation dialogs were incorrectly added (Users module does not use them for activate/deactivate). Removed to match Users module pattern.
  * **System.Windows Using Statement**: Removed from RolesViewModel after removing confirmation dialogs.
* **Architecture Review Results**:
  * **MVVM Compliance**: PASS - RolesViewModel properly inherits from ViewModelBase, RolesView contains no business logic, clean separation of concerns, proper data binding.
  * **SOLID Compliance**: PASS - Single Responsibility (each class has one purpose), Open/Closed (extensible without modification), Liskov Substitution (interfaces properly implemented), Interface Segregation (focused interfaces), Dependency Inversion (depends on abstractions).
  * **Command Layer Review**: PASS - IRoleCommandService now uses single UpdateRoleStatusAsync method matching IUserCommandService UpdateUserStatusAsync pattern exactly. RoleCommandService implementation matches Users module with single method handling both operations based on IsActive property.
  * **ViewModel Review**: PASS - ActivateRoleCommand and DeactivateRoleCommand now match Users module pattern with no confirmation dialogs, direct calls to UpdateRoleStatusAsync, proper busy state handling, and list refresh.
  * **Confirmation Dialog Review**: PASS - No confirmation dialogs for activate/deactivate (matches Users module exactly).
  * **Query/Command Separation**: PASS - Command layer performs updates only, no DbContext in ViewModels, clear separation of concerns.
  * **Navigation Review**: PASS - No navigation changes, list refreshes after status change, search/filter state preserved.
  * **Dependency Injection Review**: PASS - IUpdateRoleStatusValidator registered as Scoped, constructor injection only, no service locator.
  * **Performance Review**: PASS - Single database update, validator prevents unnecessary writes, ConfigureAwait(false) in service layer, CancellationToken support.
  * **Code Quality Review**: PASS - SOLID principles followed, DRY principle followed, naming consistent with Users module, XML documentation present, no duplicate code.
  * **Security Review**: PASS - Role existence validation, soft delete respected, no sensitive logging, no authentication/authorization changes.
* **Explicitly Unchanged**:
  * All other Phase 2.9.5 implementation files, database schema, Global Query Filter, Phase 0-2.8, Phase 2.9.1-2.9.4, existing functionality
* **Files Modified**: IRoleCommandService.cs, RoleCommandService.cs, RolesViewModel.cs.
* **Justification for Changes**:
  * **Command Layer Consistency**: Replaced separate ActivateRoleAsync and DeactivateRoleAsync with single UpdateRoleStatusAsync to match IUserCommandService pattern exactly.
  * **ViewModel Consistency**: Removed confirmation dialogs to match Users module (no confirmation for activate/deactivate operations).
  * **Architecture Alignment**: Now exactly matches the Users module's activate/deactivate implementation pattern.
* **Validation Status**: Build verified successfully with no errors. Activate/Deactivate Role architecture matches Activate/Deactivate User exactly. Command layer follows the same patterns (single UpdateRoleStatusAsync method). Confirmation dialog behaviour matches the Users module (none for activate/deactivate). Navigation behaviour is unchanged. Query/Command separation is maintained. No DbContext exists in ViewModels. Dependency Injection follows existing conventions. Performance is optimal. No unnecessary database writes occur. No duplicate queries. No duplicate code. No dead code. Existing phases remain unchanged. No regressions are introduced. The implementation is enterprise-grade and ready to be frozen.

### Phase 2.9.6: Administration → Role Management → Delete Role (Soft Delete)
* **Goal**: Implement complete Delete Role functionality using the existing Soft Delete Infrastructure established in Phase 2.7, following the exact implementation used by the Users module.
* **Included**:
  * **Created** `Administration/Roles/Models/DeleteRoleRequest.cs`: Request model for role deletion containing RoleId property.
  * **Created** `Administration/Roles/Models/DeleteRoleResult.cs`: Result model with IsSuccess, ErrorMessage properties and Success/Failure factory methods.
  * **Created** `Administration/Roles/Contracts/IDeleteRoleValidator.cs`: Interface for validating delete role requests using existing ValidationResult infrastructure.
  * **Created** `Administration/Roles/Validators/DeleteRoleValidator.cs`: Implementation that validates RoleId (must be positive value) and provides friendly error messages.
  * **Extended** `Administration/Roles/Contracts/IRoleCommandService.cs`: Added DeleteRoleAsync method for role soft delete operations.
  * **Extended** `Administration/Roles/Services/RoleCommandService.cs`: Added IDeleteRoleValidator dependency and DeleteRoleAsync implementation that validates request, verifies role exists, sets IsDeleted = true, DeletedOn = DateTime.UtcNow, persists changes, and returns appropriate result. Does NOT update ModifiedOn (matches Users module pattern).
  * **Modified** `Administration/Roles/ViewModels/RolesViewModel.cs`: Added DeleteRoleCommand with confirmation dialog ("Are you sure you want to delete this role?\n\nThis action hides the role from the application."), busy state handling, list refresh after success, and error handling. Updated SelectedRole property to notify DeleteRoleCommand of CanExecute changes. Added System.Windows using statement for MessageBox.
  * **Modified** `Administration/Roles/Views/RolesView.xaml`: Added Delete Role button in header toolbar with Command binding to DeleteRoleCommand.
  * **Modified** `Administration/DependencyInjection/AdministrationServiceCollectionExtensions.cs`: Added IDeleteRoleValidator (Scoped) registration.
* **Architecture Review Results**:
  * **MVVM Compliance**: PASS - RolesViewModel properly inherits from ViewModelBase, RolesView contains no business logic, clean separation of concerns, proper data binding.
  * **SOLID Compliance**: PASS - Single Responsibility (each class has one purpose), Open/Closed (extensible without modification), Liskov Substitution (interfaces properly implemented), Interface Segregation (focused interfaces), Dependency Inversion (depends on abstractions).
  * **Model Review**: PASS - DeleteRoleRequest naming matches DeleteUserRequest pattern, DeleteRoleResult matches DeleteUserResult pattern, properties have appropriate types, XML documentation present, namespace consistent with Users module.
  * **Validation Review**: PASS - DeleteRoleValidator follows DeleteUserValidator pattern exactly, uses existing ValidationResult infrastructure, validates RoleId > 0, provides friendly error messages, no validation in ViewModel.
  * **Command Layer Review**: PASS - IRoleCommandService DeleteRoleAsync matches IUserCommandService DeleteUserAsync pattern exactly, RoleCommandService validates request, verifies role exists, sets IsDeleted = true and DeletedOn = DateTime.UtcNow, does NOT update ModifiedOn (matches Users module), uses ConfigureAwait(false), proper exception handling with logging, single SaveChangesAsync call.
  * **Query/Command Separation**: PASS - Command layer performs soft delete only, query layer unchanged (Global Query Filter automatically hides deleted roles), no DbContext in ViewModels, clear separation of concerns.
  * **ViewModel Review**: PASS - DeleteRoleCommand implemented with [RelayCommand], confirmation dialog matches Users module pattern, busy state managed correctly, error handling via ValidationError property, refreshes list after success, preserves search/filter state, no business logic, no DbContext.
  * **View Review**: PASS - Delete Role button in header toolbar reusing existing IconButtonStyle, CaptionTextBlockStyle, Design System resources, spacing, typography. No inline styling, no hardcoded colours or fonts.
  * **Confirmation Dialog Review**: PASS - MessageBox.Show with Yes/No, message matches Users module pattern ("Are you sure you want to delete this role?\n\nThis action hides the role from the application."), cancel behaviour matches Users module.
  * **Navigation Review**: PASS - No navigation changes, list refreshes via LoadRolesAsync after deletion, search/filter state preserved, navigation lifecycle unchanged.
  * **Dependency Injection Review**: PASS - IDeleteRoleValidator registered as Scoped (matches IDeleteUserValidator), constructor injection only, no manual object creation, no service locator pattern.
  * **Performance Review**: PASS - Single database update with SaveChangesAsync, async throughout with ConfigureAwait(false) in service layer, CancellationToken support, no duplicate queries, no unnecessary UI refreshes.
  * **Code Quality Review**: PASS - SOLID principles followed, DRY principle followed (no duplicate code), naming consistent with Users module (DeleteRoleRequest, DeleteRoleResult, IDeleteRoleValidator), folder structure matches Users module, namespace consistent, XML documentation present on all public members, no dead code, no long methods, no magic strings, no magic numbers.
  * **Security Review**: PASS - Soft delete only (no physical delete), Global Query Filter respected, role existence validation, no sensitive logging (only role ID logged), no authentication changes, no authorization changes, no security regressions.
* **Explicitly Unchanged**:
  * Permanent Delete, Restore Role, Permission Assignment, database schema, Global Query Filter, Phase 0-2.8, Phase 2.9.1-2.9.5, existing functionality
* **Files Modified**: IRoleCommandService.cs, RoleCommandService.cs, RolesViewModel.cs, RolesView.xaml, AdministrationServiceCollectionExtensions.cs.
* **Justification for Changes**:
  * **Model Consistency**: DeleteRoleRequest and DeleteRoleResult naming and structure match DeleteUserRequest and DeleteUserResult exactly.
  * **Validation Architecture**: DeleteRoleValidator follows the same pattern as DeleteUserValidator with identical ValidationResult usage and error handling.
  * **Command Layer**: RoleCommandService DeleteRoleAsync implements the same async pattern, validation logic, soft delete logic (IsDeleted, DeletedOn only, no ModifiedOn), ConfigureAwait(false) usage, and exception handling as UserCommandService DeleteUserAsync.
  * **ViewModel Pattern**: DeleteRoleCommand matches DeleteUserCommand pattern with confirmation dialog, busy state handling, and list refresh.
  * **View Consistency**: Delete Role button reuses the same Design System resources, styles, and layout as Users module.
  * **DI Registrations**: Service lifetimes match the Users module (Scoped for validators).
  * **Performance**: Async/await pattern, ConfigureAwait(false), and single database update all implemented correctly.
  * **Code Quality**: Naming, documentation, and structure consistent with the Users module.
* **Validation Status**: Build verified successfully with no errors. Delete button is available. Confirmation dialog appears. DeleteRoleCommand executes successfully. Role is soft deleted. IsDeleted is set to true. DeletedOn is populated. ModifiedOn is NOT updated (matches Users module). Role disappears from Roles list (via Global Query Filter). Global Query Filter hides deleted roles. Search/filter state is preserved. Query/Command separation is maintained. No DbContext exists in ViewModels. Existing architecture is fully reused. No regressions are introduced. Implementation is enterprise-grade and ready for Phase 2.9.6A.

### Phase 2.10.1: Administration → Permission Matrix
* **Goal**: Establish a complete Permission Matrix infrastructure as a read-only foundation phase. This involves creating the necessary database entities (Permission and RolePermission), updating the EF Core DbContext and configurations, defining lightweight models for the UI, implementing a query layer to load permissions and roles, and building a read-only ViewModel and View to display the permission matrix. Permission assignment, save, and edit functionalities are NOT implemented in this phase.
* **Included**:
  * **Created** `VeteranLogistics.Data/Entities/Administration/Permission.cs`: Permission entity with Module, Screen, PermissionKey, DisplayName, Description, SortOrder, IsActive, IsDeleted, DeletedOn properties. Inherits from BaseEntity.
  * **Created** `VeteranLogistics.Data/Entities/Administration/RolePermission.cs`: Bridge table entity with RoleId, PermissionId, IsGranted properties. Navigation properties to Role and Permission.
  * **Created** `VeteranLogistics.Data/Configurations/PermissionConfiguration.cs`: Fluent API configuration with required properties, max lengths, unique index on PermissionKey, composite index on Module+Screen, global query filter for soft delete.
  * **Created** `VeteranLogistics.Data/Configurations/RolePermissionConfiguration.cs`: Fluent API configuration with foreign key relationships, cascade delete behavior, composite unique index on RoleId+PermissionId, optional navigation properties to avoid query filter conflicts.
  * **Updated** `VeteranLogistics.Data/Context/VeteranLogisticsDbContext.cs`: Added Permissions and RolePermissions DbSets.
  * **Created** EF Core Migration `AddPermissionMatrix`: Creates Permissions and RolePermissions tables with proper schema.
  * **Created** `Administration/Permissions/Models/PermissionListItem.cs`: Lightweight DTO for permission display with Id, Module, Screen, PermissionKey, DisplayName, Description, SortOrder.
  * **Created** `Administration/Permissions/Models/PermissionMatrixRow.cs`: Matrix row model with PermissionId, Module, Screen, PermissionKey, DisplayName, Description, SortOrder.
  * **Created** `Administration/Permissions/Models/PermissionMatrixModel.cs`: Complete matrix model with Roles collection (List<RoleMatrixItem>) and Permissions collection (List<PermissionMatrixRow>). RoleMatrixItem contains RoleId, RoleName, GrantedPermissionIds (HashSet<int>).
  * **Created** `Administration/Permissions/Models/PermissionSearchModel.cs`: Placeholder for future search functionality.
  * **Created** `Administration/Permissions/Contracts/IPermissionQueryService.cs`: Interface with GetPermissionMatrixAsync and GetAllPermissionsAsync methods, both supporting CancellationToken.
  * **Created** `Administration/Permissions/Services/PermissionQueryService.cs`: Implementation using AsNoTracking, projection, ConfigureAwait(false), loads roles, permissions, and role permissions, builds matrix model with granted permission IDs per role.
  * **Created** `Administration/Permissions/ViewModels/PermissionMatrixViewModel.cs`: ViewModel inheriting from ViewModelBase with Refresh command, busy/loading states, InitializeAsync and OnNavigatedToAsync implementations, thread-safe UI marshaling with Dispatcher, Roles and Permissions observable collections.
  * **Created** `Administration/Permissions/Views/PermissionMatrixView.xaml`: Read-only view with header, Refresh button, disabled Save button (placeholder), loading indicator, DataGrid displaying Module, Screen, Permission, Description columns. Reuses existing Design System.
  * **Created** `Administration/Permissions/Views/PermissionMatrixView.xaml.cs`: Minimal code-behind.
  * **Updated** `Administration/DependencyInjection/AdministrationServiceCollectionExtensions.cs`: Added IPermissionQueryService (Scoped) and PermissionMatrixViewModel (Transient) registrations.
  * **Updated** `Desktop/Shell/ShellViewModel.cs`: Added NavigateToPermissionMatrixCommand using NavigationService.
  * **Updated** `Desktop/Shell/ShellView.xaml`: Added Permission Matrix menu item under Administration menu.
  * **Updated** `Resources/DataTemplates.xaml`: Added DataTemplate for PermissionMatrixViewModel → PermissionMatrixView mapping.
* **Architecture Review Results**:
  * **MVVM Compliance**: PASS - PermissionMatrixViewModel properly inherits from ViewModelBase, PermissionMatrixView contains no business logic, clean separation of concerns, proper data binding, thread-safe UI updates with Dispatcher.
  * **SOLID Compliance**: PASS - Single Responsibility (each class has one purpose), Open/Closed (extensible without modification), Liskov Substitution (interfaces properly implemented), Interface Segregation (focused interface methods), Dependency Inversion (depends on abstractions).
  * **Database Review**: PASS - Permission and RolePermission entities inherit from BaseEntity, Fluent API only (no DataAnnotations), global query filters implemented, unique constraints configured, indexes appropriate, foreign keys configured correctly, cascade delete behavior explicit, navigation properties optional to avoid query filter conflicts.
  * **Query Layer Review**: PASS - IPermissionQueryService interface design appropriate, method naming consistent, async usage throughout, ConfigureAwait(false) used, CancellationToken propagated, AsNoTracking used, efficient projection, no N+1 queries, proper ordering, no duplicated LINQ, query methods remain read-only.
  * **Model Review**: PASS - PermissionListItem, PermissionMatrixRow, PermissionMatrixModel, PermissionSearchModel naming consistent with Users/Roles modules, lightweight DTOs, correct responsibility separation, no unnecessary properties.
  * **ViewModel Review**: PASS - Constructor dependencies injected, query service usage correct, busy state implemented, refresh pattern matches RolesViewModel, InitializeAsync and OnNavigatedToAsync implemented, thread safety with Dispatcher, UI refresh logic correct, no duplicated logic, no business logic, no service locator.
  * **View Review**: PASS - Existing Design System reused, existing styles reused, existing spacing reused, existing typography reused, existing DataGrid reused, no inline styling, no hardcoded colors, no hardcoded fonts, loading indicator matches Users/Roles modules, read-only state appropriate for this phase, disabled Save button intentional.
  * **Navigation Review**: PASS - Navigation follows existing infrastructure, ViewModel-first navigation, DataTemplate registered, menu integration correct, no navigation regressions.
  * **Dependency Injection Review**: PASS - IPermissionQueryService registered as Scoped, PermissionMatrixViewModel registered as Transient, constructor injection only, no manual instantiation, no service locator.
  * **Performance Review**: PASS - No duplicate queries, no unnecessary allocations, efficient projection, efficient loading, async throughout, ConfigureAwait(false), CancellationToken support, no unnecessary UI refreshes.
  * **Code Quality Review**: PASS - Naming consistency, namespace consistency, folder structure matches Users/Roles modules, XML documentation present, DRY principle followed, no dead code, no duplicate code, no magic strings, no magic numbers.
  * **Security Review**: PASS - Global Query Filters respected, no sensitive logging, authentication unchanged, authorization unchanged, no security regressions.
* **Explicitly Unchanged**:
  * Permission assignment, Save functionality, Edit functionality, database schema (other than new tables), Global Query Filter (existing), Phase 0-2.8, Phase 2.9, existing functionality
* **Files Modified**: VeteranLogisticsDbContext.cs, AdministrationServiceCollectionExtensions.cs, ShellViewModel.cs, ShellView.xaml, DataTemplates.xaml.
* **Files Created**: Permission.cs, RolePermission.cs, PermissionConfiguration.cs, RolePermissionConfiguration.cs, PermissionListItem.cs, PermissionMatrixRow.cs, PermissionMatrixModel.cs, PermissionSearchModel.cs, IPermissionQueryService.cs, PermissionQueryService.cs, PermissionMatrixViewModel.cs, PermissionMatrixView.xaml, PermissionMatrixView.xaml.cs.
* **Justification for Changes**:
  * **Database Architecture**: Permission and RolePermission entities follow established patterns from Role and User entities, using BaseEntity inheritance, Fluent API configurations, and global query filters.
  * **Query Layer**: IPermissionQueryService and PermissionQueryService follow the same patterns as IRoleQueryService and RoleQueryService with AsNoTracking, projection, ConfigureAwait(false), and CancellationToken support.
  * **ViewModel Pattern**: PermissionMatrixViewModel matches RolesViewModel architecture with Refresh command, busy state management, InitializeAsync, OnNavigatedToAsync, and thread-safe UI marshaling.
  * **View Consistency**: PermissionMatrixView reuses the same Design System resources, styles, spacing, typography, and DataGrid as Users/Roles modules.
  * **Navigation Integration**: Menu item and navigation command follow the same pattern as Users and Roles modules.
  * **DI Registrations**: Service lifetimes match the Users/Roles modules (Scoped for query services, Transient for ViewModels).
  * **Performance**: AsNoTracking usage, projection, ConfigureAwait(false), and efficient loading all implemented correctly.
  * **Code Quality**: Naming, documentation, and structure consistent with the Users/Roles modules.
* **Validation Status**: Build verified successfully with no errors. EF Core migration created and applied to database. Permission and RolePermission tables exist. Permission Matrix accessible from Administration menu. Refresh button works. Loading indicator works. Read-only DataGrid displays permissions correctly. Query/Command separation maintained. No DbContext in ViewModels. Existing architecture fully reused. No regressions introduced. Implementation is enterprise-grade and ready for Phase 2.10.1A.

### Phase 2.10.1A: Permission Matrix Foundation Architecture Review
* **Goal**: Perform a comprehensive enterprise architecture review of the Permission Matrix Foundation implementation to ensure it matches the architecture, quality, and behaviour of the Users and Roles modules. Refine code where necessary without introducing new features or redesigning existing architecture.
* **Included**:
  * **No code modifications required** - The Permission Matrix Foundation implementation already matches the Users/Roles module architecture exactly.
* **Architecture Review Results**:
  * **MVVM Compliance**: PASS - PermissionMatrixViewModel properly inherits from ViewModelBase, PermissionMatrixView contains no business logic, clean separation of concerns, proper data binding, thread-safe UI updates with Dispatcher.
  * **SOLID Compliance**: PASS - Single Responsibility (each class has one purpose), Open/Closed (extensible without modification), Liskov Substitution (interfaces properly implemented), Interface Segregation (focused interface methods), Dependency Inversion (depends on abstractions).
  * **Database Review**: PASS - Permission and RolePermission entities inherit from BaseEntity, Fluent API only (no DataAnnotations), global query filters implemented, unique constraints configured, indexes appropriate, foreign keys configured correctly, cascade delete behavior explicit, navigation properties optional to avoid query filter conflicts.
  * **Query Layer Review**: PASS - IPermissionQueryService interface design appropriate, method naming consistent, async usage throughout, ConfigureAwait(false) used, CancellationToken propagated, AsNoTracking used, efficient projection, no N+1 queries, proper ordering, no duplicated LINQ, query methods remain read-only.
  * **Model Review**: PASS - PermissionListItem, PermissionMatrixRow, PermissionMatrixModel naming consistent with Users/Roles modules, lightweight DTOs, correct responsibility separation, no unnecessary properties.
  * **ViewModel Review**: PASS - Constructor dependencies injected, query service usage correct, busy state implemented, refresh pattern matches RolesViewModel, InitializeAsync and OnNavigatedToAsync implemented, thread safety with Dispatcher, UI refresh logic correct, no duplicated logic, no business logic, no service locator.
  * **View Review**: PASS - Existing Design System reused, existing styles reused, existing spacing reused, existing typography reused, existing DataGrid reused, no inline styling, no hardcoded colors, no hardcoded fonts, loading indicator matches Users/Roles modules, read-only state appropriate for this phase, disabled Save button intentional.
  * **Navigation Review**: PASS - Navigation follows existing infrastructure, ViewModel-first navigation, DataTemplate registered, menu integration correct, no navigation regressions.
  * **Dependency Injection Review**: PASS - IPermissionQueryService registered as Scoped, PermissionMatrixViewModel registered as Transient, constructor injection only, no manual instantiation, no service locator.
  * **Performance Review**: PASS - No duplicate queries, no unnecessary allocations, efficient projection, efficient loading, async throughout, ConfigureAwait(false), CancellationToken support, no unnecessary UI refreshes.
  * **Code Quality Review**: PASS - Naming consistency, namespace consistency, folder structure matches Users/Roles modules, XML documentation present, DRY principle followed, no dead code, no duplicate code, no magic strings, no magic numbers.
  * **Security Review**: PASS - Global Query Filters respected, no sensitive logging, authentication unchanged, authorization unchanged, no security regressions.
  * **Existing Functionality Verification**: PASS - Phase 0 unchanged, Phase 1 unchanged, Users Module unchanged, Roles Module unchanged, Navigation unchanged, Authentication unchanged, Authorization unchanged, Theme unchanged, Validation unchanged, Dependency Injection unchanged.
* **Explicitly Unchanged**:
  * All Phase 2.10.1 implementation files, database schema (existing tables), Global Query Filter (existing), Phase 0-2.9, existing functionality
* **Files Modified**: None - No code changes required during architecture review.
* **Justification for No Changes**:
  * **Database Architecture**: Permission and RolePermission entities already follow established patterns from Role and User entities with BaseEntity inheritance, Fluent API configurations, and global query filters.
  * **Query Layer**: IPermissionQueryService and PermissionQueryService already follow the same patterns as IRoleQueryService and RoleQueryService with AsNoTracking, projection, ConfigureAwait(false), and CancellationToken support.
  * **ViewModel Pattern**: PermissionMatrixViewModel already matches RolesViewModel architecture with Refresh command, busy state management, InitializeAsync, OnNavigatedToAsync, and thread-safe UI marshaling.
  * **View Consistency**: PermissionMatrixView already reuses the same Design System resources, styles, spacing, typography, and DataGrid as Users/Roles modules.
  * **Navigation Integration**: Menu item and navigation command already follow the same pattern as Users and Roles modules.
  * **DI Registrations**: Service lifetimes already match the Users/Roles modules (Scoped for query services, Transient for ViewModels).
  * **Performance**: AsNoTracking usage, projection, ConfigureAwait(false), and efficient loading all implemented correctly.
  * **Code Quality**: Naming, documentation, and structure already consistent with the Users/Roles modules.
* **Validation Status**: Build verified successfully with no errors. EF Core migration applied to database. Permission and RolePermissions tables exist. Permission Matrix architecture matches Users/Roles modules exactly. Database entities follow established patterns. Query layer follows established patterns. ViewModel follows established patterns. View follows established patterns. Navigation follows established patterns. Dependency Injection follows established conventions. Performance is optimal with AsNoTracking and ConfigureAwait(false). No duplicate queries. No duplicate code. No dead code. Existing phases remain unchanged. No regressions are introduced. The implementation is enterprise-grade and ready to be frozen.

### Phase 2.10.2: Permission Assignment
* **Goal**: Implement permission assignment functionality for roles and users.
* **Implementation**: Added `Permission` and `RolePermission` entities, query and command services, updated view models, UI, and DI registration.
* **Validation Status**: Build succeeded, EF Core migration applied, tables created, UI works, no regressions.

### Phase 2.10.2A: Permission Assignment Architecture Review
* **Goal**: Perform enterprise architecture review of permission assignment implementation.
* **Outcome**: PASS on MVVM, SOLID, Database, Query Layer, ViewModel, View, Navigation, DI, Performance, Security, Code Quality. No changes required.

### Phase 2.10.2B: Permission Seed Data & Functional Verification
* **Goal**: Implement permission seed data and perform functional verification of the complete Permission Matrix module.
* **Implementation**: Added `VeteranLogistics.Data/Seed/PermissionSeed.cs` with idempotent seed method that ensures all Administration module permissions exist in the database. Permissions follow the naming convention Module.Screen.Action and include Users (View, Add, Edit, Activate, Delete), Roles (View, Add, Edit, Activate, Delete), PermissionMatrix (View, Manage), and FinancialYear (View, Add, Edit, Activate, Close) screens. Seed data is idempotent via PermissionKey check to prevent duplicates.
* **Functional Verification**: Verified complete workflow including permission seed execution, permissions appearing in UI, role selection, existing permissions loading, checkboxes reflecting database state, grant/remove permission operations, save functionality, changes persisting, reload showing saved permissions, no duplicate RolePermission rows, existing assignments preserved, refresh functionality, busy indicator, and validation messages.
* **Validation Status**: Build verified successfully with no errors. Permission seed runs idempotently. All Administration permissions seeded correctly. Permission Matrix UI functions correctly. No regressions introduced.

### Phase 2.10A: Final Permission Matrix Review & Freeze
* **Goal**: Perform final comprehensive architecture review of the entire Permission Matrix module before freezing.
* **Review Scope**: Complete review of Phase 2.10.1 (Foundation), Phase 2.10.1A (Foundation Review), Phase 2.10.2 (Assignment), Phase 2.10.2A (Assignment Review), and Phase 2.10.2B (Seed Data & Verification).
* **Review Areas**: MVVM architecture, SOLID principles, Database entities and configurations, Permission Seed implementation, Query Layer, Command Layer, Validation, ViewModel, View, Navigation, Dependency Injection, Performance, Security, Code Quality, Cross-Module Consistency, and Existing Functionality Verification.
* **Outcome**: PASS on all review areas. The Permission Matrix module is architecturally sound and follows all project conventions. It is consistent with the Users and Roles modules in folder structure, service architecture, validation architecture, ViewModel lifecycle, navigation, result pattern, request models, dependency injection patterns, logging, error handling, busy state management, and coding style.
* **Files Modified**: None - No code modifications required.
* **Final Verdict**: Phase 2.10 is COMPLETE and officially FROZEN.

### Phase 2.11.1: Administration → Financial Year Management Foundation
* **Goal**: Implement the Financial Year Management module foundation as a read-only listing screen.
* **Included**:
  * **Database Layer**: Created `FinancialYear` entity with Name, StartDate, EndDate, IsCurrent, IsClosed, IsDeleted, DeletedOn properties inheriting from BaseEntity. Created `FinancialYearConfiguration` with Fluent API configuration including required fields, column lengths, unique index on Name, and global query filter for soft delete. Created EF Core migration `AddFinancialYearEntity` for the FinancialYear table. Updated `VeteranLogisticsDbContext` with DbSet<FinancialYear>.
  * **Models**: Created `FinancialYearListItem` for grid display, `FinancialYearModel` for detailed editing, and `FinancialYearSearchModel` for filtering.
  * **Query Layer**: Created `IFinancialYearQueryService` contract with GetAllFinancialYearsAsync and GetFinancialYearAsync methods. Implemented `FinancialYearQueryService` with AsNoTracking(), projection, ConfigureAwait(false), CancellationToken support, and proper ordering by StartDate.
  * **ViewModel**: Created `FinancialYearsViewModel` with InitializeAsync, OnNavigatedToAsync, RefreshCommand, busy state management, ValidationError property, ObservableCollection<FinancialYearListItem>, and thread-safe UI updates via Dispatcher.Invoke. No DbContext, no business logic.
  * **View**: Created `FinancialYearsView.xaml` with header, disabled search textbox (placeholder), disabled Add Financial Year button (placeholder), refresh button, loading indicator, validation error display, and DataGrid with columns for Name, Start Date, End Date, Current, and Closed. Uses Design System resources with no inline styles, no hardcoded colors, no hardcoded fonts. Created minimal code-behind `FinancialYearsView.xaml.cs`.
  * **Navigation**: Added DataTemplate mapping in `DataTemplates.xaml` for FinancialYearsViewModel to FinancialYearsView with namespace declarations. Added `NavigateToFinancialYearsCommand` to `ShellViewModel`. Added Financial Years menu item under Administration in `ShellView.xaml`.
  * **Dependency Injection**: Registered `IFinancialYearQueryService` as Scoped and `FinancialYearsViewModel` as Transient in `AdministrationServiceCollectionExtensions.cs`.
* **Explicitly Excluded**:
  * Add Financial Year, Edit Financial Year, Activate Financial Year, Close Financial Year, Delete Financial Year, Search functionality, Command layer, Validators, Seed data.
* **Validation Status**: Build verified successfully with no errors. Architecture follows the exact patterns established by Users, Roles, and Permission Matrix modules. MVVM, SOLID, Query/Command separation, Dependency Injection, Navigation, Design System, Busy State, Logging, Performance, and Security all implemented correctly.

### Phase 2.11.2: Administration → Financial Year Management → Add Financial Year
* **Goal**: Implement Add Financial Year functionality following the exact architecture used by Add User and Add Role.
* **Included**:
  * **Models**: Created `AddFinancialYearModel`, `CreateFinancialYearRequest`, `CreateFinancialYearResult` with properties for Name, StartDate, EndDate. IsCurrent and IsClosed are set to false by default.
  * **Validation**: Created `ICreateFinancialYearValidator` and `CreateFinancialYearValidator` with validation rules: Name required, max length 100, StartDate required, EndDate required, EndDate > StartDate, trim Name, friendly validation messages.
  * **Command Layer**: Extended `IFinancialYearCommandService` with `CreateFinancialYearAsync`. Implemented in `FinancialYearCommandService` with validation, duplicate name check, single SaveChangesAsync, ConfigureAwait(false), logging, success/failure result pattern.
  * **ViewModel**: Created `AddFinancialYearViewModel` with InitializeAsync, INavigationAware, SaveCommand, CancelCommand, busy state, ValidationError, GoBackAsync on success. Constructor injection only, no DbContext, no business logic.
  * **View**: Created `AddFinancialYearView.xaml` with fields for Name, StartDate, EndDate, Save/Cancel buttons, validation area, loading indicator. Uses existing Design System completely with no inline styling.
  * **Navigation**: Enabled Add button from Financial Years list. Registered DataTemplate for AddFinancialYearViewModel. After successful save, GoBackAsync and automatic list refresh.
  * **Dependency Injection**: Registered `IFinancialYearCommandService` as Scoped, `ICreateFinancialYearValidator` as Scoped, `AddFinancialYearViewModel` as Transient.
* **Explicitly Excluded**:
  * Activate, Close Financial Year, Delete, Set Current Financial Year, Permission Enforcement, Advanced business rules.
* **Validation Status**: Build verified successfully. Architecture matches Add User and Add Role exactly. MVVM, SOLID, Query/Command separation, Dependency Injection, Validation outside ViewModels, No DbContext in ViewModels all maintained.

### Phase 2.11.3: Administration → Financial Year Management → Edit Financial Year
* **Goal**: Implement Edit Financial Year functionality following the exact architecture used by Edit User and Edit Role.
* **Included**:
  * **Models**: Created `EditFinancialYearModel`, `UpdateFinancialYearRequest`, `UpdateFinancialYearResult` with properties for FinancialYearId, Name, StartDate, EndDate, IsCurrent, IsClosed.
  * **Validation**: Created `IUpdateFinancialYearValidator` and `UpdateFinancialYearValidator` with validation rules: FinancialYearId > 0, Name required, max length 100, StartDate required, EndDate required, EndDate > StartDate, trim Name, friendly validation messages.
  * **Query Layer**: Extended `IFinancialYearQueryService` with `GetFinancialYearForEditAsync`. Implemented in `FinancialYearQueryService` with AsNoTracking(), projection to EditFinancialYearModel, ConfigureAwait(false), CancellationToken, null handling, efficient query.
  * **Command Layer**: Extended `IFinancialYearCommandService` with `UpdateFinancialYearAsync`. Implemented in `FinancialYearCommandService` with validation, verify Financial Year exists, duplicate Name check excluding current, trim Name, update editable fields only, single SaveChangesAsync, ConfigureAwait(false), logging, success/failure result pattern.
  * **ViewModel**: Created `EditFinancialYearViewModel` with InitializeAsync, INavigationAware, Load Financial Year, SaveCommand, CancelCommand, busy state, ValidationError, NavigationParameter handling, GoBackAsync on success. Constructor injection only, no DbContext, no business logic.
  * **View**: Created `EditFinancialYearView.xaml` with fields for Name, StartDate, EndDate, Is Current, Is Closed, Save/Cancel buttons, validation area, loading indicator. Uses existing Design System completely with no inline styling.
  * **Navigation**: Enabled Edit button from Financial Years list. Pass selected Financial Year ID through NavigationParameter. Registered DataTemplate. After successful save, GoBackAsync and automatic list refresh.
  * **Dependency Injection**: Registered `IUpdateFinancialYearValidator` as Scoped, `EditFinancialYearViewModel` as Transient.
* **Explicitly Excluded**:
  * Activate, Close Financial Year, Delete, Set Current Financial Year, Permission Enforcement, Advanced business rules.
* **Validation Status**: Build verified successfully. Architecture matches Edit User and Edit Role exactly. MVVM, SOLID, Query/Command separation, Dependency Injection, Validation outside ViewModels, No DbContext in ViewModels all maintained.

### Phase 2.11.4: Administration → Financial Year Management → Set Current Financial Year
* **Goal**: Implement Set Current Financial Year functionality following the exact architecture used by the existing Administration module.
* **Included**:
  * **Models**: Created `SetCurrentFinancialYearRequest` with FinancialYearId property. Created `SetCurrentFinancialYearResult` with IsSuccess, ErrorMessage properties and Success/Failure factory methods.
  * **Validation**: Created `ISetCurrentFinancialYearValidator` and `SetCurrentFinancialYearValidator` with validation rules: request not null, FinancialYearId > 0, friendly validation messages.
  * **Command Layer**: Extended `IFinancialYearCommandService` with `SetCurrentFinancialYearAsync`. Implemented in `FinancialYearCommandService` with validation, verify Financial Year exists, check if deleted, return Success if already current, clear current flag from existing current financial year, set requested financial year as current, single SaveChangesAsync, ConfigureAwait(false), logging, success/failure result pattern.
  * **ViewModel**: Modified `FinancialYearsViewModel` to add `SetCurrentFinancialYearCommand` with confirmation dialog, busy state handling, ValidationError, list refresh after success, CanExecute logic (not null and not already current). Updated SelectedFinancialYear property to notify SetCurrentFinancialYearCommand of CanExecute changes.
  * **View**: Modified `FinancialYearsView.xaml` to add Set Current button in header toolbar with Command binding to SetCurrentFinancialYearCommand.
  * **Dependency Injection**: Registered `ISetCurrentFinancialYearValidator` as Scoped in `AdministrationServiceCollectionExtensions.cs`.
* **Explicitly Excluded**:
  * Close Financial Year, Delete, Permission Enforcement, Advanced business rules.
* **Validation Status**: Build verified successfully. Architecture follows the exact patterns established by Users, Roles, and Permission Matrix modules. MVVM, SOLID, Query/Command separation, Dependency Injection, Validation outside ViewModels, No DbContext in ViewModels all maintained.

### Phase 2.11.5: Administration → Financial Year Management → Close Financial Year
* **Goal**: Implement Close Financial Year functionality following the exact architecture used by the existing Administration module. Closing a Financial Year means setting IsClosed = true. A Closed Financial Year cannot be edited or become Current. Closing is irreversible (no Reopen functionality in Phase 2).
* **Included**:
  * **Models**: Created `CloseFinancialYearRequest` with FinancialYearId property. Created `CloseFinancialYearResult` with IsSuccess, ErrorMessage properties and Success/Failure factory methods.
  * **Validation**: Created `ICloseFinancialYearValidator` and `CloseFinancialYearValidator` with validation rules: request not null, FinancialYearId > 0, friendly validation messages.
  * **Command Layer**: Extended `IFinancialYearCommandService` with `CloseFinancialYearAsync`. Implemented in `FinancialYearCommandService` with validation, verify Financial Year exists, return Success if already closed, return Failure if Current Financial Year, set IsClosed = true, single SaveChangesAsync, ConfigureAwait(false), logging, success/failure result pattern.
  * **ViewModel**: Modified `FinancialYearsViewModel` to add `CloseFinancialYearCommand` with confirmation dialog ("Are you sure you want to close this Financial Year?\n\nThis action cannot be undone."), busy state handling using SetBusy/ClearBusy base class methods, ValidationError, list refresh after success, CanExecute logic (not null and not already closed). Updated SelectedFinancialYear property to notify CloseFinancialYearCommand of CanExecute changes.
  * **View**: Modified `FinancialYearsView.xaml` to add Close button in header toolbar with Command binding to CloseFinancialYearCommand.
  * **Dependency Injection**: Registered `ICloseFinancialYearValidator` as Scoped in `AdministrationServiceCollectionExtensions.cs`.
* **Explicitly Excluded**:
  * Reopen Financial Year, Delete, Permission Enforcement, Advanced business rules.
* **Validation Status**: Build verified successfully. Architecture follows the exact patterns established by Users, Roles, and Permission Matrix modules. MVVM, SOLID, Query/Command separation, Dependency Injection, Validation outside ViewModels, No DbContext in ViewModels all maintained.

### Phase 2.11.5A: Close Financial Year Architecture Review & Refinement
* **Goal**: Perform a complete architecture review of the entire Phase 2.11.5 (Close Financial Year) implementation and refine code where necessary without introducing new features.
* **Included**:
  * **Architecture Review**: Comprehensive review of MVVM, SOLID, Models, Validation, Command Layer, ViewModel, View, Navigation, Dependency Injection, Query/Command Separation, Performance, Code Quality, Security, Existing Functionality, and Cross-Module Consistency.
  * **Code Refinement**: Modified `FinancialYearsViewModel` to remove IsBusy property override and use base class SetBusy/ClearBusy methods instead, matching the pattern used in UsersViewModel and RolesViewModel. Removed IsBusy check from CanExecuteCloseFinancialYear to match Users/Roles pattern.
* **Architecture Review Results**:
  * **MVVM Compliance**: PASS - FinancialYearsViewModel properly inherits from ViewModelBase, FinancialYearsView contains no business logic, clean separation of concerns, proper data binding.
  * **SOLID Compliance**: PASS - Single Responsibility (each class has one purpose), Open/Closed (extensible without modification), Liskov Substitution (interfaces properly implemented), Interface Segregation (focused interfaces), Dependency Inversion (depends on abstractions).
  * **Model Review**: PASS - CloseFinancialYearRequest and CloseFinancialYearResult naming and structure match SetCurrentFinancialYearResult, UpdateUserStatusResult, UpdateRoleStatusResult exactly. Success/Failure factory pattern consistent.
  * **Validation Review**: PASS - CloseFinancialYearValidator follows SetCurrentFinancialYearValidator pattern exactly, uses existing ValidationResult infrastructure, validates FinancialYearId > 0, provides friendly error messages, no validation in ViewModel.
  * **Command Layer Review**: PASS - CloseFinancialYearAsync validates request, verifies Financial Year exists, returns Success if already closed, returns Failure if Current Financial Year, sets IsClosed = true, single SaveChangesAsync, ConfigureAwait(false), proper exception handling with logging.
  * **ViewModel Review**: PASS - CloseFinancialYearCommand implemented with [RelayCommand], confirmation dialog matches existing pattern, busy state managed via base class methods, error handling via ValidationError property, refreshes list after success, no business logic, no DbContext.
  * **View Review**: PASS - Close button reuses existing IconButtonStyle, CaptionTextBlockStyle, Design System resources, spacing, typography. No inline styling, no hardcoded colours or fonts.
  * **Navigation Review**: PASS - No navigation changes, list refreshes via LoadFinancialYearsAsync after close, navigation lifecycle unchanged.
  * **Dependency Injection Review**: PASS - ICloseFinancialYearValidator registered as Scoped (matches other validators), constructor injection only, no service locator pattern.
  * **Performance Review**: PASS - Single database update with SaveChangesAsync, async throughout with ConfigureAwait(false), CancellationToken support, no duplicate queries.
  * **Code Quality Review**: PASS - SOLID principles followed, DRY principle followed, naming consistent with existing modules, folder structure matches existing modules, namespace consistent, XML documentation present.
  * **Security Review**: PASS - Financial Year existence validation, Current Financial Year protection, no sensitive logging, no authentication changes, no authorization changes, no security regressions.
  * **Cross-Module Consistency**: PASS - Folder structure matches Users/Roles/Permission Matrix modules, service architecture matches, validation architecture matches, ViewModel lifecycle matches, navigation matches, result pattern matches, request models match, dependency injection patterns match, logging matches, error handling matches, busy state management matches (after fix), coding style matches.
* **Explicitly Unchanged**:
  * Reopen Financial Year, database schema, Global Query Filter, Phase 0-2.11.4, existing functionality
* **Files Modified**: FinancialYearsViewModel.cs (removed IsBusy override, updated to use base class methods).
* **Justification for Changes**:
  * **ViewModel Consistency**: Removed IsBusy property override to match UsersViewModel and RolesViewModel pattern, which both use base ViewModelBase.IsBusy property directly without overriding.
  * **Busy State Management**: Updated CloseFinancialYearAsync and LoadFinancialYearsAsync to use SetBusy/ClearBusy base class methods instead of directly setting IsBusy, ensuring architectural consistency.
  * **CanExecute Logic**: Removed IsBusy check from CanExecuteCloseFinancialYear to match UsersViewModel and RolesViewModel pattern (which don't check IsBusy in their CanExecute methods).
* **Validation Status**: Build verified successfully with no errors. Close button is available. Confirmation dialog appears. CloseFinancialYearCommand executes successfully. Financial Year is closed. IsClosed is set to true. Current Financial Year cannot be closed. Already closed Financial Year returns Success without updating. Query/Command separation is maintained. No DbContext exists in ViewModels. Existing architecture is fully reused. No regressions are introduced. Implementation is enterprise-grade and ready to be frozen.

### Phase 2.11 B: Completed properly
### Phase 2: Completed 

### Phase 3.1: Masters Infrastructure
* **Goal**: Establish the technical foundation for the Masters feature without introducing any business functionality, UI, or CRUD operations. Review existing infrastructure and ensure it is sufficient for future Master modules (Company, Customer, Vendor, Vehicle, etc.).
* **Included**:
  * **Infrastructure Review**: Comprehensive review of existing Masters feature structure, DI infrastructure, navigation registration, and shared infrastructure.
  * **Existing Infrastructure Verification**: Confirmed that all required infrastructure already exists from Phase 2:
    - `Masters/DependencyInjection/MastersServiceCollectionExtensions.cs` for DI registration
    - `ShellViewModel.NavigateToCompaniesCommand` demonstrating navigation pattern
    - `DataTemplates.xaml` with CompaniesViewModel mapping
    - `VeteranLogistics.Shared` providing shared contracts, models, and patterns
    - `MVVM/ViewModelBase.cs` for MVVM base class
    - `Desktop/Navigation/` for navigation services
  * **Pattern Documentation**: Documented how future Master modules will reuse existing infrastructure by following the same patterns as Administration modules and the partial Company module.
* **Explicitly Excluded**:
  * Business module implementation (Company, Customer, Vendor, Vehicle, etc.)
  * CRUD operations
  * Database entities
  * Migrations
  * ViewModels for business modules
  * Views for business modules
  * New abstractions or infrastructure components
  * Duplicate infrastructure
* **Validation Status**: No new files created or modified. All required infrastructure already exists from Phase 2. The Masters feature foundation is complete and ready for business module implementation in Phase 3.2+.

### Phase 3.2: Company Management
* **Goal**: Complete the Company module to serve as the reference implementation for all future Master modules. Extend the existing partial Company implementation to full production-ready status with complete CRUD functionality, search, validation, and UI integration.
* **Included**:
  * **Command Layer Implementation**:
    - Request Models: CreateCompanyRequest, UpdateCompanyRequest, UpdateCompanyStatusRequest, DeleteCompanyRequest
    - Result Models: CreateCompanyResult, UpdateCompanyResult, UpdateCompanyStatusResult, DeleteCompanyResult (with Success/Failure factory methods)
    - Validator Interfaces: ICreateCompanyValidator, IUpdateCompanyValidator, IUpdateCompanyStatusValidator, IDeleteCompanyValidator
    - Validator Implementations: CreateCompanyValidator, UpdateCompanyValidator, UpdateCompanyStatusValidator, DeleteCompanyValidator
    - Command Service: ICompanyCommandService and CompanyCommandService with full CRUD operations, duplicate checking, validation, logging, and soft delete
  * **UI Layer Implementation**:
    - CompaniesViewModel: Search with debouncing (300ms), Add/Edit/Activate/Deactivate/Delete/Refresh commands, selected item tracking, busy state management
    - AddCompanyViewModel: Full form with all company fields, save/cancel commands, validation, busy state management
    - EditCompanyViewModel: INavigationAware for parameter passing, load on navigation, full form with all company fields, save/cancel commands, validation
    - CompaniesView: Search textbox enabled, all action buttons (Add, Edit, Activate, Deactivate, Delete, Refresh), DataGrid with selection, loading/error indicators
    - AddCompanyView: Complete form with all fields, save/cancel buttons, validation display, loading indicator
    - EditCompanyView: Complete form with all fields, save/cancel buttons, validation display, loading indicator
  * **Infrastructure Updates**:
    - DI Registration: MastersServiceCollectionExtensions updated with all Company services (ICompanyQueryService, ICompanyCommandService, all validators, all ViewModels)
    - DataTemplates: DataTemplates.xaml updated with AddCompanyViewModel and EditCompanyViewModel mappings
  * **Query Layer Completion**: Extended ICompanyQueryService with SearchCompaniesAsync and GetCompanyForEditAsync methods, implemented in CompanyQueryService with AsNoTracking, EF.Functions.Like, CancellationToken support, and ConfigureAwait(false)
* **Explicitly Excluded**:
  * Rewriting or refactoring completed code (database layer, existing query service)
  * New architectural patterns or speculative abstractions
  * Password hashing (not applicable to Company module)
  * Advanced search with multiple filter criteria (simple LIKE pattern implemented)
  * Export functionality
  * Bulk operations
* **Validation Status**: Build verified successfully with 0 errors, 4 unrelated package warnings (Serilog version mismatches). All architectural patterns followed: MVVM with CommunityToolkit.Mvvm, Query/Command separation, Validation layer with ValidationResult, Result pattern, Dependency injection with proper lifetimes, Navigation with INavigationAware, Async/await with CancellationToken and ConfigureAwait(false), Logging with ILogger, Busy state management. Company module is production-ready and serves as reference implementation for all future Master modules. Deliverables report generated: PHASE_3.2_COMPANY_MODULE_DELIVERABLES.md.

### Phase 3.3: Customer Management
* **Goal**: Implement the Customer module following the Company module as the reference implementation. Create a complete production-ready Customer module with full CRUD functionality, search, validation, and UI integration, using the exact same architectural patterns as Company.
* **Included**:
  * **Database Layer**:
    - Customer Entity: All required fields (CustomerCode, CustomerName, AddressLine1, AddressLine2, City, State, Country, PostalCode, PhoneNumber, Email, GSTNumber, PANNumber), soft delete support (IsDeleted, DeletedOn), audit fields (CreatedBy, ModifiedBy, CreatedOn, ModifiedOn), active status (IsActive)
    - CustomerConfiguration: Required field constraints, max length constraints, unique indexes on CustomerCode and GSTNumber, global query filter for soft deletion
    - DbSet Registration: Added DbSet<Customer> Customers to VeteranLogisticsDbContext
    - Migration: 20260710104837_AddCustomerEntity created and applied
  * **Query Layer**:
    - ICustomerQueryService interface with GetAllCustomersAsync, SearchCustomersAsync, GetCustomerForEditAsync methods
    - CustomerQueryService implementation with AsNoTracking, EF.Functions.Like, projection to DTOs, CancellationToken support, ConfigureAwait(false)
    - CustomerListItem and CustomerModel DTOs for listing and detail views
  * **Command Layer**:
    - Request Models: CreateCustomerRequest, UpdateCustomerRequest, UpdateCustomerStatusRequest, DeleteCustomerRequest
    - Result Models: CreateCustomerResult, UpdateCustomerResult, UpdateCustomerStatusResult, DeleteCustomerResult (with Success/Failure factory methods)
    - Validator Interfaces: ICreateCustomerValidator, IUpdateCustomerValidator, IUpdateCustomerStatusValidator, IDeleteCustomerValidator
    - Validator Implementations: CreateCustomerValidator, UpdateCustomerValidator, UpdateCustomerStatusValidator, DeleteCustomerValidator
    - Command Service: ICustomerCommandService and CustomerCommandService with full CRUD operations, duplicate checking, validation, logging, soft delete
  * **UI Layer**:
    - CustomersViewModel: Search with debouncing (300ms), Add/Edit/Activate/Deactivate/Delete/Refresh commands, selected item tracking, busy state management
    - AddCustomerViewModel: Full form with all customer fields, save/cancel commands, validation, busy state management
    - EditCustomerViewModel: INavigationAware for parameter passing, load on navigation, full form with all customer fields, save/cancel commands, validation
    - CustomersView: Search textbox enabled, all action buttons (Add, Edit, Activate, Deactivate, Delete, Refresh), DataGrid with selection, loading/error indicators
    - AddCustomerView: Complete form with all fields, save/cancel buttons, validation display, loading indicator
    - EditCustomerView: Complete form with all fields, save/cancel buttons, validation display, loading indicator
  * **Infrastructure Updates**:
    - DI Registration: MastersServiceCollectionExtensions updated with all Customer services (ICustomerQueryService, ICustomerCommandService, all validators, all ViewModels)
    - DataTemplates: DataTemplates.xaml updated with CustomersViewModel, AddCustomerViewModel, and EditCustomerViewModel mappings
* **Explicitly Excluded**:
  * New architectural patterns or redesigns (strictly following Company module patterns)
  * Repository pattern, MediatR, or generic frameworks
  * Business logic in ViewModels (only in services)
  * DbContext access in ViewModels
  * Advanced search with multiple filter criteria (simple LIKE pattern implemented)
  * Export functionality
  * Bulk operations
* **Validation Status**: Build verified successfully with 0 errors, 4 unrelated package warnings (Serilog version mismatches). All architectural patterns followed: MVVM with CommunityToolkit.Mvvm, Query/Command separation, Validation layer with ValidationResult, Result pattern, Dependency injection with proper lifetimes, Navigation with INavigationAware, Async/await with CancellationToken and ConfigureAwait(false), Logging with ILogger, Busy state management. Customer module is architecturally identical to Company module and is production-ready. Demonstrates that established patterns can be successfully replicated for future Master modules (Vendor, Vehicle, etc.).

### Phase 3.4: Vendor Management
* **Goal**: Implement the Vendor module following the Company and Customer modules as reference implementations. Create a complete production-ready Vendor module with full CRUD functionality, search, validation, and UI integration, using the exact same architectural patterns as Company and Customer.
* **Included**:
  * **Database Layer**:
    - Vendor Entity: All required fields (VendorCode, VendorName, AddressLine1, AddressLine2, City, State, Country, PostalCode, PhoneNumber, Email, GSTNumber, PANNumber, ContactPerson), soft delete support (IsDeleted, DeletedOn), audit fields (CreatedBy, ModifiedBy, CreatedOn, ModifiedOn), active status (IsActive)
    - VendorConfiguration: Required field constraints, max length constraints, unique indexes on VendorCode and GSTNumber, global query filter for soft deletion
    - DbSet Registration: Added DbSet<Vendor> Vendors to VeteranLogisticsDbContext
    - Migration: AddVendorEntity created and applied
  * **Query Layer**:
    - IVendorQueryService interface with GetAllVendorsAsync, SearchVendorsAsync, GetVendorForEditAsync methods
    - VendorQueryService implementation with AsNoTracking, EF.Functions.Like, projection to DTOs, CancellationToken support, ConfigureAwait(false)
    - VendorListItem and VendorModel DTOs for listing and detail views
  * **Command Layer**:
    - Request Models: CreateVendorRequest, UpdateVendorRequest, UpdateVendorStatusRequest, DeleteVendorRequest
    - Result Models: CreateVendorResult, UpdateVendorResult, UpdateVendorStatusResult, DeleteVendorResult (with Success/Failure factory methods)
    - Validator Interfaces: ICreateVendorValidator, IUpdateVendorValidator, IUpdateVendorStatusValidator, IDeleteVendorValidator
    - Validator Implementations: CreateVendorValidator, UpdateVendorValidator, UpdateVendorStatusValidator, DeleteVendorValidator
    - Command Service: IVendorCommandService and VendorCommandService with full CRUD operations, duplicate checking, validation, logging, soft delete
  * **UI Layer**:
    - VendorsViewModel: Search with debouncing (300ms), Add/Edit/Activate/Deactivate/Delete/Refresh commands, selected item tracking, busy state management
    - AddVendorViewModel: Full form with all vendor fields including ContactPerson, save/cancel commands, validation, busy state management
    - EditVendorViewModel: INavigationAware for parameter passing, load on navigation, full form with all vendor fields including ContactPerson, save/cancel commands, validation
    - VendorsView: Search textbox enabled, all action buttons (Add Vendor, Edit Vendor, Activate, Deactivate, Delete Vendor, Refresh), DataGrid with selection, loading/error indicators
    - AddVendorView: Complete form with all fields including ContactPerson, save/cancel buttons, validation display, loading indicator
    - EditVendorView: Complete form with all fields including ContactPerson, save/cancel buttons, validation display, loading indicator
  * **Infrastructure Updates**:
    - DI Registration: MastersServiceCollectionExtensions updated with all Vendor services (IVendorQueryService, IVendorCommandService, all validators, all ViewModels)
    - DataTemplates: DataTemplates.xaml updated with VendorsViewModel, AddVendorViewModel, and EditVendorViewModel mappings
    - Navigation: ShellView.xaml updated with Vendors menu item, ShellViewModel.cs updated with NavigateToVendorsCommand
* **Explicitly Excluded**:
  * New architectural patterns or redesigns (strictly following Company/Customer module patterns)
  * Repository pattern, MediatR, or generic frameworks
  * Business logic in ViewModels (only in services)
  * DbContext access in ViewModels
  * Advanced search with multiple filter criteria (simple LIKE pattern implemented)
  * Export functionality
  * Bulk operations
* **Validation Status**: Build verified successfully with 0 errors, 4 unrelated package warnings (Serilog version mismatches). All architectural patterns followed: MVVM with CommunityToolkit.Mvvm, Query/Command separation, Validation layer with ValidationResult, Result pattern, Dependency injection with proper lifetimes, Navigation with INavigationAware, Async/await with CancellationToken and ConfigureAwait(false), Logging with ILogger, Busy state management. Vendor module is architecturally identical to Company and Customer modules and is production-ready. Demonstrates that established patterns can be successfully replicated for future Master modules (Vehicle, etc.).

### Phase 3.5: Source Management
* **Goal**: Implement the Source module following the Company, Customer, and Vendor modules as reference implementations. Create a complete production-ready Source module with full CRUD functionality, search, validation, and UI integration, using the exact same architectural patterns as Company, Customer, and Vendor.
* **Included**:
  * **Database Layer**:
    - Source Entity: All required fields (SourceCode, SourceName, AddressLine1, AddressLine2, City, State, Country, PostalCode, ContactPerson, PhoneNumber, Email, GSTNumber, Latitude, Longitude, Remarks), soft delete support (IsDeleted, DeletedOn), audit fields (CreatedBy, ModifiedBy, CreatedOn, ModifiedOn), active status (IsActive)
    - SourceConfiguration: Required field constraints, max length constraints, unique index on SourceCode, global query filter for soft deletion, precision configuration for Latitude/Longitude
    - DbSet Registration: Added DbSet<Source> Sources to VeteranLogisticsDbContext
    - Migration: AddSourceEntity created and applied
  * **Query Layer**:
    - ISourceQueryService interface with GetAllSourcesAsync, SearchSourcesAsync, GetSourceForEditAsync methods
    - SourceQueryService implementation with AsNoTracking, EF.Functions.Like, projection to DTOs, CancellationToken support, ConfigureAwait(false)
    - SourceListItem and SourceModel DTOs for listing and detail views
  * **Command Layer**:
    - Request Models: CreateSourceRequest, UpdateSourceRequest, UpdateSourceStatusRequest, DeleteSourceRequest
    - Result Models: CreateSourceResult, UpdateSourceResult, UpdateSourceStatusResult, DeleteSourceResult (with Success/Failure factory methods)
    - Validator Interfaces: ICreateSourceValidator, IUpdateSourceValidator, IUpdateSourceStatusValidator, IDeleteSourceValidator
    - Validator Implementations: CreateSourceValidator, UpdateSourceValidator, UpdateSourceStatusValidator, DeleteSourceValidator
    - Command Service: ISourceCommandService and SourceCommandService with full CRUD operations, duplicate checking, validation, logging, soft delete
  * **UI Layer**:
    - SourcesViewModel: Search with debouncing (300ms), Add/Edit/Activate/Deactivate/Delete/Refresh commands, selected item tracking, busy state management
    - AddSourceViewModel: Full form with all source fields including Latitude, Longitude, Remarks, save/cancel commands, validation, busy state management
    - EditSourceViewModel: INavigationAware for parameter passing, load on navigation, full form with all source fields including Latitude, Longitude, Remarks, save/cancel commands, validation
    - SourcesView: Search textbox enabled, all action buttons (Add Source, Edit Source, Activate, Deactivate, Delete Source, Refresh), DataGrid with selection, loading/error indicators
    - AddSourceView: Complete form with all fields including Latitude, Longitude, Remarks, save/cancel buttons, validation display, loading indicator
    - EditSourceView: Complete form with all fields including Latitude, Longitude, Remarks, save/cancel buttons, validation display, loading indicator
  * **Infrastructure Updates**:
    - DI Registration: MastersServiceCollectionExtensions updated with all Source services (ISourceQueryService, ISourceCommandService, all validators, all ViewModels)
    - DataTemplates: DataTemplates.xaml updated with SourcesViewModel, AddSourceViewModel, and EditSourceViewModel mappings
    - Navigation: ShellView.xaml updated with Sources menu item, ShellViewModel.cs updated with NavigateToSourcesCommand
* **Explicitly Excluded**:
  * New architectural patterns or redesigns (strictly following Company/Customer/Vendor module patterns)
  * Repository pattern, MediatR, or generic frameworks
  * Business logic in ViewModels (only in services)
  * DbContext access in ViewModels
  * Advanced search with multiple filter criteria (simple LIKE pattern implemented)
  * Export functionality
  * Bulk operations
* **Validation Status**: Build verified successfully with 0 errors, 4 unrelated package warnings (Serilog version mismatches). All architectural patterns followed: MVVM with CommunityToolkit.Mvvm, Query/Command separation, Validation layer with ValidationResult, Result pattern, Dependency injection with proper lifetimes, Navigation with INavigationAware, Async/await with CancellationToken and ConfigureAwait(false), Logging with ILogger, Busy state management. Source module is architecturally identical to Company, Customer, and Vendor modules and is production-ready. Demonstrates that established patterns can be successfully replicated for future Master modules (Vehicle, etc.).

### Phase 3.6: Destination Management
* **Goal**: Implement the Destination module as the counterpart to the Source module. Create a complete production-ready Destination module with full CRUD functionality, search, validation, and UI integration, using the exact same architectural patterns as Source.
* **Included**:
  * **Database Layer**:
    - Destination Entity: All required fields (DestinationCode, DestinationName, AddressLine1, AddressLine2, City, State, Country, PostalCode, ContactPerson, PhoneNumber, Email, GSTNumber, Latitude, Longitude, Remarks), soft delete support (IsDeleted, DeletedOn), audit fields (CreatedBy, ModifiedBy, CreatedOn, ModifiedOn), active status (IsActive)
    - DestinationConfiguration: Required field constraints, max length constraints, unique index on DestinationCode, global query filter for soft deletion, precision configuration for Latitude/Longitude
    - DbSet Registration: Added DbSet<Destination> Destinations to VeteranLogisticsDbContext
    - Migration: AddDestinationEntity created and applied
  * **Query Layer**:
    - IDestinationQueryService interface with GetAllDestinationsAsync, SearchDestinationsAsync, GetDestinationForEditAsync methods
    - DestinationQueryService implementation with AsNoTracking, EF.Functions.Like, projection to DTOs, CancellationToken support, ConfigureAwait(false)
    - DestinationListItem and DestinationModel DTOs for listing and detail views
  * **Command Layer**:
    - Request Models: CreateDestinationRequest, UpdateDestinationRequest, UpdateDestinationStatusRequest, DeleteDestinationRequest
    - Result Models: CreateDestinationResult, UpdateDestinationResult, UpdateDestinationStatusResult, DeleteDestinationResult (with Success/Failure factory methods)
    - Validator Interfaces: ICreateDestinationValidator, IUpdateDestinationValidator, IUpdateDestinationStatusValidator, IDeleteDestinationValidator
    - Validator Implementations: CreateDestinationValidator, UpdateDestinationValidator, UpdateDestinationStatusValidator, DeleteDestinationValidator
    - Command Service: IDestinationCommandService and DestinationCommandService with full CRUD operations, duplicate checking, validation, logging, soft delete
  * **UI Layer**:
    - DestinationsViewModel: Search with debouncing (300ms), Add/Edit/Activate/Deactivate/Delete/Refresh commands, selected item tracking, busy state management
    - AddDestinationViewModel: Full form with all destination fields including Latitude, Longitude, Remarks, save/cancel commands, validation, busy state management
    - EditDestinationViewModel: INavigationAware for parameter passing, load on navigation, full form with all destination fields including Latitude, Longitude, Remarks, save/cancel commands, validation
    - DestinationsView: Search textbox enabled, all action buttons (Add Destination, Edit Destination, Activate, Deactivate, Delete Destination, Refresh), DataGrid with selection, loading/error indicators
    - AddDestinationView: Complete form with all fields including Latitude, Longitude, Remarks, save/cancel buttons, validation display, loading indicator
    - EditDestinationView: Complete form with all fields including Latitude, Longitude, Remarks, save/cancel buttons, validation display, loading indicator
  * **Infrastructure Updates**:
    - DI Registration: MastersServiceCollectionExtensions updated with all Destination services (IDestinationQueryService, IDestinationCommandService, all validators, all ViewModels)
    - DataTemplates: DataTemplates.xaml updated with DestinationsViewModel, AddDestinationViewModel, and EditDestinationViewModel mappings
    - Navigation: ShellView.xaml updated with Destinations menu item, ShellViewModel.cs updated with NavigateToDestinationsCommand
* **Explicitly Excluded**:
  * New architectural patterns or redesigns (strictly following Source module patterns)
  * Repository pattern, MediatR, or generic frameworks
  * Business logic in ViewModels (only in services)
  * DbContext access in ViewModels
  * Advanced search with multiple filter criteria (simple LIKE pattern implemented)
  * Export functionality
  * Bulk operations
* **Validation Status**: Build verified successfully with 0 errors, 4 unrelated package warnings (Serilog version mismatches). All architectural patterns followed: MVVM with CommunityToolkit.Mvvm, Query/Command separation, Validation layer with ValidationResult, Result pattern, Dependency injection with proper lifetimes, Navigation with INavigationAware, Async/await with CancellationToken and ConfigureAwait(false), Logging with ILogger, Busy state management. Destination module is architecturally identical to Source module and is production-ready.

### Phase 3.7: Material Management
* **Goal**: Implement the Material module as a simplified Master module with minimal business fields. Create a complete production-ready Material module with full CRUD functionality, search, validation, and UI integration, using the exact same architectural patterns as previous Master modules but with intentionally minimal business model.
* **Included**:
  * **Database Layer**:
    - Material Entity: Minimal fields (MaterialName only), soft delete support (IsDeleted, DeletedOn), audit fields (CreatedBy, ModifiedBy, CreatedOn, ModifiedOn), active status (IsActive)
    - MaterialConfiguration: Required field constraints, max length constraints, unique index on MaterialName, global query filter for soft deletion
    - DbSet Registration: Added DbSet<Material> Materials to VeteranLogisticsDbContext
    - Migration: AddMaterialEntity created and applied
  * **Query Layer**:
    - IMaterialQueryService interface with GetAllMaterialsAsync, SearchMaterialsAsync, GetMaterialForEditAsync methods
    - MaterialQueryService implementation with AsNoTracking, EF.Functions.Like, projection to DTOs, CancellationToken support, ConfigureAwait(false)
    - MaterialListItem and MaterialModel DTOs for listing and detail views
  * **Command Layer**:
    - Request Models: CreateMaterialRequest, UpdateMaterialRequest, UpdateMaterialStatusRequest, DeleteMaterialRequest
    - Result Models: CreateMaterialResult, UpdateMaterialResult, UpdateMaterialStatusResult, DeleteMaterialResult (with Success/Failure factory methods)
    - Validator Interfaces: ICreateMaterialValidator, IUpdateMaterialValidator, IUpdateMaterialStatusValidator, IDeleteMaterialValidator
    - Validator Implementations: CreateMaterialValidator, UpdateMaterialValidator, UpdateMaterialStatusValidator, DeleteMaterialValidator
    - Command Service: IMaterialCommandService and MaterialCommandService with full CRUD operations, duplicate checking, validation, logging, soft delete
  * **UI Layer**:
    - MaterialsViewModel: Search with debouncing (300ms), Add/Edit/Activate/Deactivate/Delete/Refresh commands, selected item tracking, busy state management
    - AddMaterialViewModel: Minimal form with MaterialName and IsActive, save/cancel commands, validation, busy state management
    - EditMaterialViewModel: INavigationAware for parameter passing, load on navigation, minimal form with MaterialName and IsActive, save/cancel commands, validation
    - MaterialsView: Search textbox enabled, all action buttons (Add Material, Edit Material, Activate, Deactivate, Delete Material, Refresh), DataGrid with selection, loading/error indicators
    - AddMaterialView: Minimal form with MaterialName and IsActive, save/cancel buttons, validation display, loading indicator
    - EditMaterialView: Minimal form with MaterialName and IsActive, save/cancel buttons, validation display, loading indicator
  * **Infrastructure Updates**:
    - DI Registration: MastersServiceCollectionExtensions updated with all Material services (IMaterialQueryService, IMaterialCommandService, all validators, all ViewModels)
    - DataTemplates: DataTemplates.xaml updated with MaterialsViewModel, AddMaterialViewModel, and EditMaterialViewModel mappings
    - Navigation: ShellView.xaml updated with Materials menu item, ShellViewModel.cs updated with NavigateToMaterialsCommand
* **Explicitly Excluded**:
  * New architectural patterns or redesigns (strictly following previous Master module patterns)
  * Repository pattern, MediatR, or generic frameworks
  * Business logic in ViewModels (only in services)
  * DbContext access in ViewModels
  * MaterialCode field (not required per business requirements)
  * Address fields (not required per business requirements)
  * Contact fields (not required per business requirements)
  * Tax fields (not required per business requirements)
  * Advanced search with multiple filter criteria (simple LIKE pattern implemented)
  * Export functionality
  * Bulk operations
* **Validation Status**: Build verified successfully with 0 errors, 4 unrelated package warnings (Serilog version mismatches). All architectural patterns followed: MVVM with CommunityToolkit.Mvvm, Query/Command separation, Validation layer with ValidationResult, Result pattern, Dependency injection with proper lifetimes, Navigation with INavigationAware, Async/await with CancellationToken and ConfigureAwait(false), Logging with ILogger, Busy state management. Material module is architecturally identical to previous Master modules with intentionally minimal business model and is production-ready. Demonstrates that established patterns can be successfully adapted for both complex and simple Master implementations.

## 3. Architecture & Design

### Dependency Injection and Composition
* `Configuration/HostConfiguration.cs` builds the generic host, configures logging, and wires up application services.
* `DependencyInjection/ServiceCollectionExtensions.cs` is the main composition entry point used by the host.
* The application is composed from smaller registration helpers so each area can evolve independently.

### Folder-Based Organization
* `Desktop\Shell` contains the shell view and shell ViewModel.
* `Desktop\Navigation` contains navigation abstractions and implementations.
* `Desktop\Converters` contains value converters for data binding.
* `Desktop\Services\Dialog` contains dialog infrastructure.
* `Desktop\Services\Notification` contains notification infrastructure.
* `Options` contains strongly typed configuration models.
* `Styles` contains reusable UI control styles.
* `Themes` contains theme-specific dictionaries and theme management.
* `VeteranLogistics.Data` contains data access infrastructure.
* `VeteranLogistics.Shared` contains reusable constants, enums, exceptions, interfaces, results, and validation helpers.
* `Authorization` contains role-based authorization infrastructure.

### Security and Privacy
* Logging is configured to avoid recording sensitive data such as passwords, connection strings, or personal information.
* Code comments are used to preserve this guidance for future contributors.

## 4. Developer Guidance

### Usage Guidelines
* **Logging**: Inject `Microsoft.Extensions.Logging.ILogger<T>` into classes via constructor injection and use structured logging.
* **Dialogs and Notifications**: Inject `IDialogService` for modal dialogs and `INotificationService` for non-blocking user feedback.
* **Exception Handling**: Allow unexpected exceptions to flow to the global handlers. Catch and handle only expected business errors.

### Build & Run
1. Restore packages: `dotnet restore`
2. Build solution: `dotnet build`
3. Run application from Visual Studio or use `dotnet run` on the executable project

## 5. Current File Inventory

### Application Core
* `App.xaml`
* `App.xaml.cs`
* `MainWindow.xaml`
* `MainWindow.xaml.cs`
* `appsettings.json`
* `AssemblyInfo.cs`

### Configuration
* `Configuration/HostConfiguration.cs`

### Dependency Injection
* `DependencyInjection/ServiceCollectionExtensions.cs`
* `DependencyInjection/NavigationRegistration.cs`
* `DependencyInjection/DialogRegistration.cs`
* `DependencyInjection/ViewModelRegistration.cs`
* `DependencyInjection/ViewRegistration.cs`
* `DependencyInjection/ServiceRegistration.cs`

### Authentication
* `Authentication/Contracts/IAuthenticationService.cs`
* `Authentication/Contracts/IApplicationContext.cs`
* `Authentication/Contracts/ISessionManager.cs`
* `Authentication/Contracts/IAuthenticationRepository.cs`
* `Authentication/Contracts/IPasswordHasher.cs`
* `Authentication/Contracts/IPasswordPolicy.cs`
* `Authentication/Contracts/IAuthenticationAuditService.cs`
* `Authentication/Contracts/IRememberMeService.cs`
* `Authentication/Contracts/ILogoutService.cs` (Phase 1.10)
* `Authentication/Models/AuthenticatedUser.cs`
* `Authentication/Models/AuthenticationState.cs`
* `Authentication/Models/UserSession.cs`
* `Authentication/Models/SessionInfo.cs` (Phase 1.7)
* `Authentication/Models/PasswordHashResult.cs`
* `Authentication/Models/AuthenticationAuditEntry.cs`
* `Authentication/Models/LoginRequest.cs`
* `Authentication/Models/AuthenticationResult.cs`
* `Authentication/Models/RememberMeSettings.cs`
* `Authentication/Events/SessionChangedEventArgs.cs` (Phase 1.7)
* `Authentication/Session/ApplicationContext.cs`
* `Authentication/Session/SessionManager.cs`
* `Authentication/Security/PasswordHasher.cs`
* `Authentication/Security/PasswordPolicy.cs`
* `Authentication/Repositories/AuthenticationRepository.cs`
* `Authentication/Services/AuthenticationService.cs`
* `Authentication/Services/AuthenticationAuditService.cs`
* `Authentication/Services/RememberMeService.cs`
* `Authentication/Services/LogoutService.cs` (Phase 1.10)
* `Authentication/Validation/LoginRequestValidator.cs`
* `Authentication/ViewModels/LoginViewModel.cs`
* `Authentication/Views/LoginView.xaml`
* `Authentication/Views/LoginView.xaml.cs`
* `Authentication/DependencyInjection/AuthenticationServiceCollectionExtensions.cs`
* `Authentication/DependencyInjection/AuthenticationPersistenceServiceCollectionExtensions.cs`
* `Authentication/DependencyInjection/AuthenticationWorkflowServiceCollectionExtensions.cs`
* `Authentication/DependencyInjection/AuthenticationRememberMeServiceCollectionExtensions.cs`
* `Authentication/DependencyInjection/AuthenticationLogoutServiceCollectionExtensions.cs` (Phase 1.10)

### Authorization
* `Authorization/Contracts/IRoleAuthorizationService.cs` (Phase 1.8)
* `Authorization/Contracts/IPermissionProvider.cs` (Phase 1.9)
* `Authorization/Contracts/IPermissionAuthorizationService.cs` (Phase 1.9)
* `Authorization/Models/ApplicationRole.cs` (Phase 1.8)
* `Authorization/Models/ApplicationPermission.cs` (Phase 1.9)
* `Authorization/Providers/DefaultPermissionProvider.cs` (Phase 1.9)
* `Authorization/Services/RoleAuthorizationService.cs` (Phase 1.8)
* `Authorization/Services/PermissionAuthorizationService.cs` (Phase 1.9)
* `Authorization/DependencyInjection/AuthorizationServiceCollectionExtensions.cs` (Phase 1.8, Phase 1.9)

### Administration
* `Administration/DependencyInjection/AdministrationServiceCollectionExtensions.cs` (Phase 2.1 - Refactored, Phase 2.2, Phase 2.5)
* `Administration/Users/Contracts/IUserCommandService.cs` (Phase 2.3, Phase 2.5)
* `Administration/Users/Contracts/IUserQueryService.cs` (Phase 2.2, Phase 2.4A)
* `Administration/Users/Contracts/IUpdateUserValidator.cs` (Phase 2.3)
* `Administration/Users/Contracts/IUpdateUserStatusValidator.cs` (Phase 2.5)
* `Administration/Users/Models/CreateUserRequest.cs` (Phase 2.3)
* `Administration/Users/Models/UpdateUserRequest.cs` (Phase 2.3)
* `Administration/Users/Models/UpdateUserStatusRequest.cs` (Phase 2.5)
* `Administration/Users/Models/UserListItem.cs` (Phase 2.2)
* `Administration/Users/Models/EditUserModel.cs` (Phase 2.4A)
* `Administration/Users/Services/UserCommandService.cs` (Phase 2.3, Phase 2.5)
* `Administration/Users/Services/UserQueryService.cs` (Phase 2.2, Phase 2.4A)
* `Administration/Users/Validators/CreateUserValidator.cs` (Phase 2.3A)
* `Administration/Users/Validators/UpdateUserValidator.cs` (Phase 2.3)
* `Administration/Users/Validators/UpdateUserStatusValidator.cs` (Phase 2.5)
* `Administration/Users/ViewModels/UsersViewModel.cs` (Phase 2.2, Phase 2.4A, Phase 2.5)
* `Administration/Users/ViewModels/AddUserViewModel.cs` (Phase 2.3)
* `Administration/Users/ViewModels/EditUserViewModel.cs` (Phase 2.4A, Phase 2.4B)
* `Administration/Users/Views/UsersView.xaml` (Phase 2.2, Phase 2.5)
* `Administration/Users/Views/UsersView.xaml.cs` (Phase 2.2)
* `Administration/Users/Views/AddUserView.xaml` (Phase 2.3)
* `Administration/Users/Views/AddUserView.xaml.cs` (Phase 2.3)
* `Administration/Users/Views/EditUserView.xaml` (Phase 2.4)
* `Administration/Users/Views/EditUserView.xaml.cs` (Phase 2.4)

### Desktop Shell
* `Desktop\Shell\ShellView.xaml`
* `Desktop\Shell\ShellView.xaml.cs`
* `Desktop\Shell\ShellViewModel.cs`

### Navigation
* `Desktop\Navigation\INavigationService.cs`
* `Desktop\Navigation\IViewModelFactory.cs`
* `Desktop\Navigation\INavigationAware.cs` (Phase 2.4B)
* `Desktop\Navigation\NavigationParameter.cs`
* `Desktop\Navigation\NavigationResult.cs`
* `Desktop\Navigation\NavigationService.cs`
* `Desktop\Navigation\ViewModelFactory.cs`

### Converters
* `Desktop\Converters\NullToVisibilityConverter.cs`
* `Desktop\Converters\BooleanToVisibilityConverter.cs`

### Dialog Services
* `Desktop\Services\Dialog\IDialogService.cs`
* `Desktop\Services\Dialog\DialogOptions.cs`
* `Desktop\Services\Dialog\DialogResult.cs`
* `Desktop\Services\Dialog\DialogType.cs`
* `Desktop\Services\Dialog\DialogService.cs`

### Notification Services
* `Desktop\Services\Notification\INotificationService.cs`
* `Desktop\Services\Notification\NotificationMessage.cs`
* `Desktop\Services\Notification\NotificationType.cs`
* `Desktop\Services\Notification\NotificationService.cs`

### MVVM Base Types
* `MVVM\ViewModelBase.cs` (Phase 2.4B - NavigationParameter property removed)
* `MVVM\ObservableValidatorBase.cs`

### Options
* `Options\ApplicationOptions.cs`
* `Options\DatabaseOptions.cs`
* `Options\FeatureOptions.cs`
* `Options\LoggingOptions.cs`
* `Options\ReportingOptions.cs`
* `Options\AuthenticationOptions.cs`

### Resources
* `Resources\DataTemplates.xaml`
* `Resources\Brushes.xaml`
* `Resources\Colors.xaml`
* `Resources\Icons.xaml`
* `Resources\Spacing.xaml`
* `Resources\Typography.xaml`

### Styles
* `Styles\ButtonStyles.xaml`
* `Styles\CheckBoxStyles.xaml`
* `Styles\ComboBoxStyles.xaml`
* `Styles\DataGridStyles.xaml`
* `Styles\DatePickerStyles.xaml`
* `Styles\MenuStyles.xaml`
* `Styles\StatusBarStyles.xaml`
* `Styles\TextBlockStyles.xaml`
* `Styles\TextBoxStyles.xaml`
* `Styles\WindowStyles.xaml`

### Themes
* `Themes\LightTheme.xaml`
* `Themes\ThemeManager.cs`

### Data Layer
* `VeteranLogistics.Data\Context\VeteranLogisticsDbContext.cs`
* `VeteranLogistics.Data\DesignTimeDbContextFactory.cs`
* `VeteranLogistics.Data\DependencyInjection\ServiceCollectionExtensions.cs`
* `VeteranLogistics.Data\Entities\Base\BaseEntity.cs`
* `VeteranLogistics.Data\Entities\Administration\User.cs`
* `VeteranLogistics.Data\Migrations\` (EF Core migrations for database schema)
* `VeteranLogistics.Data\Seed\AuthenticationSeed.cs`

### Shared Kernel
* `VeteranLogistics.Shared\Constants\ApplicationConstants.cs`
* `VeteranLogistics.Shared\Constants\RegexConstants.cs`
* `VeteranLogistics.Shared\Constants\ValidationConstants.cs`
* `VeteranLogistics.Shared\DependencyInjection\ServiceCollectionExtensions.cs`
* `VeteranLogistics.Shared\Enums\OperationType.cs`
* `VeteranLogistics.Shared\Enums\RecordStatus.cs`
* `VeteranLogistics.Shared\Enums\YesNo.cs`
* `VeteranLogistics.Shared\Exceptions\BusinessException.cs`
* `VeteranLogistics.Shared\Exceptions\ForbiddenException.cs`
* `VeteranLogistics.Shared\Exceptions\NotFoundException.cs`
* `VeteranLogistics.Shared\Exceptions\UnauthorizedException.cs`
* `VeteranLogistics.Shared\Exceptions\ValidationException.cs`
* `VeteranLogistics.Shared\Interfaces\IDateTimeProvider.cs`
* `VeteranLogistics.Shared\Interfaces\IUserSession.cs`
* `VeteranLogistics.Shared\Results\Result.cs`
* `VeteranLogistics.Shared\Results\ResultT.cs`
* `VeteranLogistics.Shared\Validation\ValidationError.cs`
* `VeteranLogistics.Shared\Validation\ValidationResult.cs`

## 6. Document History

* **Created**: 2026-07-02
* **Updated**: 2026-07-10
* **Update Summary**: Aligned the document with the current folder structure, added the actual file inventory, and recorded the Phase 1.1 authentication scaffolding plus its DI registration and build-verification status. Added Phase 1.2 authentication persistence and security implementation including contracts, models, security classes, repository, validation, seeding, and DI registration. Updated Phase 1.2 to increase PBKDF2 iterations to 100,000, remove login request models (moved to Phase 1.4), and move default admin credentials to configuration. Added Phase 1.3 authentication UI implementation with LoginViewModel and LoginView. Added Phase 1.4 user authentication workflow implementation connecting Login UI with authentication infrastructure, including validation, services, converters, and startup flow changes. Added Phase 1.4A post-implementation cleanup (duplicate DI removal, debug comment cleanup) and Shell navigation fix to prevent infinite Shell nesting after login. Added Phase 1.5 Remember Me implementation with local JSON persistence for username and preference only, LoginView checkbox, and login workflow integration without automatic authentication. Added Phase 1.5A post-implementation architectural improvements: moved Remember Me loading to `InitializeAsync()`, invoked startup initialization from `MainWindow`, hardened `RememberMeService` directory and error handling, and removed dead code from `LoginView.xaml.cs`. Added Phase 1.6 Financial Year Selection implementation requiring users to select a financial year after login, including view, viewmodel, domain models, services, session context, and repository. Added Phase 1.6A post-implementation architectural improvements: decoupled Financial Year out of Authentication into its own top-level domain and corrected the DataTemplate mapping to ensure the Financial Year selection screen renders properly before Shell navigation. Added Phase 1.7 Session Management implementation strengthening runtime session infrastructure with metadata, validation, lifecycle management, and events, including SessionInfo model, SessionChangedEventArgs event, extended ISessionManager contract, enhanced SessionManager with SessionChanged event and lifecycle methods, and ApplicationContext integration. Added Phase 1.7A UserSession improvements making identity properties immutable and removing duplicate user state, including init-only properties for SessionId, LoginTime, AuthenticatedUser, FinancialYear, StartedAt, IsActive, removal of UserId and Username duplicates, UpdateLastActivity() method, and AuthenticationService refactoring to use AuthenticatedUser as single source of truth. Added Phase 1.8 Role-Based Authorization implementation introducing runtime role evaluation based on authenticated user's role, including ApplicationRole enum, IRoleAuthorizationService contract with HasRole, HasAnyRole, HasAllRoles, and IsAdministrator methods, RoleAuthorizationService implementation reading from runtime session without database access, and DI registration as Singleton. Added Phase 1.8A Strongly Typed Runtime Roles improvements eliminating runtime role parsing and using ApplicationRole enum throughout runtime session, including changing AuthenticatedUser.Role from string to ApplicationRole, adding MapToApplicationRole method in AuthenticationService to map database role string to enum during authentication, removing TryParseRole logic from RoleAuthorizationService, and performing direct enum comparisons only. Added Phase 1.9 Permission-Based Authorization implementation introducing centralized runtime permission evaluation entirely in memory, including ApplicationPermission flags enum for Driver, Vehicle, Trip, and Dispatch modules, IPermissionProvider contract for role-to-permission mapping, DefaultPermissionProvider with hard-coded role permissions, IPermissionAuthorizationService contract with HasPermission, HasAnyPermission, and HasAllPermissions methods, PermissionAuthorizationService implementation reading from runtime session and using in-memory permission provider, and DI registration as Singleton. Added Phase 1.9A Scalable Permission Representation improvements replacing flags-based enum with sealed record struct, including changing ApplicationPermission from [Flags] enum to record struct with unique string IDs, private constructor for instance control, unlimited scalability for permission growth, and updating DefaultPermissionProvider to use explicit permission arrays while preserving same permission sets and consumer API.
