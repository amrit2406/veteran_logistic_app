<UserControl x:Class="veteran_logistic.FinancialYear.Views.FinancialYearSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:veteran_logistic.FinancialYear.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Background="{DynamicResource BackgroundBrush}"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType">

    <Grid>

        <Border MaxWidth="400"
                MaxHeight="300"
                Width="400"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="24"
                SnapsToDevicePixels="True">

            <Border.Effect>
                <DropShadowEffect Color="Black"
                                  Opacity="0.08"
                                  BlurRadius="16"
                                  ShadowDepth="2"
                                  Direction="270" />
            </Border.Effect>

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Select Financial Year"
                           FontSize="24"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           Margin="0,0,0,8"
                           HorizontalAlignment="Center"/>

                <TextBlock Grid.Row="1"
                           Text="Please select the financial year you want to work with."
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           Margin="0,0,0,16"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"/>

                <ComboBox Grid.Row="2"
                          Height="36"
                          Margin="0,0,0,24"
                          FontSize="14"
                          VerticalContentAlignment="Center"
                          ItemsSource="{Binding FinancialYears}"
                          SelectedItem="{Binding SelectedFinancialYear, Mode=TwoWay}"
                          DisplayMemberPath="Name"/>

                <TextBlock Grid.Row="3"
                           Text="{Binding ErrorMessage}"
                           Margin="0,0,0,16"
                           Foreground="{DynamicResource ErrorBrush}"
                           TextWrapping="Wrap"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>

                <Grid Grid.Row="4">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Height="36"
                            Content="Exit"
                            Command="{Binding ExitCommand}"
                            Style="{DynamicResource SecondaryButtonStyle}"/>

                    <Button Grid.Column="2"
                            Height="36"
                            Content="Continue"
                            Command="{Binding ContinueCommand}"
                            Style="{DynamicResource PrimaryButtonStyle}"/>

                </Grid>

                <!-- Busy Overlay -->
                <Border Grid.Row="0"
                        Grid.RowSpan="5"
                        CornerRadius="8"
                        Background="#33000000"
                        Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">

                    <StackPanel VerticalAlignment="Center"
                                HorizontalAlignment="Center">

                        <TextBlock Text="{Binding BusyMessage}"
                                   Foreground="White"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,12"
                                   HorizontalAlignment="Center"/>

                        <ProgressBar Width="140"
                                     Height="4"
                                     IsIndeterminate="True"/>

                    </StackPanel>

                </Border>

            </Grid>

        </Border>

    </Grid>

</UserControl>